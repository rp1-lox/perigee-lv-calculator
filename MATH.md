# MATH.md — Rocket Playground Methodology

Every physics calculation in the program, where it lives, what it assumes, and where it's weak. **This file is load-bearing: if you change any math, update this file in the same commit** (CLAUDE.md enforces this). Critiques are collected at the bottom — add to them honestly when you spot a weakness; they are the to-do list for model quality.

Conventions: masses kg · thrust kN (converted to N internally) · Isp seconds · ΔV m/s unless noted · altitudes km above surface · `G0 = 9.80665 m/s²` · Earth `MU` (km³/s²) and `RE` (km) in `010-constants.js` · other bodies in `PROG_BODIES`. Every stage/engine carries ONE representative Isp — there is no altitude-dependent Isp anywhere in the program; users choose sea-level, vacuum, or trajectory-averaged values by convention (typically SL-ish for first stages, vacuum for uppers).

---

## 1. Rocket equation (the foundation)

`rocketEq(isp, m0, mf)` (140-physics.js) — Tsiolkovsky:

```
ΔV = g0 · Isp · ln(m0 / mf)
```

Degenerate inputs (mf ≤ 0, m0 ≤ mf) return 0. The program-side twins `progRocketEqDv(m_wet, m_prop_consumed, isp)` and its inverse `progRocketEqPropNeeded(m_wet, dv, isp) = m_wet·(1 − e^(−ΔV/(g0·Isp)))` live in 380 and drive mission burns.

Usable propellant everywhere excludes residuals: `prop_usable = prop · (1 − res/100)`.

## 2. Launch performance — the Townsend-Schilling model

`lvPerformance(stages, booster, pay, fairingMass, fairingJ, parkingAlt, onOrbitDV, siteLat, azMin, azMax)` (140-physics.js) is the single source of truth for ascent performance — the LV calculator, the mission LAUNCH event, the orbits-page vehicle selector, and every trade-study sweep all call it. It is an **empirical ΔV-budget model**, not a trajectory integrator: no altitude, drag, or pitch profile is simulated. Structure:

**2.1 Ideal ΔV per stage.** Serial stages burn bottom-up. For stage s with payload-above mass `spM[s]` (all stages above + payload + fairing while attached):

```
m0 = dry + prop_usable + spM[s],  mf = dry + spM[s]
ΔV_s = rocketEq(isp, m0, mf);   burn time bt_s = prop_usable / ṁ,  ṁ = F/(g0·Isp)
```

**2.2 Parallel boosters — event-driven integrator.** When booster groups exist, stage 0's ΔV/burn-time are replaced by a piecewise integration: between "events" (a group igniting at T-0 / after a predecessor / at a fixed time; a group or the core running dry) the active engine set is constant, so each interval is one rocket-equation step at the **blended effective Isp**:

```
Isp_eff = ΣF / (Σṁ · g0)        (true thrust-weighted harmonic mean, per interval)
```

Per-group `parallelMode`: `independent` (each burns its own tanks), `crossfeed` (boosters feed the core — the core reaches staging with full tanks; core mass-flow is drawn from the booster groups evenly), `throttle` (the core throttles to `coreThrottle` while that group burns — also reduces liftoff thrust). Spent groups drop their dry mass immediately. A guard caps the loop at 500 events. This block reduces exactly to the plain serial computation for a single ground-lit group (regression-tested).

**2.3 Gravity + drag losses (the empirical core).** With `Hp = parkingAlt` (km):

```
K3 = 429.9 + 1.602·Hp + 1.224e-3·Hp²         (m/s)
K4 = 2.328 − 9.687e-4·Hp                      (m/s per second)
T3s = 3·(1 − e^(−0.333·Vcirc/(g0·Isp_avg)))·g0·Isp_avg / A0
Tmix = 0.405·Ta + 0.595·T3s
ΔV_penalty = K3 + K4·Tmix
```

where `A0` = liftoff thrust / liftoff mass, `Isp_avg` = burn-time-weighted average Isp, and `Ta` = burn time **only up to reaching Vcirc** (fractional within the crossing stage via an exponential prop-fraction interpolation) — deliberately so low-thrust upper stages aren't charged gravity/drag losses for burns that happen after insertion. The K3/K4/T3s forms and the 0.405/0.595 mix are fitted constants from the Townsend/Schilling launch-performance estimation method; treat them as a package — do not tune individual constants without re-validating against known vehicles.

**2.4 Earth rotation credit.** `rotVel(lat, azMin, azMax)`: `V_rot = ωE·RE·cos(lat)·cos(az−90°)`, maximized over {azMin, azMax, 90°} and clamped to [0, ωE·RE·cos(lat)]. A site whose azimuth window excludes due east gets partial credit.

**2.5 The budget.**

```
DVasc = Vcirc + ΔV_penalty − V_rot
DVtot = DVasc + onOrbitDV
margin = ΣΔV_s − DVtot
```

**2.6 Max payload.** `lvMaxPayload(...)`: 40-iteration bisection on payload ∈ [0, 2,000,000 kg] for margin = 0 (margin is monotonically decreasing in payload, so bisection is valid). Returns 0 if even zero payload has negative margin.

## 3. Destination ΔV (`onOrbitDV`)

Two implementations that MUST agree: the original inside frozen `calculate()` (160) and the pure transcription `destOnOrbitDV(dest, siteLat)` (145) — pinned together by regression goldens captured from the real calculate() (tests/math.test.js). All burns are impulsive, all transfers two-body Earth.

**Orbit mode** (target apogee/perigee/inc from a circular parking orbit at `parkingAlt`), with `vPeri/vApo` from vis-viva:
- Target ≈ parking (both within 5 km): 0.
- Circular target (|apo−peri| < 5 km): Hohmann `dv1 = |v_peri(transfer) − Vc_park|`, then either
  - **combined plane change at apoapsis** if a plane change is needed AND apogee > 10,000 km: `√(va² + vc² − 2·va·vc·cosθ)` replacing the circularization burn (the classic GEO trick), or
  - separate circularization + plane change at parking-orbit speed: `2·Vc·sin(θ/2)`.
- Elliptical target: `dv1` raises apogee, `dv2 = |vApo(target) − vApo(transfer)|` adjusts perigee at apoapsis (skipped when |perigee − parkingAlt| < 50 km), plane change added separately (never combined).
- Plane change is only charged when `inc < |siteLat| − 0.5°` — the model assumes launch inclination = site latitude (minimum-inclination due-east launch) and that HIGHER-than-latitude inclinations are reachable by launch azimuth at no ΔV cost (the azimuth window only affects V_rot).

**Escape mode** (C3, declination): from vis-viva at the parking perigee,

```
V_inj = √(Vesc² + C3),  Vesc² = 2μ/r     →  onOrbitDV = V_inj − Vcirc(parking)
```

plus a declination plane-change penalty `2·Vc·sin(Δ/2·...)` when |siteLat| exceeds |declination| by > 0.5° (Δ = the excess). C3 below −Vesc² is rejected as an error.

## 4. Stage-and-a-half (S1.5)

`_s15BecoSplit(s)` (150). An S1.5 stage (Atlas-style: jettisonable booster engines + continuing sustainer) is split into two virtual serial phases before ANY physics:

- **BECO point** from a jettison-TWR criterion using **stage-only mass** (payload above is deliberately ignored; the comment claims < 5 % ΔV error for Atlas-class): `m_after = F_sust/(twr·g0)`; propellant before BECO is `prop_ph1 = (dry+prop) − (m_after + jet_mass)`.
- **Phase 1** (all engines): thrust = authored stage thrust; Isp = the authored stage Isp, OR — when the optional `s15_boost_isp` field is present — the **mass-flow blend** `Isp_ph1 = F_tot / ((F_tot−F_sust)/Isp_boost + F_sust/Isp_sust)`. Note the sustainer term reuses `s15_sust_isp` (stored as vacuum — see critique 10a).
- **Phase 2** (sustainer alone): thrust = `s15_sust_thrust`, Isp = `s15_sust_isp`, dry mass reduced by the jettisoned pack.

⚠️ The pure physics (`lvPerformance`/`lvMaxPayload`) knows nothing about s15 fields — every consumer must expand first. Splitters: `calculateWithS15` (worksheet), `_fleetExpandStages` (560), `_tsExpandStages` (165, also used by 167). This invariant has been violated twice; the gate has an Atlas golden guarding it.

## 5. Mission engine (program mode)

**Live stages** (370): a stage instance = dry mass + tanks (`fill`/`capacity` per propellant type). Burns drain tanks in order (`progBurnPropellant`); boiloff (`progBoiloff(fill, rate, days, insulation) = fill·e^(−rate·insulation·days)`, `progApplyStageBoiloff` applies it to every tank of a stage) is **ACTIVE (T2)**: `missionRecompute` calls it on EVERY live (non-EXPENDED/RECOVERED) vehicle's stages whenever the mission clock advances — COAST, the coast leg of a BURN (HOHMANN/TLI) or MANEUVER, and (negligibly) LAUNCH ascent time. Propellant type provenance matters here: LV stages get an Isp-inferred type from `progVehicleDefToLiveStages` (400) — always one of `LOX_LH2`/`LOX_CH4`/`LOX_RP1`, all of which have nonzero boiloff rates, so **LV stages DO boil off**. Spacecraft stages default to `propType: 'MMH/NTO'` (440) — NOT a key in `PROG_PROPELLANT_TYPES` — so an unedited spacecraft stage's tanks see `pt` resolve to `undefined` in `progApplyBoiloff` and **silently lose nothing**; only spacecraft stages whose propType is explicitly set to a `PROG_PROPELLANT_TYPES` key (e.g. `LOX_LH2`) boil off. This is a caveat, not a bug: unknown propellant = storable-by-default per critique 16f below.

**Boiloff ordering (T2)**: applied to ALL live vehicles at the point the clock advances, not just the acting one — so an idle depot boils off while another vehicle coasts/burns elsewhere. Per event type: LAUNCH — boiloff applied AFTER the ascent burn (order is immaterial; ascent is minutes, boiloff over that span is negligible — modeled but negligible, not skipped). BURN (HOHMANN/TLI) — boiloff applied AFTER the burn, for the coast LEG THAT BURN INITIATES (departure now, arrival later; matches the transit-corridor convention, critique 16e). MANEUVER — boiloff applied BEFORE the maneuver's own propellant burn, for the coast leg that PRECEDES it (the maneuver is the arrival burn ending that coast, e.g. LOI ending a TLC→LLO leg) — so a maneuver's burn draws from POST-boiloff tanks, meaning a coast that drains enough cryo propellant correctly starves the arrival burn and trips the existing burn-overdraw check (572 #2) with no separate check needed. COAST — boiloff applied for the coast's own authored `days`.

**Ascent staging** (`_missionApplyLaunch`, 570): the LAUNCH event runs `lvPerformance` on the snapshot vehicle (S1.5 expanded, boosters included in stage 0) to get `DVasc` (required) and per-stage ΔVs. It then walks stages bottom-up: stages whose full ΔV is consumed before meeting the requirement are burned dry and EXPENDED; the stage that crosses the requirement is the **insertion stage** — its propellant use comes from inverting the rocket equation on the full remaining stack (`progRocketEqPropNeeded` with mass-above included), leaving real residual propellant for later mission ΔV. Upper stages that never ignite ride to orbit untouched (there is deliberately NO forced stage-at-burnout). Spacecraft payload stages are never burned during ascent. When no performance data exists, the required ΔV falls back to `_missionDvToOrbit(body, alt) = Vcirc + fixed loss` with a per-body loss table (Earth 1550, Mars 1100, Venus 1700, Moon 20, Mercury 200, Titan 1400 m/s).

**Maneuvers** use the node-map physics (below) for required ΔV, then `progRocketEqDv/PropNeeded` against the active vehicle's stage stack. **Docking** requires `progOrbitalStateMatch`: same body, apogee/perigee within 1 km, inclination within 0.1°, LAN within 1°.

**Mission time (T1 — `missionRecompute`, 570)**: time is DERIVED from the replay, never authored as position. A mission clock (seconds) walks `m._expanded` in order; T-0 is the FIRST LAUNCH event (anything authored before it sits at T+0). Duration rules per event type:

| Event | Auto duration |
|---|---|
| LAUNCH | Ascent burn time — `lvPerformance`'s `tBT` (sum of per-stage burn times), cached onto `stagingResult.burnTime` |
| BURN (HOHMANN) | `progHohmannTOF(body, periBefore, burnParam)` — the Hohmann coast this burn initiates |
| BURN (TLI) | `progHohmannTOF('Earth', periBefore, PROG_MOON_ORBIT_R − R_Earth)` — the translunar coast |
| BURN (LOI) | 0 — arrival burn at the end of an already-counted TLI coast |
| BURN (CIRC / PLANE_CHANGE / CUSTOM) | 0 — treated as impulsive with no separate coast leg |
| MANEUVER | `progTransferTOF(fromNode, toNode)` — the coast, not the (impulsive) burn itself |
| COAST | `days × 86400` (authored — the ONLY event type where the user authors time directly; `durationOverride` is not applicable/needed here) |
| everything else (DEPLOY, SEPARATE, DOCK, EXPEND, RENDEZVOUS, transfers, REENTER, RECOVER) | 0 |

`progHohmannTOF(body, alt1_km, alt2_km)` = half-ellipse period `π·√(a³/μ)`, `a=(r1+r2)/2`. `progTransferTOF(fromNode, toNode)` classifies the node pair (mirrors `_nmDvPhysics`'s node shape): same-body circular/elliptic/surface → Hohmann half-ellipse between average altitudes; Earth → lunar transit/LLO/lunar-surface → translunar half-ellipse to `PROG_MOON_ORBIT_R`; Earth → interplanetary transit (Mars/Venus/…) → heliocentric Hohmann TOF via `PROG_HELIO_R`/`PROG_MU_SUN`, UNLESS the program has a selected Lambert launch window (`PROG_ACTIVE_PROGRAM.launchWindow.tof_days`), which is authoritative when present; anything else (surface↔surface, unmodeled pairs) → 0.

`PROG_HELIO_R` (360) now covers all eight planets (Mercury/Venus/Earth/Mars/Jupiter/Saturn/Uranus/Neptune — mean heliocentric radii, km), so `progTransferTOF`'s interplanetary branch transparently produces Hohmann TOFs for the outer planets too (previously only Mars/Venus were tabulated); critique 16c (no launch-window phasing) applies equally to these new entries — an Earth→Jupiter/Saturn/etc. TOF is a generic co-planar Hohmann estimate, not tied to an actual date. `PROG_MOON_ORBITS` (360) generalizes `PROG_MOON_ORBIT_R` into a small parent/radius map (`Moon` around Earth, `Titan` around Saturn at 1,221,900 km) for the Trajectory view's local-scene rings; it is not yet consumed by `progTransferTOF` (which still special-cases Earth↔Moon directly) — a future TOF generalization to arbitrary moon systems should read from this map instead of hand-adding cases.

**Overrides**: each log entry may carry `durationOverride` (seconds) and, for MANEUVER, `dvOverride` (m/s) — both authored fields that ride undo/autosave for free since they live on `m.log`. `durationOverride` replaces the auto duration in the clock accumulation. `dvOverride` replaces the node-map-computed ΔV requirement **before** propellant computation in `_missionApplyManeuver`, so the rocket equation (and everything downstream — flight-readiness checks, state-panel numbers) consumes the custom value consistently; there is no separate override-aware code path to keep in sync. Both are cached alongside the auto value (`e.durationAuto`/`e.durationUsed`, `e.dvAuto`/`e.dvRequired`) so the UI can badge overridden events "(custom)" and offer "reset to auto". `_metFmt(sec)` renders the running clock as `T+MM:SS` / `T+HH:MM` / `T+Nd HH:MM`; display only, never fed back into layout or physics.

## 6. Node-map ΔV physics (360 + 430)

`progNmComputeEdgeDv(from, to)` → `_nmDvPhysics`: same-body transfers compose Hohmann legs (`progDvHohmann`, vis-viva exact), plane changes (`progDvPlaneChange = 2v·sin(θ/2)`; `progDvPlaneChangeFull` gets the true angle between planes from the spherical law of cosines `cosθ = cos i1·cos i2 + sin i1·sin i2·cos ΔLAN`; `progDvCombined` vector-adds a plane change with a coplanar burn), and apoapsis circularization (GTO→GEO). Cross-body transfers are **patched conics with impulsive burns**:

- **TLI**: Hohmann from LEO to the Moon's orbital radius.
- **LOI/TEI**: v∞ at the Moon = |v_moon − v_apo(TLI)|, then hyperbolic-to-circular at LLO: `ΔV = √(v∞² + 2μM/r) − √(μM/r)` (the Oberth form). TEI is assumed symmetric to LOI. The code itself documents the known underestimate (~822 m/s vs ~900 real Apollo LOI with free-return constraints).
- **TMI/TVI + MOI/VOI**: heliocentric circular-coplanar Hohmann between planet orbits gives v∞ at departure/arrival; departure and capture burns use the same Oberth form at LEO/target-orbit radius. No plane change, no synodic timing (timing lives in the porkchop, separately).
- **Surface↔orbit**: Earth ascent uses the launch model; lunar/Mars ascent are scaled baselines (`1870 m/s` at LLO-100, `3810 m/s` at MCO-400, scaled by the ratio of target-orbit circular speeds). Descent numbers mirror ascent.

## 7. Porkchop plotter (410)

A real **Lambert solver** (universal variables with Stumpff functions C(ψ), S(ψ), bisection on ψ) over a departure-date × time-of-flight grid. **R1 (2026-07-09): planet positions come from the REAL ephemeris (§7a), projected to the ecliptic plane (z dropped) because `progLambert2D` is 2D** — a small error for the destinations plotted (Mars i = 1.85°, Venus 3.39°; see critique 42). `dep_day` now means **days past the program epoch (`PROG_ACTIVE_PROGRAM.epochJD`)** — departure days are real dates (default epoch 2026-07-09: the Earth→Mars optimum lands at dep_day ≈ 118 ≈ 2026-11-04, TOF ≈ 327 d, C3 ≈ 8.9 km²/s² — the actual late-2026 window; the old calibrated fiction pinned the optimum near dep_day 0 by construction). Output: C3 heatmap; the selected cell sets the program's launch window / coast duration. Independent of the node-map ΔV numbers. The grid cache is keyed by destination + epochJD.

**Now user-facing** (previously dormant machinery with no UI entry point). Mars/Venus only (`PROG_PORK_DESTINATIONS`); other transit destinations show a greyed "⚟ Launch window…" button with a tooltip instead. Entry points: the Add-Event MANEUVER form and the event edit modal, both on interplanetary legs (`toNode.orbit.type==='transit' && orbit.body==='Sun'`); a chip on maneuver cards shows the active window or "Hohmann default" and reopens the modal. The modal (`modal-mission-porkchop`, 570/410) renders the C3 heatmap onto a `<canvas>` (`progPorkRenderCanvas`, one pixel per grid cell, `image-rendering:pixelated`) with a live hover readout and click-to-stage selection; Apply commits the pending selection and calls `missionRecompute`. The heatmap's blue→red C3 ramp is a fixed perceptual gradient used as **data**, exempt from the chrome theming rules (`progPorkC3Color`) — canvas chrome/labels (axis captions, buttons) use `var(--…)` as usual.

`launchWindow` shape: `{ destination, dep_day, tof_days, c3 }` (`PROG_PORK_DESTINATIONS = ['Mars','Venus']`), stored on `PROG_ACTIVE_PROGRAM.launchWindow` and destination-keyed so a Mars window never affects a Venus leg — both `progTransferTOF` (360) and any leg-timing consumer check `lw.destination === dest` before using it. It rides `buildProgramObject()`/autosave for free since `PROG_ACTIVE_PROGRAM` is bundled wholesale (450/455) — no separate persistence path needed.

**Precedence** (highest wins): explicit per-event `durationOverride` (authored on the log entry) > selected `launchWindow` for that destination > generic Hohmann TOF estimate. Implemented once, in `progTransferTOF` (feeds `durationAuto`) and the mission replay's `durationUsed = overrideSec != null ? overrideSec : durationAuto` (570) — the window is just one of the two inputs to `durationAuto`, so no separate override-vs-window branch was needed.

## 7a. Body kinematics — REAL EPHEMERIS RAILS (R1, 2026-07-09)

**This section was rewritten for R1.** The old C1a model (circular, coplanar, uniform angular motion via `PROG_BODY_KINEMATICS`, with a per-mission "planet-phase calibration" render fiction via `progCalibratedTheta0`/`_trajGetPlanetCalibration`) is RETIRED. Bodies now ride their real orbits; real phases connect real arcs, so the calibration had nothing left to fix.

**Model** (360): full Keplerian mean elements + secular rates, evaluated at real absolute time.

- **Planets** — `PROG_BODY_ELEMENTS`: the JPL approximate mean elements (Standish, valid 1800–2050): a (AU), e, I, L, ϖ, Ω at J2000 + rates per Julian century. Frame: ecliptic-J2000, +x = vernal equinox. AU = 1.495978707e8 km.
- **Moon** — `PROG_MOON_ELEMENTS.Moon`: simplified mean elements about Earth (a = 384,400 km, e = 0.0549, i = 5.145° to the ecliptic; L/ϖ/Ω at J2000 with per-DAY rates incl. the 18.6-year node regression −0.052954°/day). **Mean-element-only** — no evection, variation, or other solar-perturbation terms (critique 41).
- **Titan** — schematic: circular at 1,221,870 km **in Saturn's orbital plane** (I/Ω copied from Saturn's J2000 elements). The real Titan orbits near Saturn's *equatorial* plane, tilted ~26.7° from its orbital plane — deliberately wrong-but-simple (critique 43).

**Epoch**: MET 0 = `PROG_ACTIVE_PROGRAM.epochJD` (Julian date; default `PROG_DEFAULT_EPOCH_JD = 2461230.5` = 2026-07-09 00:00 UTC — "missions start today"). Absolute time for element evaluation = `(epochJD − 2451545.0)·86400 + MET_seconds`. epochJD persists with the program object (rides `buildProgramObject()`/autosave/session wholesale; `applyProgramObject` applies the default to pre-R1 files). **No epoch UI yet** (critique 45).

**Evaluation** (`progBodyLocalEphemState` → `_progElementsEval`, 360): elements at time T → ω = ϖ−Ω, M = L−ϖ (normalized); Kepler's equation M = E − e·sinE by Newton (tol 1e-8, `progKeplerSolveE`); perifocal position (a(cosE−e), b·sinE); rotation R3(−Ω)R1(−i)R3(−ω) into ecliptic-J2000. **Velocity is analytic, never finite-differenced**: perifocal Ė-form with the TABLE's mean-motion rate dM/dt = L̇−ϖ̇ (for the Moon this differs ~0.5% from √(μ/a³) — the real lunar rate is solar-perturbed; using the table rate keeps position and velocity mutually consistent), plus the secular frame-rotation terms Ω̇·(ẑ×r) + ω̇·(ĥ×r) (the Moon's node regression alone contributes ~4 m/s — needed to meet the gate's 1e-3 km/s velocity-consistency pin).

**API surface** (all pure):
- `progBodyEphemState(body, t_s)` → heliocentric 3D `{r:[x,y,z], v}` km, km/s; recursive for moons (parent + local). The ONE body-position source.
- `physBodyStateAt(body, t_s, overrides)` (385) is now a thin wrapper over it; `overrides` is accepted and IGNORED (signature compatibility).
- `progBodyWorldPos(body, t_s)` → `{x, y, z}` (renderer reads x/y — top-down ecliptic projection; z carried for R2's 3D camera).
- `progBodyWorldPosCalibrated(body, t_s, overrides)` → **compatibility alias** that ignores `overrides` and returns the real position. Every legacy call site (574/565) kept compiling; the calibration machinery behind it (`progCalibratedTheta0`, `_trajGetPlanetCalibration`, `_trajCalibrationCache`, second-leg ghost rendering, overrides threading in 565, the "planet phases calibrated" footer) was deleted.
- `progBodyAngleAt(body, t_s)` → atan2 of the real in-plane position (planets heliocentric, moons parent-relative), normalized [0, 2π) — the angle consumers (phasing in 565, moon-lead arc rotation in 574) now phase against reality.

**Render coherence (this phase)**: the top-down view still draws heliocentric/moon rings as SCHEMATIC CIRCLES at `PROG_HELIO_R`/`PROG_MOON_ORBITS.r` while glyphs sit at their real positions — an eccentric planet visibly sits off its ring (Mercury: real r 4.60e7–6.98e7 km vs ring 5.79e7; measured ~24 px off at Mercury-fit zoom). This visibly-honest mismatch is stated in the view footer; R2 replaces the rings with true sampled orbits (critique 40).

**Gate pins (R1)**: Earth |r| at epoch ∈ [1.467e8, 1.53e8] km and perihelion/aphelion in real bands; Mercury implied e ≈ 0.2056 from min/max radius; Moon |z| amplitude ∈ [30,000, 38,000] km over a month and geocentric distance spans the real 363k–405k km band; analytic-vs-numeric velocity < 1e-3 km/s (Earth, Moon, Titan); epoch shift of +365.25 d returns Earth to within 2e5 km; porkchop optimum at the real 2026 Mars window; `progBodyWorldPosCalibrated` alias ignores overrides.

**View-time rule** (unchanged from C1a): the trajectory view's `_trajViewTime(m)` (574) picks the time used to place body glyphs — selected event's post-event time (`metStart + durationUsed`), else `m._metTotal`, else 0.

## 7b. Trajectory view LOD ramps + mission-state rendering (C2)

**LOD opacity windows**: `_trajLodOpacity(sizePx, lo, hi)` (574) replaces C1b's hard visibility culls with a linear fade — 0 below `lo`, 1 through the middle of the window, a ramp back to 0 approaching `hi` (finite windows only). Ramp width is `min((hi-lo)*0.2, lo*0.2)` — capped at 20% of `lo` itself, not 20% of the full span, because every window in this app pairs a small `lo` (8-30px) with a huge `hi` (15x the viewport diagonal, i.e. "effectively never fades back out") — an uncapped 20%-of-span ramp would be thousands of px wide and make anything just past `lo` read as nearly invisible. `hi = Infinity` (or any non-finite/≤lo value) selects "fade in only, no ceiling" mode, used by the zone-of-influence window. Windows (feature: `[lo px, hi multiplier of viewport diagonal]`): heliocentric planet ring `[8, 15x]`; moon ring `[10, 15x]` (inherited from parent, see below — not applied standalone); mission orbit ring `[12, 15x]`; transfer arc `[10, 15x]`; zone-of-influence `[30, ∞]` (fade-in only). Body discs keep their pre-existing min-px clamp + true-scale rule with NO fade (a body is always at least visible).

**One fade authority per feature**: a moon's ring and a body's embedded mission content (its own moons + mission orbit rings/arcs) do NOT apply their own LOD window on top of their parent's — they inherit the parent body's zone-of-influence alpha (`parentZoiAlpha` / `zoiAlpha` in `_trajWorldSVG`) as a single multiplicative wrapper `<g opacity="...">`. A ring's own window (`missionOrbitRing`, `transferArc`) governs ONLY that feature's fade within an already-visible zone — it is never multiplied against the parent's zone alpha a second time.

**Far representation** (fixes the C1b zoom-out handoff): a planet's disc never fades (unchanged true-scale/min-clamp rule), but its NAME label is force-eligible — bypasses the normal screenSize/minSize gate — whenever `trueR * zoom < _TRAJ_MIN_BODY_PX`, i.e. whenever the disc itself has hit its min-px clamp (we're zoomed out far enough relative to THAT body that it's rendered as glyph+name rather than a legible true-scale disc). This is evaluated per-body, not tied to the ring's own visibility window, specifically because the 8 heliocentric rings span wildly different radii (Mercury's ring is tiny next to Neptune's) — at full solar zoom-out every body is equally "far" even though its ring's own LOD alpha may already be 0. Overlay symbology (labels/markers) inherits its parent feature's resolved alpha via a `opacity` field threaded through `_trajRegisterLabel`/`_trajResolveLabels` — a label still needs BOTH its feature's alpha > 0 AND its own size/collision gate to render.

**Corridor state rings are gone**: `_trajCorridorMoon` (a peri≈LEO / apo≈moon-orbital-radius snapshot detector) now SUPPRESSES ring generation entirely at the extraction layer (`addOrbitRing` returns `null` for a corridor-shaped snapshot) rather than relabeling it "TLC corridor" — transfer arcs carry all transit meaning now, so a mid-coast vehicle snapshot no longer draws a redundant giant ellipse ring alongside its arc.

**Moon-lead arc orientation**: `_trajArcRotationForTarget(ox, oy, targetX, targetY)` (pure) solves the transfer-arc rotation so its arrival endpoint lands on a specific target point rather than a fixed schematic angle. Derivation: `_trajTransferArcPath`'s arrival endpoint (local `(-rApo·scale, 0)`, since periapsis is fixed at local angle 0) lands at screen angle `180° + rotDeg` after rotation, so `rotDeg = atan2(dy,dx)·180/π − 180` (normalized to `(-180°,180°]`) makes it land on `(targetX,targetY)`. Applied at two sites: (a) the Sun-frame interplanetary case, target = `progBodyWorldPos(destBody, t_arrive)` where `t_arrive = leg.metArrive` (or current viewTime if the leg has no recorded arrival); (b) the local-moon case (e.g. Earth-frame TLC arriving at the Moon — though see the Moon-frame patched-conic note below for how this leg actually gets drawn), target = `progBodyAngleAt(destMoon, t_arrive)` projected onto the moon's ring. A dim dashed-outline **ghost marker** (`_trajGhostMarker`) is drawn at the arrival-time position, LOD-gated and labeled "`<body>` at arrival", ONLY when that position differs from the body's current-viewTime position by more than 3px on screen — otherwise the Moon hasn't visibly moved and the marker would just be clutter.

**Patched-conic seam in the destination's own frame** (`_trajLocalRadius`, C2 fix, then C2-review fix): a transit leg whose `destination` field equals the FRAME body itself (e.g. rendering the Moon frame, and the leg's `fromO` is `{type:'transit', destination:'Moon'}`) has no finite body-centered departure radius — the true departure point is a hyperbolic approach relative to the Moon, which this app doesn't model (patched-conic ΔV only, §6). Previously this made `_trajLocalRadius` return `null` for the departure end, so the leg's arc silently never rendered AT ALL in the destination's own frame (only the parent-frame stub burn-marker showed) — the actual TLC coast arc with its vehicle dot was invisible. Fix: a schematic fallback returns a finite "SOI edge" departure point, giving the arc a finite (if arbitrary) starting radius to draw from. **Follow-up fix (C2 acceptance review):** the fallback originally used `20 × the destination body's OWN radius` (~34,748 km for the Moon) — a value with no relationship to the actual departure-arrival gap. Because every LOD gate in this file is evaluated in real screen px (one-fade-authority rule, above), that made the leg's visibility a function of camera zoom alone: at Moon anchor the camera is zoomed in tight enough that even this tiny schematic radius reads as a large on-screen arc past the `transferArc` window's 10px floor; at Earth anchor (zoomed out to frame the whole Earth-Moon gap) the SAME arc's drawn extent collapsed under that floor and the leg silently vanished — even though the mirror leg in the parent (Earth) frame, which correctly uses the moon's full ORBITAL radius (`mo.r`) as its far endpoint, rendered fine at the same zoom. Fixed by scaling the SOI-fallback radius off `mo.r` instead (`max(2×bodyR, 0.5×mo.r)`; ~192,200 km for the Moon) so both ends of the cross-frame leg agree on the physical scale of the gap they schematically bridge, and the arc's drawn extent clears the `transferArc` LOD window's px floor with headroom at typical Earth-fit zoom (verified in-browser: ~19px vs. the 10px floor, up from ~4px pre-fix). Not a physical Hill-sphere/SOI calculation — the real value is picked purely to keep the arc's on-screen size consistent and above the LOD floor across camera anchors, not to model the true hyperbolic approach geometry (the Moon's actual Hill sphere is far smaller, ~60,000 km ≈ 0.156×mo.r).

**Mission-state trimming**: each transfer leg is classified relative to the current `viewTime` (`vt`) using its `met`/`metArrive` (both required — a zero-duration/impulsive leg, e.g. an injection burn with `met === metArrive`, always renders "planned" since it has no coast to trim): **history** (`metArrive <= vt`, dimmed to `_TRAJ_HISTORY_ALPHA = 0.35`, solid stroke) — the leg has fully completed; **current** (`met <= vt < metArrive`, full strength, solid stroke, PLUS a schematic vehicle dot) — the leg spans the current viewTime; **planned** (`met > vt`, today's dashed rendering, unchanged) — not yet departed. Orbit rings get the same history treatment when ALL their owning vehicles have a recorded vehicle-level `EXPEND` at or before `vt` (`expendMetByOwner`, keyed by `_originKey` — stage-level EXPENDs don't retire a whole vehicle so are intentionally not tracked here; a multi-owner co-located ring with even one surviving owner stays current).

**Vehicle position (schematic, NOT Kepler)**: for a "current" leg, `_trajLegPathFraction(metDepart, tof, viewT) = clamp((viewT-metDepart)/tof, 0, 1)` gives a LINEAR path-length fraction, and `_trajArcPointAt(r1,r2,scale,rotDeg,ox,oy,t)` walks the SAME half-ellipse `_trajTransferArcPath` draws (parametrized by `theta = t·π` sweeping periapsis→apoapsis) to place a dot exactly on the rendered path. **Critique**: this is uniform PATH-LENGTH-ANGLE progress, not true orbital motion — a real vehicle moves faster near periapsis (Kepler's second law / true anomaly rate is non-uniform), so the dot's on-screen speed is constant while a real vehicle's isn't. Acceptable for a schematic "where roughly is it" indicator; not a claim about real transit speed at any point along the arc.

## 7c. Physics core P0 — two-body machinery (385)

Foundation layer for the n-body plan (PHYSICS_PLAN.md); **not yet consumed by any mission math** — P0 adds capability, changes no behavior. All 3D-native (`[x,y,z]` arrays; the program passes z = 0 until the P5 camera).

- **Elements ⇄ state** (`physElementsToState` / `physStateToElements`): classical set {a, e, i, Ω, ω, ν}, perifocal→inertial rotation R3(−Ω)R1(−i)R3(−ω). a < 0 hyperbolic. Degeneracies pinned explicitly: e ≈ 0 → ω := 0, ν measured from node (or +x if equatorial); i ≈ 0 → Ω := 0, ω = longitude of periapsis. Threshold 1e-10.
- **Universal-variable propagation** (`physKeplerPropagate`): Curtis Alg. 3.4 — Newton on χ with Stumpff C/S (single copy, moved from 410; the Lambert solver consumes the same functions), then f/g and ḟ/ġ. One code path for ellipse/parabola/hyperbola; returns null on non-convergence (80-iteration cap) rather than a wrong state.
- **Body rails states** (`physBodyStateAt`): since R1 a thin wrapper over `progBodyEphemState` (360) — the ONE body-position source — full 3D element-evaluated position AND analytic velocity, recursive for moons. The `overrides` param is a retired-calibration vestige, ignored.
- **Pinned in the gate**: round-trips to 1e-9 across LEO/GTO/Molniya-like/inclined/hyperbolic; energy and |h| invariance along propagation; full-period return to start; half-ellipse arrival time consistent with `progHohmannTOF`. R1 re-goldens (2026-07-09): Moon speed rel. Earth at t=0 = 1.0618 km/s (was the circular 1.018 pin — real speed varies ~0.97–1.08 over the eccentric orbit); Earth speed at epoch 29.292 km/s (was 29.78 — July is aphelion season); Moon state has z ≠ 0 (real 5.145° inclination); the old v⊥r and z=0 pins are gone (real orbits are neither circular nor coplanar).

## 7d. Physics integrator P1 — restricted n-body propagation (386)

Vessels are massless test particles; bodies ride analytic rails (`physBodyStateAt`, §7c). **Not yet consumed by mission math** (P2 wires it in). Model:

- **Frame policy**: integration happens in the vessel's dominant body's frame (its SOI), with perturbers in the standard relative-motion form — direct minus indirect tidal terms, `μ_p[(d−r)/|d−r|³ − d/|d|³]`. With one perturber this IS the restricted three-body problem in relative coordinates; it also keeps coordinates small near the body that matters.
- **SOI model**: `r_SOI = a·(μ/μ_parent)^(2/5)` (Moon ≈ 66,183 km, Earth ≈ 9.25e5 km, pinned in gate). Frame handoffs (`physPatchState`) convert position AND velocity through the shared rails; transitions are detected per-step and recorded as events.
- **Integrator**: leapfrog (kick-drift-kick), 2nd-order symplectic — energy error oscillates instead of drifting (measured ~1e-14 relative over 100 LEO orbits). Step size = local circular-orbit timescale / 360, quantized DOWN onto a fixed power-of-4 ladder (1…65,536 s) — quantization is what makes runs **bit-for-bit deterministic** (replay/undo/golden safety).
- **Events**: apsides via r·v sign change; SOI crossings; `physFindEventTime` bisection available for precise event location.
- **Single entry point**: ALL propagation goes through `physPropagateSegment` (hard invariant — no ad-hoc integrators). Sample polylines decimated to ≤ maxSamples with endpoints/frame-changes preserved.
- **Measured fidelity (2026-07-09 tuning run)**: two-body phase error ~0.3 km/orbit at LEO (vs analytic Kepler); Jacobi constant on a violent e=0.95 lunar-grazing orbit drifts ≤ 7.7e-4 relative over 15 days. **Free-return golden re-derived for R1's real rails (same day)**: old circular-rail seed (apogee 455,000 km, burn angle 4.5379 rad → 62 km return perigee) → new seed against the real eccentric/5.145°-inclined Moon: **apogee 445,000 km, burn angle 4.98 rad → 201 km return perigee** (scan: apogee × burn-angle grid, scratchpad `r1_scan.js`); pinned band [0, 2000] km unchanged.

## 7e. Physics mission bridge P2 — solved node burns (565 + 570 hooks)

First phase where the physics stack is CONSUMED by the mission model. `PHYS_ENABLED` (565, const, true) gates everything; results live in the module side-table `_physTrajByMission[missionId] = {legs}` — never on the mission object (autosave/undo serialize `m` wholesale; verified blob-size parity in-browser).

- **Solved burn (`physSolveNodeBurn`)**: for an existing node-map MANEUVER, the departure state is a circular parking orbit around `fromOrbit.body` at its mean altitude; the burn POINT is chosen by **analytic phasing** (`physPhaseBurnAngle`): the Hohmann-style transfer spans π of true anomaly, so the burn point sits diametrically opposite the destination's railed position (`progBodyAngleAt`) at `t_dep + TOF_schematic` (same geometry as 574's `_trajArcRotationForTarget`). Interplanetary legs use the same rule against the destination planet's heliocentric angle — deliberately coarse; P4's shooter refines aim. The burn VECTOR is prograde with **magnitude taken verbatim from the existing ΔV engine** (`progNmComputeEdgeDv` / `dvOverride`) — ΔV *required* accounting is therefore byte-identical with physics on vs off (verified: Apollo seed 3,143 / 822 m/s unchanged).
- **Leg propagation (`physRebuildMissionTrajectories`)**, called at the `missionRecompute` tail before autosave/undo/checks: injection legs (→ transit corridor) are propagated with `physPropagateSegment` (ctx bodies Earth/Moon/Sun cislunar, Sun/Earth/planet interplanetary) to 1.5× the schematic TOF; `converged` records whether the trajectory actually entered the destination SOI (a miss is EXPECTED pre-P4 and recorded honestly). Same-body legs are pure two-body: 64 analytic `physKeplerPropagate` samples, no integrator (perf; null-return handled). The EXITING corridor leg (TLC→LLO) carries no propagation of its own — the injection leg already flew it — but receives the physics coast TOF from that propagation's SOI-entry/periapsis timing.
- **Duration precedence** is now `durationOverride > physics leg TOF > launchWindow > Hohmann` (the last two inside `progTransferTOF`). **Stale-by-one convergence dance**: durations are consumed DURING replay but legs are built AFTER it, so replay N reads the side-table from rebuild N−1 (`physMissionRecomputeBegin` rotates current→previous at recompute top; `physLegTofFor` only trusts a leg whose from/to nodes and departure MET still match). After each rebuild, any leg whose physics TOF disagrees >1% with the duration the replay actually used triggers exactly ONE extra recompute pass (`_physRecomputePass` depth guard — never loops). Measured on the Apollo seed: the TLC coast shortened from the schematic 430,059 s Hohmann to the propagated 406,480 s Moon-SOI periapsis arrival, converging in one extra pass; full recompute incl. both passes ≈ 12 ms (< 50 ms budget).
- **Reconciliation delta (Apollo seed, physics on vs off)**: ΔV *required* and injection propellant identical (3,143 m/s / 79,111 kg). The LOI leg's *delivered* ΔV and propellant shift 638→639 m/s and 9,959→9,967 kg — NOT a burn-model change but the physics TOF shortening the coast ~6.5 h, so cryo boiloff over the coast drops and slightly more propellant survives to the arrival burn. This is the intended consequence of physics TOF joining the duration chain (boiloff is a function of coast time, §5 T2).
- **MNODE** (`{type:'MNODE', at:{kind:'met', value_s}, dvPro_ms, dvRad_ms, dvNrm_ms}`, authored via `missionExecManeuverNode` — console/API only until P4's dock UI): burns propellant through the SAME rocket-equation path as MANEUVER burn steps with |Δv| = √(pro²+rad²+nrm²); advances NO orbit state.

## 7f. Physics render integration P3 — polylines + SOI spurs (574 + 385)

First phase where the propagated trajectories are DRAWN. The trajectory view's leg loop consults the physics side-table (`physMissionLeg`, §7e) per authored MANEUVER: a hit with samples renders a **sampled polyline** instead of the schematic Hohmann half-ellipse; a miss (physics off, unconverged aim with no frame-local samples, degenerate legs) falls back to the schematic arc unchanged.

- **Continuous multi-frame drawing convention (patched-conic, KSP-style; reworked 2026-07-09 after first flight)**: leg samples are `{t, r, frame}` with `r` relative to the sample's frame body AT TIME `sample.t`. The leg's HOME pass (the frame its schematic record lives in) draws the WHOLE path as one continuous polyline: every sample maps to `anchorOf(sample.frame) + r·zoom`, where `anchorOf` resolves each frame body's CURRENT DRAWN position at viewTime (never absolute heliocentric sample positions — bodies move between sample.t and viewTime and absolute positions would smear). The original per-frame rendering made the Moon-frame passage inherit the Moon's ZOI fade, so at Earth-anchor zooms the trajectory literally vanished at the encounter and reappeared after — a continuous trajectory must never gap, so cross-frame sub-segments now bypass the destination's ZOI gate by construction (they belong to the home pass). Frame seams: the two gluings disagree by the body's drift between the handoff sample's t and viewTime, and connecting them with a chord read as a phantom straight-line burn (user-reported 2026-07-09 under the R2 tilted camera, where the gap grows) — so the polyline now BREAKS at each frame handoff and a small dashed circle marks the SOI transition (tooltip: "SOI handoff: A → B · MET"). The destination frame's arrival-leg pass emits ONLY the arrival burn marker at the clip point (no redraw, no double-draw).
- **Converged-only rule (2026-07-09; amended same day by P4)**: a physics polyline renders ONLY when the propagation reached its destination SOI (`converged`). An untargeted ballistic path is a line to nowhere on screen (first-flight Venus report) while the schematic calibrated arc DOES visually connect. **P4 update**: interplanetary and moon legs are now aimed by the differential corrector (§7g), so they normally converge and graduate to physics polylines; the schematic arc remains the fallback for legs the fixed |Δv| genuinely cannot deliver (converged:false is still legal and honest). MNODE legs (§7g) are exempt from the gate entirely — a vector burn's ballistic path IS the authored content and draws unconditionally.
- **Position coherence (R1 update — closes critique 35's calibration half for real)**: `physRebuildMissionTrajectories` phases burns against the REAL ephemeris angle and both physics and renderer resolve positions through `progBodyEphemState` — one position source, no fiction left to disagree about. (The pre-R1 text here described threading the mission's calibration overrides through physics; that machinery is deleted.)
- **Clipping**: the propagation runs to 1.5× the schematic TOF (§7e), but the drawn polyline is clipped at the EXITING leg's `metArrive` (`_trajPhysClipT`) so post-capture integrator samples don't render as a phantom continuation past the arrival burn.
- **Leg state & vehicle dot**: planned/current/history treatment is derived from the PROPAGATED span (departure MET → clip time / last sample), not the impulsive schematic record's met === metArrive. The "current" vehicle dot linearly interpolates the bracketing samples in the drawn frame at viewTime (`_trajPolylinePointAt`) — this retires the linear-path-fraction schematic (critique 21) for physics legs; sample spacing follows the integrator's step ladder, so the dot's on-screen speed is now (piecewise-linearly) Kepler-true.
- **Escape/capture spurs** (`physEscapeGeometry`, 385 — pure, gate-pinned): an interplanetary injection leg lives in the Sun frame's leg list, so the departure body's local frame previously showed nothing. P3 draws the escape hyperbola from the parking-ring periapsis out to the SOI edge (`r_SOI` per §7d): e = 1 + r_p·v∞²/μ (v∞² = C3 from the transit node spec), asymptote half-angle β = acos(1/e), conic r(ν) = p/(1+e·cosν) sampled from ν = 0 to r = r_SOI. The spur is rotated so the OUTGOING asymptote (the drawn far end) aligns with the departure body's heliocentric velocity direction — prograde for outbound (superior) destinations, retrograde for inbound — with the burn marker at the hyperbola periapsis, which sits exactly on the parking ring by construction. A mirrored **capture spur** renders at the arrival planet when no physics samples reached it (expected pre-P4): incoming asymptote anti-parallel to the schematic arrival v∞ direction (≈ −v_planet for arrivals from an inner origin), sweep mirrored. This replaces the arbitrary R×20 schematic fallback for interplanetary arrivals.
- **LOD/fade discipline**: polylines and spurs are embedded body-frame content — they inherit the body's zoiAlpha from the caller's group wrapper (one fade authority) and gate their OWN drawn extent through the existing `transferArc` px window; the extreme-end numerical culls (extent < 0.5px, > 20× viewport diagonal) apply unchanged.

### 7f-R2. 3D camera + true-geometry rendering (2026-07-09, R2)

- **ONE projection seam** (`_trajProjectVec`, pure + gate-pinned): rotate the floating-origin world vector by az about the ecliptic normal, tilt by (π/2−el) about the screen-x axis, drop the view axis as `depth`. At el=π/2, az=0 it is EXACTLY the pre-R2 mapping (identity pins in the gate) — 3D is a superset, not a re-projection. Every emission site projects through the pass context (`_trajProj3` for 3D vectors, `_trajProjLocal` for planar ecliptic offsets); projection linearity lets local geometry project as offsets added to the body's projected anchor.
- **True-geometry rings**: heliocentric and moon rings sample the body's REAL orbit (`progBodyOrbitElementsAt` + `progOrbitSamplePoints`, uniform eccentric anomaly, 120 pts) — Mercury's drawn min/max radius ratio measured 0.6587 vs (1−e)/(1+e)=0.6589, and its glyph now rides its own orbit (critique 40 RESOLVED). Mission orbit rings sample {a,e from peri/apo, i = authored inclination, **Ω=ω=0 by convention** — annotated in the tooltip, not faked}. The `<circle>`/`<ellipse>`+arc-flag orbit emitters retired (schematic transfer arcs are sampled+projected too, `_trajArcProjectedPath`; 230's flat mini-diagram keeps the original helper).
- **Painter depth sorting**: emissions are {depth, svg} records sorted back-to-front, STABLE (emission order tiebreaks at el=90° where depths degenerate, preserving pre-R2 layering). Granularity is per-BODY (glyph + embedded content share the body's center depth); orbit rings span all depths and draw behind all glyphs by construction — exact painter order for a ring is undefined anyway.
- **Interaction**: shift-drag / right-drag rotates (el clamped [5°, 90°], ~30 fps throttle, measured 1.3 ms/frame); pan and cursor-anchored zoom un-project the screen delta into the ecliptic plane (divide v by sin el, inverse-az rotate) so both track the cursor under tilt; the mid-drag viewBox slide uses the PROJECTED planar offset delta (exact, projection is linear). Reset returns to canonical top-down; fly-to preserves orientation. Ecliptic reference grid fades in with tilt (alpha ∝ sin tilt).
- **Verified**: Earth's ecliptic ring foreshortens to exactly 0.500 at el=30°; the Moon's ring measures 0.4326 (its real 5.145° plane visibly differing from the ecliptic); Earth/Moon painter order flips when az rotates by π; Apollo polyline continuous at el=90° and 30° (endpoint 1.8 px from the drawn Moon top-down; 8.6 px at tilt — the z=0-vessel vs real-Moon-z gap R3 closes, stated in the footer).

## 7g. Physics targeting & authoring P4 — differential corrector, MNODE, free return (565 + 570 + 574)

The phase that makes the propagated legs actually HIT their targets, and makes MNODE a real, propagated, drawable event.

- **Generic corrector (`physShootToTarget`)**: n-DOF (n = 1 or 2) Newton iteration on x with a finite-difference Jacobian (per-component eps, default 1e-3 rad) and step-halving damping (a trial step whose miss norm grows — or goes non-finite — is halved up to 4 times; if no damped step improves, the solve terminates). Hard termination guarantees: singular/non-finite Jacobian → `converged:false`; propagation budget (`maxProps`, default 40) → `converged:false`; `maxIter` default 12. All trajectory evaluation goes through the caller-supplied `opts.propagate`, which is always `physPropagateSegment` (single-integrator invariant). Deterministic by construction (fixed eval order, no randomness, deterministic integrator).
- **ΔV accounting parity (sacred)**: for existing node-map MANEUVER legs the corrector adjusts the burn POINT (anomaly θ on the parking ring) and the burn DIRECTION (in-plane pitch off prograde) — NEVER the magnitude. |Δv| remains verbatim from `progNmComputeEdgeDv`/`dvOverride`; required-ΔV and propellant totals are byte-identical with the shooter on or off. A target unreachable at the fixed magnitude returns `converged:false` cleanly and the renderer keeps the schematic arc.
- **Leg aiming (`physShootLegAim`)**: seeds from the P2 analytic phased solve (`physSolveNodeBurn`), then shoots 1-DOF on θ with miss = closest-approach-to-destination − targetRadius (targetRadius = destination body R + the mean altitude of the first later arrival orbit in the log, else 100 km; tolerance SOI/3 — **loosened for R1** from min(SOI/3, max(1000 km, 0.25·targetRadius)) because the burn is still in the ecliptic plane while real targets are inclined: the closest approach cannot go below the target's out-of-plane offset at encounter, up to sin(5.145°)·r ≈ 36,000 km for the Moon; R3's normal-direction DOF re-tightens this). If 1-DOF stalls it escalates to 2-DOF (θ + pitch) with a second miss component pinning the closest-approach TIME to the schematic arrival (seconds × 0.5 km/s → km scale) so the 2×2 Jacobian is full-rank — chosen over the plan's `[missKm, 0]` sketch because a constant zero row is singular by construction. Closest approach prefers exact destination-frame periapsis events over (decimated) sample minima. `converged` on the stored leg still means "propagation entered the destination SOI" — the shoot result only improves aim; even an unconverged shoot's best x replaces the analytic seed.
- **Encounter step cap (`ctx.dtMax`, 386)**: heliocentric cruise otherwise reaches the 65,536 s ladder rung — ~1.5M km of relative motion per step, enough to step clean OVER a planet's SOI (found on the combined Apollo+Mars mission: the same Mars leg converged or not depending on where the coarse steps happened to land). Interplanetary legs (shooter AND rebuild propagation, which must match) cap the ladder at the 16,384 s rung — a fixed constant, so determinism is preserved; the cap merely selects a smaller rung. Cislunar legs need no cap (the ladder is already fine at lunar distances). Mercury's small SOI (~112k km) can still be step-skipped at this cap — accepted; converged:false falls back to the schematic arc honestly.
- **Shooter cache**: solutions cached in a module map keyed by `fromNode|toNode|met(0dp)|dv(1dp)` (R1 dropped the calibration-override component with the calibration itself) — re-shot only when the leg's signature changes, so a warm recompute costs ~one propagation per leg (the stale-by-one convergence pass, §7e, rides the cache for free).
- **MNODE propagation**: a vector burn takes the ACTIVE vehicle's node-map orbit at its MET (cached onto the log entry as `orbitAtBurn` by 570's replay — same derived-cache pattern as `orbitAfter`), reconstructs the mean-altitude circular ring (same reconstruction as `physSolveNodeBurn`), places the burn at anomaly θ = n·MET (circular mean-motion phase — the burn point is a deterministic function of authored MET, so no separate anomaly field is needed), applies Δv in the orbit's local frame (prograde unit + radial-out unit; **normal is ignored in the 2D world** and its input is disabled/annotated in the UI), and propagates for min(30 days, 3 post-burn periods when the new orbit stays inside 0.8·SOI). If the vehicle sits in a transit corridor or on a surface the leg records `converged:false` with a note (no parking ring to reconstruct — the corridor is a schematic, not a state). MNODE legs render UNCONDITIONALLY (no converged gate) via the same continuous multi-frame polyline path as §7f, clicking selects the event. Propellant/ΔV accounting is unchanged from §7e (|Δv| through the rocket equation).
- **Free-return template (`physFreeReturnSolve`)**: an AUTHORING solve, so here the magnitude IS free (the user is creating a new burn, not re-rendering an accounted one). 2-DOF shoot on x = [burn MET, |Δv|] — the MET dimension sweeps the burn anomaly through θ = n·MET, round-tripping exactly through the MNODE leg builder. Seeded from the R1 golden (transfer energy of a 445,000 km apogee; burn angle 4.98 rad rotated with the Moon's real in-plane ephemeris angle at departure — old circular-rail seed was 455,000 km / 4.5379 rad). Targets: lunar flyby distance at the seed trajectory's own measured value (or 0.15·SOI fallback) AND Earth return perigee in the [30, 500] km band (aimed at the seed's own perigee when already in band). With the real eccentric/inclined Moon the rotational-symmetry shortcut is only approximate (critique 38) — the corrector does real work at nonzero departure METs (measured: t=0 converges at 0 iters, t=2 d at 4 iters, both in-band). The UI prefills the Vector Burn fields with the solved values; it never auto-pushes.
- **Pinned in the gate** (14 asserts): Earth→Moon aim converges (miss < SOI/3, ≤12 iters) and the solved aim actually enters the Moon SOI; |dv| magnitude parity to 1e-12; unreachable dv/10 case returns converged:false without throwing in a bounded propagation budget; a singular-Jacobian corrector case terminates; free-return solve from 185 km LEO converges with return perigee in the P1 golden's [0, 2000] km band and a plausible 3.0–3.3 km/s magnitude; both solvers bit-deterministic across repeat calls.

## 7h. 3D physics coherence R3 — inclined vessel states, MNODE normal, shooter yaw (565 + 570, 2026-07-10)

The phase where the MISSION layer stops passing z = 0. The physics core (385/386) was 3D-native from P0; R3 makes the mission bridge construct 3D states.

- **Inclined departure states**: `physAimBurnState` (the ONE parking-ring state constructor, now shared by `physSolveNodeBurn`, the shooter, the MNODE builder and the free-return solver) builds the ring state from elements {a = r1, e = 0, i = authored inclination, **Ω = 0 convention**, ω = 0, ν = θ} via `physElementsToState` — the SAME orbit the R2 ring renderer draws, so the drawn ring and the physics orbit are one object. The prograde unit is v̂ of that state; the burn direction is d̂v = cos(yaw)·(cos(pitch)·v̂ + sin(pitch)·r̂) + sin(yaw)·ĥ, exactly unit length by orthonormality, so **|dvVec| = the engine-supplied magnitude to 1e-12 regardless of aim** (parity preserved through the new DOF). With i = yaw = 0 the construction reproduces the pre-R3 planar states bit-identically (gate-pinned). Note θ is now the in-plane true anomaly, not an ecliptic angle — the analytic phasing aims the same way and the shooter refines.
- **MNODE Normal comes alive**: the MNODE burn vector is pro·v̂ + rad·r̂ + nrm·ĥ on the mean-altitude ring in the orbit's authored plane (θ = n·MET convention unchanged). The Add-Event dock / edit modal Normal inputs are enabled (annotation "+ along the orbit normal ĥ"). A nrm-only burn rotates the orbit plane about r̂ by atan(dv_n/v) — gate-pinned via the h-vector rotation angle, plus a pure two-body propagation holding the new plane.
- **Shooter 3rd DOF (yaw)**: `physShootLegAim` escalates 1-DOF (θ) → 2-DOF (θ + pitch) → 3-DOF (θ + pitch + yaw), each stage only when the previous stalls above the solver tolerance. The 3-DOF miss vector is [closest-approach − targetR, arrival-timing × 0.5 km/s, out-of-plane miss dz at closest approach] — dz gives yaw a dedicated equation. `physShootToTarget` gained an n = 3 Cramer solve. **Moon-leg acceptance tightened to min(SOI/3, 25,000 km)** with a stricter 5,000 km SOLVER tolerance driving the Newton loop (the solve keeps polishing past the acceptance bar; a stall between the two is still accepted honestly, reported with its measured miss). Propagation budget 12 + 16 + 30 = 58 per leg; the per-signature cache (now including the departure inclination) makes warm recomputes ~free.
- **Free-return solve in 3D**: `physFreeReturnSolve(alt, t0, overrides, incDeg = 28.5)` solves from the active vehicle's authored orbit plane (570 passes `orbitState.inclination`). The R1 rotated-golden seed shortcut is replaced by a **deterministic seed scan**: apogee candidates track the Moon's real geocentric distance at the ~4.7 d encounter (+60k/+75k km past it — the eccentric Moon spans 363k–405k km, fixed apogees strand off-phase departures), a 24-point burn-anomaly grid, two-level phi refinement (1/8 then 1/64 coarse steps — the corridor is ~0.01 rad wide) and a ±16k km apogee refinement. Solutions round-trip exactly through the MNODE builder (same inclination + θ = n·MET). **Window coverage is physical, not universal**: from a fixed Ω = 0 inclined ring the Moon must be near the ring's plane at encounter, so some departure METs have no free return at the fixed-ring geometry (measured @28.5°: t0 = 0 and 5 d converge, t0 = 2 d and 10 d honestly fail) — the UI's "try a different MET" message is the designed answer.
- **Solved RAAN for moon-leg departures (added at first-flight verification, 2026-07-10 — R3.1's tier-3 rule pulled forward because physics NEEDS it)**: with Ω pinned to 0, a 28.5° parking plane can sit up to ~34° from the Moon's plane, and a fixed-|Δv| TLI physically cannot cross that gap (yaw redirects; it cannot buy plane change) — the live Apollo seed stalled at ~1.1e5 km miss. `physShootLegAim` now solves Ω so the ring's plane CONTAINS the target's position at scheduled arrival (n(Ω)·m̂ = 0 → a·sinΩ + b·cosΩ = c, two roots — ascending/descending geometry — both tried; no real root or interplanetary legs keep Ω = 0). This is precisely "launch into the plane of the target", which is what real missions do with launch timing. The solved Ω rides the aim cache and the leg rebuild (`aim.raan`); the DRAWN parking ring still uses Ω = 0 until R3.1 wires state-derived ring orientation — a known, temporary visual disagreement.
- **Deterministic seed grid**: before Newton, a 16-point θ grid × each Ω root (cheap scan-quality propagations: 1.1× cutoff, `stepsPerOrbit` 90, 8 samples) finds the basin — the analytic phase angle is an ECLIPTIC angle while ν is in-plane, so for inclined departures the analytic seed alone can strand Newton in a ~1e5 km local minimum.
- **Step-ladder perf re-derivation (2026-07-10)**: cold re-shoot measured 6.9 s — 612k integrator steps, dominated by the pow-4 ladder quantizing "want 14.7 s" down to 4 s (~3.7× more steps than the accuracy policy asks). Ladder densified to powers of 2 (still fixed = still deterministic) + `ctx.stepsPerOrbit` override for scan-quality propagation. Two goldens re-derived with dated comments: LEO rung 4 s → 8 s; two-body 20-orbit error bound 10 km → 30 km (measured 23.35 km — dt doubled → 2nd-order error ×4; deliberate accuracy-for-speed trade, schematic-grade per critique 28). Measured after: cold seed ~150–250 ms, full re-shoot 90 ms, warm recompute ~5 ms.
- **Render coherence**: no 574 code changes — polylines already project 3D samples. Measured endpoint-to-drawn-Moon gap: 12.8 px top-down / 6.5 px at el = 30° — both are the REAL ~38,000 km flyby periapsis offset foreshortened differently (R2's artifact pattern — small top-down, large tilted — is gone). The footer's "coplanar until R3" note replaced with "vessel orbits: Ω,ω assumed 0".
- **Pinned in the gate** (254 → 275): inclined ring radius/i/z-amplitude; i=0 bit-back-compat; |dvVec| parity at nonzero pitch+yaw; inclined `physSolveNodeBurn` h-tilt + planar fallback; MNODE plane rotation = atan(dv_n/v); 28.5°-vs-Moon plane-mismatch shoot converges under min(SOI/3, 25,000), deterministically; `stepsPerOrbit` coarsening pin; free-return goldens re-derived (old default ecliptic: met 4187 s / 3149 m/s / perigee ≈ 164 km → new default 28.5°: met ≈ 5101 s / 3148 m/s / perigee ≈ 279 km; explicit incDeg=0 stays in band, perigee ≈ 195 km). Browser-measured: MNODE 400 m/s normal burn lifts the orbit 3,032 km out of plane; free return from 28.5° converges at 3,148 m/s → 200 km return perigee; required ΔV byte-identical (TLI 3,143 / LOI 822 m/s); no autosave leakage.

## 7i. State-derived orbit rings + solved-RAAN defaults R3.1 (565 + 574, 2026-07-10)

Closes the "rings draw from the authored spec while trajectories are real states — two authorities for one geometry" root cause behind the user's misalignment/arbitrary-polar-LLO reports (COHERENCE SERIES intro). ONE orientation-precedence rule for every drawn mission-orbit ring, three tiers (tier 1, authored Ω/ω, wired in R3.2 — see §7j):

1. **Authored orientation (R3.2, §7j)** — an orbit spec carrying explicit `lan_deg`/`argp_deg` wins outright, ahead of tiers 2 and 3.
2. **State-derived (this phase, the core)** — `physRebuildMissionTrajectories` (565) now records two orientation fields on a CONVERGED n-body (moon/interplanetary) leg, side-table only (`_physTrajByMission`, never on `m`):
   - `departElements = {a: r1, e: 0, i: incRad, raan: aim.raan, argp: 0}` — the EXACT circular ring the differential corrector actually departed from (§7h's `aim.raan` solve). Since the departure state is constructed by `physAimBurnState` with that same `(r1, incRad, raan)`, this is definitionally the flown plane, not a re-derivation.
   - `arrivalElements = {a, e, i, raan, argp}` — the OSCULATING plane of the real arrival state (`physArrivalOsculatingElements`, new pure helper next to `physClosestApproachKm`), with `{a, e}` OVERRIDDEN to the authored target orbit's size (`toOrbit.perigee/apogee`) and only `{i, raan, argp}` taken from the state. **This split is deliberate and load-bearing**: `{peri, apo, inclination-as-charged}` remain the ΔV-engine's accounting truth (critique 50 — required ΔV must stay byte-identical with physics on/off); the PLANE is flight truth, recovered from where the vehicle actually ended up. Size and orientation are independent axes of the same ring.
   - Arrival-state reconstruction is approximate by necessity: the propagator (386) records POSITION-only samples plus periapsis events (`{t, rMag}`, no vector) — there is no velocity to read off directly. `physArrivalOsculatingElements` takes the LAST periapsis event in the destination's frame (nearest the real arrival), brackets it with the two destination-frame samples straddling that event time, reconstructs velocity by central finite difference (`(r1-r0)/(t1-t0)`), and reconstructs position by linear interpolation between those same two samples rescaled onto the event's exact `rMag`. This is NOT a re-integration — it trusts the existing decimated samples — but it is deterministic (same propagation → same bracket → same result, gate-pinned) and adequate for RING ORIENTATION, which is all this phase consumes. It is explicitly not precise enough to drive a targeting residual (§7g's shooter still targets closest-approach radius, unchanged).
   - 574 (`_trajExtractMission`) consumes both fields in a pass after the ring/leg extraction: for each converged leg with `departElements`/`arrivalElements`, it resolves the leg's `fromNode`/`toNode` back to orbit specs, computes the SAME `(body, peri, apo)` dedupe key `_trajOrbitKey` uses for ring identity, and — if a ring with that key exists and doesn't already carry `rec.elements` — stamps `rec.elements = {i, raan, argp, source:'flight'}`. **First converged leg wins**; a later leg touching the same ring key never re-orients it (stability under replay, matching the plan's requirement). `_trajRingSVG` samples with `rec.elements`' `{i, raan, argp}` when present (still `{a, e}` from the ring's own `peri`/`apo` — size is never touched by this override) instead of the `{i: rec.inc, raan: 0, argp: 0}` convention; the ring's burn-marker-on-ring property falls out for free because the departure/arrival states that produced `elements` are the SAME states the marker's own MET interpolates.
3. **Solved-RAAN default (degenerates to the old convention here)** — the plan's tier 3 (solve Ω so a parentless ring contains its creating point) is NOT separately implemented in R3.1: every ring that has physics parentage gets tier 2 for free (a departure ring's creating point is definitionally on it, an arrival ring's creating point is the arrival state itself), and R3's §7h TLI solve (`physShootLegAim`'s `aim.raan`) already IS the "solve Ω for the departure ring" case the plan describes — R3.1 only had to WIRE it to the renderer. Genuinely parentless rings (schematic-only legs, define-an-orbit previews, orphan snapshots with no MANEUVER touching them) keep `Ω = ω = 0` exactly as before — no new solve was needed because R3 had already done the only Ω-solving this phase's scope requires. Documented here rather than silently claimed as "implemented but unused."

**Tooltip/footer honesty**: a ring with `rec.elements` shows `i=X° Ω=Y° (plane from flight)` in its `<title>` instead of `(Ω,ω assumed 0)`. The trajectory-view footer's blanket "vessel orbits Ω,ω assumed 0" note is now conditional-ish: **"vessel orbit planes: from flight where flown, Ω=0 otherwise"**.

**Critique 46 RESOLVED, critique 49 amended** (see below) — for FLOWN legs the drawn ring plane and the physics departure/arrival plane are now the same object (departure: literally, by construction; arrival: to within the finite-difference reconstruction's accuracy, gate-pinned h-vector angle < 1e-6 rad on a synthetic state). The disagreement survives only for UNFLOWN rings (no converged leg ever touched them) and for the residual accounting split of critique 50 (size vs. orientation truth), which this phase does not touch.

Gate (7 new asserts, `tests/math.test.js`, R3.1 block): `physArrivalOsculatingElements` h-vector round-trip on a synthetic bracketed arrival matches the true state's h-vector (angle < 1e-6 rad); determinism (identical inputs → identical elements); a ring sampled with the derived `{i, raan, argp}` has a plane normal matching the arrival h-vector (< 1e-9 rad, pure geometry — exact once the elements match); `_trajRingSVG` with `rec.elements` renders and is annotated "plane from flight"; without `rec.elements` the pre-R3.1 `i=0` rendering is byte-for-byte unchanged (backwards-compat pin); no periapsis event in the destination frame → clean `null`, no throw.

Browser-verified (Apollo seed, `devSeedApolloMission({force:true})`, 2026-07-10): LEO departure ring plane vs. the physics leg's departure h-vector — **0.0°** (identical by construction, §7h — the ring samples the exact same `{a, e, i, raan}` the corrector departed from). LLO ("Moon 100") arrival ring vs. the Moon's real orbital plane — **5.96°**, i.e. close to the Moon's own ~5.145° ecliptic inclination (offset by the arrival's real out-of-plane miss) — replacing the OLD render's arbitrary `i=90°` (Ω=0 convention default), which sat ~90° away from anything physically meaningful; this closes the user's "inserting directly into a polar orbit for seemingly no reason" report for the Apollo mission's specific LLO ring. Both rings' tooltips read "plane from flight" live in the trajectory view (mission-page → Trajectory tab, confirmed via DOM query). Tilted view (az=0.7 rad, el=30°) renders both rings with zero NaN coordinates. Regression: a fresh launch-only seed (`{maneuvers:false}`) still renders its LEO ring with the old `(Ω,ω assumed 0)` annotation — unflown rings are byte-for-byte unchanged. Required ΔV confirmed byte-identical (TLC 3,143 / LLO 822 m/s); autosave/session blob (`_buildSessionObject()`) has neither a `"samples"` nor an `Elements` key; console clean; warm `missionRecompute` measured 6.8 ms (< 60 ms budget). The general Ω=0-for-unflown-rings gap remains, critique 49.

## 7j. Orbit orientation authoring — LAN/ω fields R3.2 (430/570 node specs + 565 + 574 + 430's ΔV engine, 2026-07-10)

Wires the tier 1 the R3.1 precedence rule (§7i) left unbuilt: an orbit spec can now AUTHOR its own orientation instead of only inheriting it from flight or defaulting to Ω=ω=0.

- **Spec fields**: orbit specs gain OPTIONAL `lan_deg` (Ω) and `argp_deg` (ω) alongside `inclination`. Absent means unauthored — R3.1 tiers 2/3 apply exactly as before. No dedicated (de)serialization code was added anywhere: orbit specs already serialize WHOLESALE (autosave/`_buildSessionObject`, `.program` files, the user orbit library's per-orbit JSON, `nodeMapCustomNodes`), so a plain optional property on the spec object round-trips automatically through every one of those paths — verified with a JSON round-trip gate test rather than assumed (`tests/math.test.js`, R3.2 block).
- **Authoring UI**: the Define-an-Orbit panel (`src/index.html`, `#panel-define-orbit`) gains two optional numeric inputs — `#orbit-lan` (LAN Ω) and `#orbit-argp` (Arg. periapsis ω) — parsed with the panel's existing arithmetic-expression convention via a new `gvOpt(id)` helper (140-physics.js) that returns `null` on a blank field instead of coercing to 0 (the existing `gv()` always coerces to 0, which would make "unauthored" inexpressible). `collectCurrentOrbit()`/`loadOrbitPreset()` (130/120) carry the fields through save/load round-trip; `_missionOrbitToNodeOrbit()` (570) carries them from a saved orbit onto the node-map orbit spec physics actually consumes. The manual custom-node modal (`missionOpenCustomNodeModal`/`missionSaveCustomNode`, 570) got the same two optional fields for symmetry when authoring a node directly rather than via a saved orbit.
- **Node card badge**: node tooltips (`<title>` on both built-in and custom node-map nodes, 570) now append a compact orientation clause via `_missionOrientationBadge(node, missionId)` — `(authored)` when the node's own spec carries `lan_deg`; `— from flight` with the derived `{i, Ω}` when a converged physics leg's departure (`departElements`, matched by `fromNode` id) or arrival (`arrivalElements`, matched by the same `(body, peri, apo)` key `_trajOrbitKey` uses for ring identity) touched this node; `default (Ω=0)` otherwise.
- **Rendering precedence (574)**: factored into a pure, unit-tested `_trajRingOrientationFor(rec)` — returns `{i, raan, argp, source}` in radians. `_trajExtractMission`'s `addOrbitRing` now stamps `rec.elements = {..., source:'authored'}` at RING CREATION time when the contributing orbit spec carries `lan_deg`; the R3.1 state-derived pass only ever writes `rec.elements` when it is still absent (`if (rec && !rec.elements)`, unchanged code), so write-order alone enforces the full three-tier precedence — no explicit priority comparison needed. `_trajRingSVG` consumes `_trajRingOrientationFor(rec)` instead of inlining the `rec.elements` check; tooltip reads "(Ω,ω authored)" for tier 1, "(plane from flight)" for tier 2 (unchanged text), "(Ω,ω assumed 0)" for tier 3 (unchanged text).
- **Physics departure (565, `physShootLegAim`)**: when `fromOrbit.lan_deg != null`, `raanRoots` collapses to the single authored value and the existing ascending/descending two-root solve (§7h) is skipped entirely — the departure ring plane is FIXED, not re-solved. A fixed-|Δv| leg that cannot reach the Moon from that exact plane honestly reports `converged:false` (the solve is never allowed to silently wander onto a different, reachable plane the user didn't author). `physSolveNodeBurn`'s single-burn (same-body) construction and the MNODE builder's `orbitAtBurn` reconstruction (both call `physAimBurnState`) thread the same authored raan for consistency, so a same-body burn and a vector-burn maneuver both honor an authored departure plane, not just n-body legs.
- **Physics arrival (565 shooter)**: when the TO orbit carries both `lan_deg` and `inclination`, a plane-alignment residual is added to the 3-DOF stage's miss vector, REPLACING the plain out-of-plane `dz` component (R3's yaw-closing term) with `angle(arrival h-vector, authored target normal) × SOI_km` — same slot, different target, computed via the existing `physArrivalOsculatingElements` helper (§7i) so no new state-reconstruction machinery was needed. The **hand-tuned conditioning constant is the SOI scale itself** (`× soi`): it puts a full-turn plane miss (π rad) at the same order of magnitude as the radial/timing miss components already in the vector, so no single residual dominates the Newton step — documented inline at the computation, per the plan's explicit "document the constant as hand-tuned" instruction. Because only the 3-DOF stage's residual checks the plane, the 1-DOF/2-DOF early-convergence shortcuts are disabled whenever a target plane is authored (their small radial/timing miss says nothing about whether the plane was reached) — the ladder is forced to escalate to 3-DOF, and the "best stage" comparison at the end is restricted to the 3-DOF solution for the same reason. Converged:false remains honest when the fixed |Δv| cannot reach the authored plane. The R3.1 `arrivalElements` recording is untouched — for a converged authored-target leg the derived plane matches the authored one by construction (the residual that drove convergence IS that match), verified with a gate case that flies the shoot and checks the resulting state actually enters the Moon's SOI.
- **Edge-cost flag (guarded, default OFF)**: `progNmComputeEdgeDv`/`_nmCoaxialTransferDv` (430) previously had **no plane-change term at all** for same-body node-to-node transfers (purely coaxial vis-viva, `inclination` on a node spec was cosmetic there) — a gap this phase does not otherwise touch (accounting stays schematic per critique 50). A new per-node `priceOrientation` boolean (absent = false; only ever set on `nodeMapCustomNodes` entries, never on built-ins) opts a specific transfer INTO real plane pricing: when either endpoint sets it, `_nmCoaxialTransferDv` computes the real plane angle between the two orbits' authored `{i, Ω}` via the existing `progDvPlaneChangeFull` (spherical law of cosines, §6) at burn 1's radius, and combines it vectorially (`Math.sqrt(dv1² + dvPlane²)`, the same convention `progDvCombined` already uses elsewhere) with the existing tangential burn. Flag absent/false is a no-op branch (`if (priceOrientation && ...)`), so every existing mission's ΔV total is untouched by construction, not merely by testing — pinned by a parity gate (flag omitted === flag explicit `false`) plus a monotone-in-plane-angle gate (larger LAN gap prices more) and a same-plane-authored gate (zero angle prices identically flag on or off). No UI exposes the flag this phase, as directed by the plan.
- **MNODE/free-return (item 5)**: MNODE's burn-frame reconstruction threads the vehicle's `orbitAtBurn.lan_deg` when present (same `physAimBurnState` raan parameter as the departure/arrival cases). Free-return is explicitly UNCHANGED — it still solves from the conventional Ω=0 departure plane (critique 49(b)); wiring a vehicle's authored plane into `physFreeReturnSolve` was out of this phase's scope per the plan.
- **Honesty note (MATH.md, this section)**: authored LAN is an ORBIT PROPERTY we model — the geometry the vehicle occupies once it's there. REACHING a specific LAN at a specific time is a LAUNCH-WINDOW/wait problem this phase does not model: `physShootLegAim`'s departure-plane fixing means an authored plane that the fixed-|Δv| burn cannot reach at the SCHEDULED MET honestly fails, but nothing in this phase lets the user say "wait until the plane comes around" — that's a phasing/timing cost, not a geometry cost, and belongs to a future COAST-event-based launch-window model, not to the orbit spec.

Gate (11 new asserts, `tests/math.test.js`, R3.2 block — `src/js/430-program-module-phase-8-node-map.js` added to the test harness's `FILES` list since it is pure at module scope): spec round-trip (`lan_deg`/`argp_deg` survive a JSON round-trip; absent fields stay absent, never coerced to 0); rendering precedence (`_trajRingOrientationFor` returns tier 'authored'/'flight'/'default' correctly from synthetic recs); departure-plane fixing (authored `lan_deg` on the FROM orbit makes `physShootLegAim` return exactly that raan, both on a normal case and a "hostile" 90°-rotated plane, in both cases without throwing and within the propagation budget); determinism (repeat solves match exactly); arrival plane-alignment (a reachable authored target plane near the Moon's real inclination converges and the resulting state actually enters the Moon's SOI); edge-cost flag parity (flag omitted === flag explicit false, byte-identical `total_ms`/`dv1_ms`), monotonicity (larger LAN gap → larger `total_ms`), zero-angle identity (same authored plane on both sides prices identically flag on/off), and determinism.

Browser verification for this phase (a)-(f) from the task brief — authoring a custom orbit and reloading, the Apollo-seed regression numbers, the node-card badge states, the hostile-plane departure honest-failure, and the autosave-blob diff — could **not** be captured in this session: the Chrome extension the harness's browser tooling depends on (`claude-in-chrome`) reported not connected (`tabs_context_mcp` returned "Claude in Chrome is not connected") and the fallback `preview_*` tools were unavailable in this environment. The pure-math gate (297 assertions, up from 281 pre-R3.2, all green under `python build.py`) is the verification actually performed; the interactive checks remain open and should be run the next time browser tooling is available in this environment.

## 7k. Maneuver gizmo — KSP-style MNODE authoring R3.3 (5745-maneuver-gizmo.js + 574 + 565, 2026-07-10)

Replaces "type numbers into the Vector Burn form" as the PRIMARY authoring flow for MNODE events (the form stays as the precise-entry fallback and remains fully functional — nothing in this phase touches it). Trajectory-view-side only; no new state lives on `m` or in autosave (module-local `_trajGizmo`, per the two-layer rule's symbology-is-render-state convention).

- **Node state reconstruction** (`_trajGizmoNodeState`, 5745): mirrors 565's MNODE burn-frame builder exactly — `orbitAtBurn`-equivalent orbit → mean-anomaly circular-ring state via the SAME `physAimBurnState(body, rMean, theta, 0, 0, incRad, 0, raan)` call the real replay uses, so the gizmo's handle directions and the eventual committed burn's frame are definitionally the same basis. For an EXISTING MNODE this uses the exact recorded `orbitAtBurn` (565 writes this every replay — precise). For a PENDING (not-yet-authored) node it approximates with the vehicle's CURRENT end-of-replay `orbitState` (`PROG_ACTIVE_PROGRAM.vehicles[m.vehicleId].orbitState`) rather than re-replaying the log up to the clicked MET — a deliberate simplification: precise historical reconstruction at an arbitrary earlier MET would require a partial replay the mission engine doesn't expose as a standalone query. Documented here rather than silently assumed exact.
- **Placement simplifications (both documented, per the plan's explicit "acceptable simplification" allowance)**:
  - **Ring double-click → MET = current view time.** The theta→MET anomaly inversion (`theta = n·(MET − epoch)` solved backward from the clicked point's angle on the ring) was skipped in favor of the simpler, always-correct "spawn at now" — avoids a wrap-around/branch-selection edge case (multiple METs map to the same ring angle) for a first cut. A future pass can add along-ring drag-to-snap MET on top of this.
  - **Physics-polyline double-click → nearest SAMPLE's MET to the current view time**, not nearest to the clicked pixel (`_trajGizmoNearestSampleMet(samples, targetT)`, pure/gate-tested). True click-to-world resolution would require inverse-projecting the SVG click point back through `_trajProj3`/zoom/anchor into world km and then bisecting the sample array by distance — the inverse of a non-invertible orthographic projection (multiple world points along the view ray project to the same screen point) without an extra depth cue. Nearest-to-view-time is deterministic, cheap, and matches what the user is already looking at (the "current" vehicle-position dot on the polyline).
- **Handle geometry**: local-frame axes (rHat, vHat, hHat) come straight from `physAimBurnState`'s returned basis (non-Gram-Schmidt-orthonormalized — same basis 565 actually applies dv against, so the gizmo can't drift from what will be flown). Each handle direction is `_trajProj3(hat) → normalize in SCREEN space` (`_trajGizmoScreenDir`, pure/gate-tested); handle LENGTH is a constant 22 RENDER units (not km, not px) — because render-space geometry is already `km·zoom` baked into a fixed ±200 viewBox (the two-layer design's whole point), a constant render-unit length maps to an approximately constant on-screen px length at ANY zoom without the gizmo needing to know the current zoom factor at all. The normal handle's screen-projected length shrinks toward zero at top-down (el=90°) camera angles by construction (h⊥ecliptic, camera looking straight down the same axis); below a 0.05 (render-space) length threshold it swaps to a diamond glyph pinned to a fixed fallback screen direction rather than collapsing to an unusable point-sized knob.
- **Drag → Δv mapping** (`_trajGizmoPxToDv`, pure/gate-tested): **gain = 2 m/s per screen pixel, dragged along the handle's own screen direction (dot product of the mouse delta with the handle's unit screen vector — not raw x/y)**, 0.2 m/s/px with Shift held (fine control). Both constants are HAND-TUNED, chosen so a ~100 px drag (a comfortable mouse gesture) produces a ~200 m/s burn — the right order of magnitude for a LEO-to-transfer-orbit-class maneuver — and are explicitly ZOOM-INDEPENDENT (screen px in, m/s out; no reference to `cam.wKm`/zoom anywhere in the mapping), matching the plan's "px-to-m/s gain ~ zoom-independent" instruction. Dragging past the handle's zero point flips the sign naturally (the along-handle projection goes negative), authoring retrograde/anti-radial/anti-normal without a separate UI affordance.
- **Live preview = SCRATCH propagation only**: every drag tick (throttled to one `physPropagateSegment` call per ~100 ms via `_trajGizmoScheduleScratch`/`setTimeout`) computes `state = {r: node.r, v: node.v + dvVec}` and calls `physPropagateSegment(state, met, met+horizon, {center, bodies, overrides:{}}, {maxSamples:128})` — results land in a plain module-local `_trajGizmo.preview` var, NEVER `_physTrajByMission` (the sanctioned side-table for COMMITTED legs) and never `m.log`. The horizon is `min(30 d, 3×period)` for a bound elliptical result, else 30 d flat — same convention 565's real MNODE leg builder uses. The preview renders as a dashed `var(--accent)` path drawn directly into a dedicated `<g class="traj-gizmo-preview">` child of the scene layer, updated in place (not a full `_trajWorldSVG` re-render) — cheap enough for 10 Hz dragging. **Scope limitation (documented, not fixed this phase)**: the scene-preview draw only plots samples whose `frame` equals the node's own body — an SOI-handoff mid-preview (e.g. dragging a lunar-departure burn hard enough to leave Earth's SOI in the scratch propagation) silently truncates the drawn dashed path at the handoff rather than continuing into the new frame, unlike the committed-leg renderer (`_trajPhysLegRender`) which has multi-frame anchoring. Acceptable for the common case (tuning a burn that stays in one SOI); a future pass should reuse `_trajPolylineSVG`'s anchor-resolution machinery for the preview too.
- **Mutation discipline**: the log is touched exactly ONCE, on mouseup (`_trajGizmoCommit`) — either `missionExecManeuverNode(id, {value_s, dvPro_ms, dvRad_ms, dvNrm_ms})` for a new node, or a direct field mutation (`e.dvPro_ms = ...`) on the existing MNODE entry followed by `missionRecompute(m)` for an edit — both paths end in exactly one `missionRecompute` call, so `missionUndoCapture`'s hook at the recompute tail produces exactly one undo step per gizmo interaction, matching `missionExecManeuverNode`'s pre-existing single-step contract. Escape or right-click (`contextmenu`, default-prevented) DURING a drag calls `_trajGizmoCancelDrag`, which reverts `dv[axis]` to its pre-drag value and clears the preview — zero log mutation, verified structurally (the commit path is only ever reached from `_trajGizmoHandleUp`, never from the cancel path).
- **Editing existing MNODEs**: `_trajSelectEventFromView` (574) now also calls `_trajGizmoOnEventSelected`, which opens the gizmo pre-loaded with the event's `dvPro_ms/dvRad_ms/dvNrm_ms` when an MNODE event is selected (ring/polyline click, or the event-card list — same shared selection path), and detaches a currently-attached COMMITTED gizmo when anything else is selected. A ✕ glyph in the readout plate (and Escape when not mid-drag) calls `_trajGizmoClose()` — dismisses the gizmo without touching the log.
- **Re-render survival** (item 5 of the plan): `_trajGizmoRepaintOverlay` re-derives `_trajGizmo.node` from the mission's CURRENT log/`orbitAtBurn` every repaint whenever the gizmo is attached to a committed event (`authIdx != null`) — so a gizmo survives `missionRenderDetail()` (undo/redo, another edit elsewhere) with up-to-date geometry, and self-closes if its event was undone out from under it. A PENDING (uncommitted, `authIdx == null`) gizmo is NOT auto-dropped on unrelated re-renders in this implementation — the plan explicitly allows either behavior ("may drop... acceptable"); this implementation keeps it live since dropping it silently on an incidental re-render (e.g. a camera-only `_trajApplyCam` call, which does NOT go through this code path at all, or another selection elsewhere) would discard in-progress authoring more often than the plan's phrasing implies is desirable. Gizmo state is never read by `_buildSessionObject`/autosave — confirmed by construction (it is a `let` in 5745, never assigned onto any object that path serializes).
- **Known scope limits** (new critique below): the ring-click MET simplification means dragging directly on the schematic transfer arc (before it has physics samples) is not currently a placement path at all — only mission ORBIT RINGS and PHYSICS POLYLINES spawn gizmos, per the plan's explicit list; the scratch-preview single-frame limitation above; and the pending-node approximate reconstruction (current-orbit, not point-in-history) above.

Gate (20 new asserts, `tests/math.test.js`, R3.3 block — `src/js/5745-maneuver-gizmo.js` added to the harness's `FILES` list; DOM-touching functions in that file are excluded from testing by construction, only the six pure helpers are exercised): axis orthonormality and the degenerate zero-velocity case for `_trajGizmoAxes`; the px→dv gain (normal + shift-fine + sign flip + zero) for `_trajGizmoPxToDv`; nearest-sample MET resolution including the empty-array case for `_trajGizmoNearestSampleMet`; readout string formatting (sign prefixes, MET mm:ss) for `_trajGizmoFormatReadout`; and screen-direction normalization plus the degenerate-fallback branch for `_trajGizmoScreenDir`.

Browser verification for this phase (drag-to-200-m/s, normal-handle tilt behavior, Escape-cancels-cleanly, edit-existing-MNODE-updates-not-duplicates, and the pan/zoom/rotate regression sweep from the task brief's item (e)) could **not** be captured in this session: both `preview_*` (returned "not available — use the ext-shaped equivalent") and `claude-in-chrome`'s `tabs_context_mcp` (returned "Claude in Chrome is not connected") were unreachable in this environment, consistent with the R3.2 session's note above. What WAS verified: the pure-math gate (317 assertions, up from 297 pre-R3.3, all green under `python build.py`); `node --check` syntax validation of the full concatenated build; and a direct grep of the built `lv_calc.html` confirming the interactive wiring is actually present in the shipped artifact (`ondblclick="_trajGizmoRingDblClick(...)"` / `_trajGizmoLegDblClick(...)` on ring and polyline hit-paths, `onmousedown="...;_trajGizmoHandleDown(event,'pro'/'rad'/'nrm')"` with `event.stopPropagation()` ahead of the handler call — so a handle grab cannot leak into `trajPanStart`, `_trajGizmoOnEventSelected` wired into `_trajSelectEventFromView`). The interactive drag loop itself — the core "drag, watch the trajectory bend" experience this phase exists to build — remains UNVERIFIED end-to-end and should be exercised the next time browser tooling is available in this environment.

### R3.4 — Gizmo usability + KSP parity (2026-07-10, same-day follow-up on user first-flight feedback)

Four changes, all additive to R3.3's model above (the axis basis, mutation discipline, and pending/committed reconstruction rules are UNCHANGED):

- **Camera rework (574, the enabler)**: `trajPanStart`/`trajPanMove` no longer branch on `mode` — every left-drag (plain, shift, or right-button) now rotates az/el; the entire pan branch (`worldDx`/`worldDy` world-delta math, the mid-drag viewBox slide, the overlay `translate()` hack) is DELETED, not merely dead-code-guarded. `trajWheelZoom` no longer does cursor-anchored offset math — zoom is now a pure `wKm` change about the anchor. `cam.relOffsetKm` stays on the camera struct (the fit/zoom-to-content math in `_trajFitWKmForBody`/`_trajCamCenterKm` still reads it) but is now written ONLY as `{x:0, y:0}` everywhere (verified by grep — no assignment writes a non-zero value post-R3.4). This is the KSP camera contract (drag rotates, scroll zooms, click focuses) and is what makes six 12px handles reliably grabbable — under the old model, a drag that started even slightly off a handle would pan the whole scene out from under the cursor.
- **Six pull-out handles** (`_TRAJ_GIZMO_HANDLES`, 5745): replaces R3.3's 3 bidirectional handles. Each of the 3 axes (prograde/radial/normal) now has TWO handles, one per sign, and each handle owns only its own non-negative "side": `_trajGizmoHandleSideMag(dv0, alongPx, shiftHeld) = max(0, dv0 + gain·alongPx)` where `dv0` is the handle's OWN side magnitude at drag-start (`max(component,0)` for a "+" handle, `max(-component,0)` for "-"). Pulling a handle away from the node always increases its own side; dragging back toward the node reduces it to exactly 0 and stops — it can never cross into the opposite handle's negative territory (that handle must be grabbed instead). This matches KSP's model and removes R3.3's "drag past zero flips the sign" behavior, which was disorienting once six independent knobs are on screen (a sign flip on a knob you're NOT currently watching is surprising). Visual: the "+" handle is a solid filled knob; its "-" partner is the SAME color as a dashed hollow ring (outline distinction only — no second hue introduced, per the theming rule against inventing new chromatic literals). Hit circles are a transparent 12px-radius circle laid over a smaller (7px) visible knob, satisfying the ≥12px grab target without inflating the glyph. Hover (`_trajGizmoHoverKnob`) is a pure `setAttribute` swap on the visible knob's `r`/`stroke-width`/`opacity` via `onmouseenter`/`onmouseleave` — no re-render, per the plan.
- **Node-time control** (center knob, `_trajGizmoCenterDown/Move/Up`, 5745): the center knob's hit circle (≥12px) now does double duty. A DRAG (movement > 3px, the same click-vs-drag threshold used elsewhere in 574/570) slides the node's MET along its orbit: the mouse delta is projected onto the node's prograde SCREEN direction (`dirs.pro`, already computed for the handle geometry) to get `pxAlong`, then `dMET = _trajGizmoCenterDragDMet(pxAlong, kmPerPx, vMagKms) = pxAlong · kmPerPx / |v|` where `kmPerPx = cam.wKm / rect.width` (the viewBox-to-px ratio at the CURRENT zoom — re-derived every drag start, so zooming between gizmo opens changes the drag's km/px scale correctly). MET is clamped `>= 0` and the node state is re-derived from `_trajGizmoNodeState(m, newMet, authIdx)` (SAME reconstruction path as everywhere else — theta = n·MET off the recorded/current orbit) on every move tick, live-updating the preview. A CLICK (no drag) toggles a small flyout menu (`.traj-gizmo-menu`, plain HTML div positioned canvas-relative via `_trajWorldToScreen`'s already-canvas-relative px output — NOT inside the SVG overlay, since it needs ordinary DOM click handlers) with "+1 orbit" / "-1 orbit" (MET ± the orbital period, `_trajGizmoOrbitPeriodMet(mu,r,v)` — a pure vis-viva `a = 1/(2/r − v²/μ)` → `T = 2π√(a³/μ)`, returns `null` for a non-elliptical state so the buttons are inert rather than wrong) and "Delete node" / "Discard node" (committed: `m.log.splice(authIdx,1)` + `missionRecompute` — the standard mutation path, one recompute/one undo step; pending: just closes the gizmo, nothing was ever pushed to the log). Center-drag release on a COMMITTED node commits `e.at.value_s = newMet` through the same one-recompute discipline as a handle release; on a PENDING node it only updates local gizmo state (nothing to commit until a handle drag actually authors the node).
- **Closest-approach pair** (`_trajGizmoClosestApproach`/`_trajGizmoSoiEntryT`/`_trajGizmoPickTarget`, 5745, pure + gate-tested): target selection scans `m.log` forward from the node's MET for the next `MANEUVER` entry whose `toNode` resolves (via `_missionNmNodeById`) to a body different from the node's own body; failing that, defaults to the Moon for an Earth-centric node; otherwise no CA pair is shown. `_trajGizmoClosestApproach(samples, targetBody, railFn)` scans a leg/preview's `samples` array and returns the sample nearest the target, FRAME-AWARE: if a sample's `frame` already equals `targetBody` (post-SOI-transition) the distance is `|s.r|` directly; otherwise the target's real position (`physBodyStateAt`, default `railFn`) is resolved into the SAME frame as the sample (`target.r − frameBody.r`, both heliocentric) before differencing — exact across an SOI-patched propagation without ever converting the craft's own coordinates. `_trajGizmoSoiEntryT` finds the first sample whose frame flips to the target, used purely as the "ENCOUNTER" trigger and the T+ time shown in that state. Rendering: a small × marker at the craft's CA position (drawn in the gizmo's OVERLAY svg, screen-projected the same way as the handle geometry) plus a `_trajGhostMarker`-style ghost of the target body AT its real CA-time position (drawn into the scene preview layer, same frame-matching caveat as the rest of the scratch preview below), plus a plate line — `"CA {km} km · in T+…"` normally, `"ENCOUNTER · T+…"` (accent-highlighted) once `_trajGizmoSoiEntryT` fires.
- **Fidelity ladder** (user-requested mid-phase addendum, `_trajGizmoScheduleScratch`/`_trajGizmoRunScratch(mode)`/`_trajGizmoPreviewFidelity`, 5745): dragging (handle or center) now runs TWO propagation cadences instead of one. On the existing ~100ms move-tick throttle, a CHEAP pass runs — `physPropagateSegment` with `ctx.bodies` restricted to `[node.body]` (perturbers dropped) and `maxSamples: 64` — reusing the SAME integrator entry point as the full pass rather than a hand-rolled two-body loop (simpler, and the expensive part of a full pass is the multi-body perturbation SUM inside `physPropagateSegment`, not call overhead, so restricting `bodies` genuinely cuts cost). Every move also (re)arms a 2000 ms debounce timer (`_TRAJ_GIZMO_FULL_DEBOUNCE_MS`, HAND-TUNED, no calibration against frame budget or machine speed — picked as "long enough that continuous dragging never triggers it, short enough that a paused drag refines within about the time a user re-reads the readout plate"); once the node has been left alone (no move tick) for that long while the gizmo is still attached to an active drag, ONE full-fidelity pass runs (`bodies` = the original full set, `maxSamples: 128` — identical to R3.3's original always-full scratch) and REPLACES the preview + CA pair. The decision of which mode to run is a pure, gate-tested function — `_trajGizmoPreviewFidelity(lastMoveMs, nowMs, debounceMs=2000) → 'cheap'|'full'` — the actual `setTimeout` scheduling around it is plumbing and is NOT gate-tested (per the module's existing DOM-plumbing/pure-helper split). A cheap-mode CA result is flagged `approx:true` and the plate readout prefixes it with `~` (`~CA 12,340 km · in T+…` / `~ENCOUNTER · …`) so the user knows the number will sharpen once they pause. On release/commit the behavior is unchanged from R3.3 — the REAL committed leg always rebuilds at full fidelity through the normal `missionRecompute`/`_trajPhysLegRender` path; the ladder only governs the SCRATCH preview shown mid-drag.
- **Known scope carry-overs from R3.3** (unchanged by this phase): the pending-node approximate reconstruction (current orbit, not point-in-history); the scratch preview's single-body-frame draw limit (now doubly true — the CHEAP pass literally cannot see other bodies at all by construction, and even the FULL pass's drawn PATH still only plots same-frame samples, though the CA helper itself is frame-aware and will correctly report an encounter even though the drawn dashed line stops at the SOI boundary); ring-click MET-inversion is still skipped (superseded in spirit by center-drag, which now provides an interactive along-orbit adjustment after placement — closing part of the R3.3 "future pass" note).

Gate: 26 new asserts added to the R3.3 block in `tests/math.test.js` (343 total, up from 317): `_trajGizmoHandleSideMag` (pull-away increases, push-back clamps at 0 and never goes negative, fine-drag scaling); `_trajGizmoCenterDragDMet` (the gain formula, zero/negative `vMagKms` guard, sign preservation); `_trajGizmoOrbitPeriodMet` (positive/finite for a circular Earth LEO state, matches `2π√(a³/μ)` for a=r circular, a +1/-1 orbit round-trips MET exactly, `null` for a hyperbolic state); `_trajGizmoClosestApproach` (finds a KNOWN synthetic minimum exactly, matches across a frame-aware re-derivation, correctly handles a sample already re-framed to the target, empty/no-target guards); `_trajGizmoSoiEntryT` (finds the first re-framed sample, `null` when none); a determinism pair (identical inputs → identical outputs) for both `_trajGizmoHandleSideMag` and `_trajGizmoClosestApproach`; `_trajGizmoPreviewFidelity` (boundary behavior at the debounce threshold, custom `debounceMs`).

Headless verification (browser tooling was confirmed unreachable again this session — `preview_screenshot` returned "not available"): grepped the built `lv_calc.html` and confirmed (a) `worldDx` no longer appears anywhere in `trajPanMove`/the pan branch (it's gone, not dead code), (b) `const mode = 'rotate'` is unconditional in `trajPanStart`, (c) all 6 handle definitions (`key: 'pro'/'retro'/'radOut'/'radIn'/'nrm'/'antinrm'`) plus `_trajGizmoHoverKnob` wiring are present, (d) `_trajGizmoCenterDown` and `.traj-gizmo-menu` markup are wired, (e) `_trajGizmoClosestApproach`/`_trajGizmoPickTarget` and the fidelity-ladder symbols (`_trajGizmoPreviewFidelity`, `_TRAJ_GIZMO_FULL_DEBOUNCE_MS`) are present in the shipped artifact. Beyond the gate, a standalone `vm` smoke (not part of the committed gate — ad hoc, run against the SAME module set the test harness loads) built a REAL translunar-class departure state via `physAimBurnState` off a 185 km Earth parking orbit plus a 3.15 km/s prograde burn, propagated it 5 days with `physPropagateSegment(bodies:['Earth','Sun','Moon'])`, and confirmed `_trajGizmoClosestApproach` against the Moon exactly matches an independently brute-forced minimum over the same real sample set (not a canned array) and that the reported CA time falls within the propagation span. That particular departure geometry (theta=0, unphased against the Moon's actual position) does not itself reach lunar SOI — proving the helper is CORRECT on real physics samples, not that this specific burn is a real translunar trajectory; SOI-encounter behavior is covered by synthetic-data gate assertions instead. The full interactive loop (drag responsiveness, hover swap, center-drag time-slide feel, menu, fidelity-ladder visual settling) remains UNVERIFIED end-to-end pending browser tooling.

### R6.2' Phase B step 5 — solved-maneuver decomposition basis fix (565/5745, 2026-07-10)

The gizmo's handle readout for a solved maneuver (§7k's basis) and 565's display-only `dvPro_ms/dvRad_ms/dvNrm_ms` mirror on a unified `MNODE(mode:'solved')` entry both decompose the leg's real `dvVec` via `_trajGizmoDecomposeDv` against a local (v̂,r̂,ĥ) triad built by `_trajGizmoAxes(r,v)`. Through R6.2' Phase A that triad came from the MEAN-MOTION reconstruction (`_trajGizmoOrbitNodeAt`/565's mean-anomaly builder) — the right convention for a MANUAL vector burn (which literally IS authored against that reconstruction), but the WRONG anomaly for a shooter-refined moon/interplanetary leg, whose real departure point (`aim.theta/pitch/yaw` from `physShootLegAim`) sits elsewhere on the parking ring. This made a physically ~pure-prograde Apollo TLI read as pro −2085/rad +2151/nrm +950 m/s — magnitude-correct (the basis is orthonormal so `|dvVec|` round-trips exactly regardless of theta) but directionally misleading.

**Fix**: `physSolveNodeBurn`/the n-body shooter branch/the same-body analytic branch (565) now each stamp `leg.burnState = {r, v}` — the actual PRE-burn departure state the dv vector was applied to (the shooter's refined `physAimBurnState` result when converged, else the analytic seed's pre-burn state; `physAimBurnState` itself now also returns `vPre`, the circular velocity before dv is added, so callers don't need to re-subtract). Both the gizmo's `_trajGizmoManeuverNodeState` (when given a `missionId`/`authIdx`) and 565's own dv-component mirror decompose against `_trajGizmoAxes(leg.burnState.r, leg.burnState.v)` when present, falling back to the mean-motion reconstruction for legs that haven't computed a burnState (or for a genuinely manual MNODE, which correctly still wants the mean-motion basis it was authored against). ΔV ACCOUNTING is untouched by any of this — `dvRequired`/`dv_actual` still come from `progNmComputeEdgeDv`/`dvOverride` exclusively; only the pro/rad/nrm DISPLAY numbers and, per the detach fidelity note below, the DETACHED trajectory shape change.

**Detach fidelity**: dragging a handle on a solved maneuver still detaches it to a manual `MNODE(mode:'manual')` (§7k's `_trajGizmoDetachManeuverIfNeeded`), seeded with the solved pro/rad/nrm — but now the detach ALSO stamps `e.burnState = leg.burnState` (plain `{r:[...],v:[...]}` arrays, JSON-safe, rides autosave/session export fine) onto the log entry. 565's manual-MNODE leg builder checks for `e.burnState` first and, when present, applies the dv vector in THAT exact recorded state's basis (via `_trajGizmoAxes`) instead of reconstructing from mean-motion phase — so the detached leg's propagated path tracks the ORIGINAL solved leg's real path, not a path departing from a different point on the ring. Re-solving (`missionMnodeResolveToTarget`, 570) deletes the `burnState` stamp — the solver takes over the departure state again, and a later detach re-captures whatever the solver produces this time (never a stale burnState from a previous solve).

Note on the basis itself: `_trajGizmoAxes` builds rHat/vHat/hHat by independently normalizing r and v (NOT Gram-Schmidt-orthogonalized) — exactly invertible (compose→decompose round-trips components, not just magnitude) only when r⊥v (a purely tangential-velocity state, which every `physAimBurnState`-constructed circular parking-orbit state IS by construction). A state with a radial velocity component would make the triad non-orthogonal and the decomposition components would no longer round-trip exactly (magnitude still would, via the orthonormal-in-practice-for-orbital-states triad) — not a concern here since every burnState this pass stamps comes from `physAimBurnState`'s own circular-ring construction.

Gate: 10 new assertions (475 total, up from 465) — `_trajGizmoAxes` right-hand-triad sanity on a synthetic circular state; decomposition against a recorded basis for a mixed pro/rad/nrm case; a TLI-like case (dv purely along the recorded vHat) decomposing to ~pure prograde (|rad|,|nrm| < 1e-6 m/s); and a compose→decompose round-trip identity on a tilted-but-tangential (r⊥v) state.

## 7l. J2 / orbital-economy layer R4 (385/386, 2026-07-10)

Closed-form secular precession from Earth's (and other bodies') oblateness, plus an opt-in integrator term so the effect can be DRAWN on a real propagated orbit, not just quoted as a number.

- **`PROG_BODY_J2`** (385): a data map, not a model — `{Earth: 1.08263e-3, Mars: 1.9555e-3, Moon: 2.033e-4, Venus: 4.458e-6}`. Bodies absent from the map (e.g. Jupiter, Titan) have NO J2 model; callers must treat that as "no data" (functions return `null`), never silently assume zero oblateness.
- **`physJ2NodalRate(aKm, e, iRad, body)`**: standard first-order secular result dΩ/dt = −(3/2)·J2·n·(R/p)²·cos(i), p = a(1−e²), n = √(μ/a³). Sign convention (pinned in the gate): a RETROGRADE orbit (i > 90°, cos i < 0) gets a POSITIVE nodal rate (eastward node regression) — this is what makes sun-synchronous orbits (i ≈ 98°) work, matching Earth's ≈ +0.98565°/day mean heliocentric drift.
- **`physJ2ApsidalRate(aKm, e, iRad, body)`**: dω/dt = (3/4)·J2·n·(R/p)²·(5cos²i − 1) — vanishes at the "critical inclination" i ≈ 63.4° (Molniya's actual design inclination, chosen historically for exactly this reason: an apogee that doesn't drift).
- **`physJ2SunSyncCheck(aKm, e, iRad, body)`**: compares the nodal rate (deg/day) against Earth's mean heliocentric rate 360°/365.2422 days = 0.98565°/day, `sunSync:true` within 0.02°/day. Earth-only by construction (other bodies always report `sunSync:false` — "sun-synchronous" is a specifically Earth-relative launch-planning concept, not exported to Mars/Venus/Moon orbits here).
- **Readout chip** (230-orbit-diagram.js, Define-an-Orbit panel): when inclination is authored and the target orbit resolves, a compact line renders in the mini-diagram's footer — `"J2: node −0.99°/day · apsis +0.12°/day"`, plus an accent-colored `"· sun-synchronous ✓"` when `physJ2SunSyncCheck` passes. Earth-only (the diagram itself is Earth-centric only, per its own header comment) — no chip for escape-mode or other bodies.
- **Optional integrator term** (`physAccel`, 386): `ctx.j2 === true` opts a propagation INTO the J2 acceleration term for its center body (if that body has a `PROG_BODY_J2` entry); DEFAULT OFF — no existing caller sets `ctx.j2`, so this phase is zero behavior change for every currently-shipped propagation (mission legs, shooter, gizmo preview, free-return solve), gate-pinned bit-identical. Form (Vallado sign convention): a_J2 = −(3/2)·J2·μ·R²/r⁵ · [x(1−5z²/r²), y(1−5z²/r²), z(3−5z²/r²)].
- **`ctx.j2` must survive `physPropagateSegment`'s internal ctx rebuild** — the function constructs its own working `ctxNow` per call and only carries forward fields it explicitly lists; `j2` was added to that list alongside `center`/`bodies`/`overrides`/`railFn`/`dtMax` (an easy place for a new ctx field to silently vanish mid-propagation if not threaded through).
- **Pinned in the gate**: 800 km @ 98.6° nodal ≈ +0.9856°/day (within 0.03) and reports sun-synchronous; Molniya (a=26562, e=0.74, i=63.4°) apsidal ≈ 0 (within 0.01°/day); ISS-like 400 km @ 51.6° nodal ≈ −5.0°/day (within 0.3) and NOT sun-synchronous; a body with no J2 entry returns `null` from both rate functions and the sun-sync check; `physAccel` with `ctx.j2` unset/false is bit-identical to the pre-R4 acceleration, `ctx.j2:true` measurably differs; a 5-orbit numerical propagation of the 800 km/98.6° case with `ctx.j2:true` shows RAAN regressing in the sign and rough magnitude the closed-form secular rate predicts (generous 2° tolerance — a coarse trend check, not a precision pin).

## 7m. Body surface rendering + LOD ladder R6.2 (236/574, 2026-07-10)

**Presentation layer only — no physics/ΔV consequence.** Replaces the trajectory view's single clamped-circle body glyph with a three-tier ladder keyed off the body's TRUE (unclamped) apparent screen radius `trueR_km · zoom`: **surfaced disc** (≥24 px — base color + coastlines/maria/region ellipses/cloud bands + a limb-shading overlay), **plain disc** (2–24 px — the pre-existing colored circle with the `_TRAJ_MIN_BODY_PX` floor), **symbol chip** (<2 px — a constant-radius outlined circle with the body's astronomical glyph, reusing the same symbols as the Orbits page destination picker, 060-orbit-categories.js). `trueR_km` now comes from the real `PROG_BODIES[body].R` (was a schematic constant `3` for every planet/moon before this phase — that schematic value is also why the old glyph could shrink arbitrarily far below the intended floor at extreme zoom-out; the floor math itself, `_trajBodyPxR`'s `Math.max(trueR·zoom, _TRAJ_MIN_BODY_PX)`, was already correct and is unchanged).

- **Surface geometry** (`_trajLatLonUnit`, `_trajSpinRotate`, `_trajGeoPolyPath`, `_trajGeoEllipsePath`, `_trajSurfacedDiscSVG`, 574): lat/lon → unit-sphere point in the body's own frame → rotated by the body's spin angle about the WORLD +z (ecliptic-normal) axis → projected through the pass camera (`_trajProj3`, the one projection seam) → scaled to the disc's screen radius. Front-hemisphere only (camera depth ≥ 0); polygon runs break (new subpath) at the limb, same spirit as the existing `viewClampUnits` pattern — filled paths with limb breaks are an accepted rudimentary approximation, not survey-grade cartography.
- **Data source** (236-geo-data.js, new this phase): `PROG_GEO_EARTH` — Natural Earth 110m coastline polygons, Douglas-Peucker simplified, packed as flat integer-tenths-of-a-degree arrays. `PROG_GEO_FEATURES` — Moon maria + Mars regions as `{lat, lon, rLat, rLon, kind}` ellipse approximations from published feature centers (USGS Gazetteer). `PROG_GEO_STYLE` — per-body fill palette, a DATA-palette exemption from the no-chromatic-literals chrome rule (same class as `PROG_BODY_COLORS`, 570). Jupiter/Saturn get no feature data — their bands are procedural lat-strips through the same projection (so they visibly tilt with camera elevation, not a flat texture).
- **Spin angle is a display convention, not a calibrated physical rotation** (`_trajBodySpinAngle`, 574): **Earth** = `2π·viewT_s/86164.1` (sidereal day), **Mars** = `2π·viewT_s/88642.66` (sidereal day) — both with an implicit epoch offset of 0 (NOT tied to any real prime-meridian-at-epoch reference; a Greenwich-facing coastline at viewT=0 is coincidence, not GMST). **Moon**: tidally locked — `progBodyAngleAt('Moon', t) + π`, i.e. the Moon's own Earth-relative orbital position angle (which IS the Earth→Moon direction) plus π, so the nearside meridian (lon 0, where the packed maria live) faces the ACTUAL Moon→Earth direction at every viewT, not a fixed angle. All other bodies: spin angle 0 (no vector surface features are drawn for them, so the angle is unused).
- **Axial tilt is NOT modeled — the single biggest honesty gap of this phase**: every body's spin axis is assumed coincident with the ecliptic normal (`_trajLatLonUnit`'s `+z`). Real axial tilts (Earth 23.4°, Mars 25.2°, others un-modeled here) mean the drawn terminator/pole geometry and the exact limb-crossing latitude of near-polar features are visibly wrong whenever the camera isn't near top-down (`el ≈ 90°`) — acceptable for the "mission-planning flavor" this phase targets, wrong for anything claiming seasonal or lighting accuracy. See critique 55.
- **Limb shading**: one `<radialGradient>` per surfaced-disc instance (unique id per render pass, `_trajGeoIdSeq`), transparent center → ~30–35% black rim (`PROG_GEO_STYLE[body].limb`, a neutral alpha scrim — allowed per the theming exemptions), center offset ~32% of the disc radius toward the Sun's SCREEN direction (a cheap 2D `atan2` between the already-projected Sun and body render positions, not a 3D sun-vector solve) — a rudimentary day-side/terminator cue, not physically-lit shading.
- **Perf clamp**: if the disc's true screen radius exceeds 4× the viewport diagonal (camera deep inside a body, disc mostly off-screen), `_trajSurfacedDiscSVG` skips surface-geometry sampling entirely and emits the base-color fill + limb gradient only — bounds path-string size for the worst-case zoom without a separate viewport-region clip of the lat/lon data itself.
- **Gate** (`tests/math.test.js`, 16 new assertions, 2026-07-10): `_trajLatLonUnit` lands the expected axis points and stays unit-length; `_trajSpinRotate` about +z leaves the pole fixed and rotates the equator correctly; `_trajBodySpinAngle` is monotonic in viewT for Earth/Mars, zero for a body with no vector surface (Venus), and matches `progBodyAngleAt('Moon', t) + π` exactly for the Moon; `_trajTrueBodyRadiusKm` returns the real `PROG_BODIES[body].R` (not the old schematic 3) and the documented Sun constant.

**R6.2 round-4 fixes (574, 2026-07-11)** — three defects found in the round-3 flight test:
- **Exact hemisphere clip, not per-vertex culling** (`_trajHemiClipRuns`, `_trajClippedRunsToPath`): the original approach dropped back-facing vertices and let the SVG fill close the resulting gap with a straight chord through the disc interior — visible pole-on as giant wedges of land color sweeping across the whole disc. The fix clips each polygon (coastlines, maria/region ellipses, gas-giant bands — all three call sites now route through the one clipper) against the front hemisphere (`_trajProj3(...).depth ≥ 0`) and, for every edge that crosses the boundary, interpolates the two endpoint UNIT VECTORS linearly and renormalizes — exact, not approximate, because depth is linear in the input vector, so the interpolated point has depth exactly 0 and therefore lands exactly on the unit circle. Consecutive open runs are stitched with an SVG elliptical arc along the limb (radius `rPx`) instead of a chord; the short-way arc is used when the winding direction is ambiguous (acceptable at coastline/maria polygon scale — the existing disc `clipPath` remains as a second safety net). A real bug surfaced during gate-test authoring: a polygon whose front run straddles the array's start/end seam (dirs[n−1] front, dirs[0] front, connected the "long way" around through the back) got split into a spurious extra fragment by a naive single-pass loop; fixed by splicing the loop-end open run onto the front of `runs[0]` when a wraparound continuation is detected. 5 new gate assertions (front-only/back-only/single-crossing/double-crossing/wraparound-continuity).
- **Stable per-instance ids, not a per-render counter** (defect2): `_trajGeoIdSeq` minted a fresh `<radialGradient>`/`<clipPath>` id every ~33ms rebuild during rotation; since the world layer is swapped wholesale via `innerHTML`, a paint could land between "old ids torn down" and "new ids wired up," reading as a one-frame flicker. Replaced with a deterministic id per body per render instance (`traj-geo-<missionId>-<body>-limb/-clip`) — the wholesale swap now recreates the SAME ids atomically every pass, so no `url(#)` reference ever dangles. Verified stable across 20 synthetic rotation frames (same id string every frame) and non-colliding between Earth and its Moon in the same pass.
- **Real terminator, not a 2D screen-dot offset** (`_trajTerminatorNightPath`, defect3): the limb-shading gradient's day-side offset (unchanged, still a cheap 2D `atan2` cue) made day/night unreadable at a glance. Added an actual terminator: the body→Sun unit vector is now carried as a WORLD-frame 3-vector (`sunDir3`, not a screen angle) all the way into the disc renderer; its camera-projected `depth` component is the orthographic terminator ellipse's minor-axis fraction. Rather than solving the ellipse algebraically, the terminator great circle (parametrized by two world-frame vectors spanning the plane ⊥ `sunDir3`, NOT spin-rotated — illumination depends on sun geometry, not the body's own rotation) is sampled and pushed through the SAME hemisphere clipper coastlines use (doubles as the "ellipse feature" clipper), giving the front-visible terminator arc exactly. That arc plus the correct limb arc (the one that avoids the Sun's own screen angle — the other limb arc is day-side) closes the night region, filled with the existing neutral black-alpha scrim (0.4, allowed exemption) clipped to the disc. Degenerate limits (`|depth| ≥ 0.995`, sun ~directly toward/away from the viewer) are handled explicitly rather than relying on the general formula to degrade gracefully: fully lit emits no scrim at all; fully dark emits a full-disc scrim capped at 0.55 alpha (a soft floor so surface features stay faintly legible, never pitch black). Verified: the terminator tracks Sun-body geometry (near-fixed screen path over a 6h `viewT` scrub, matching Earth's small orbital drift) while land geometry rotates ~90° under it in the same 6h — confirms the terminator does NOT spin with the body, as required.
- **Gate**: 9 new `_trajHemiClipRuns` assertions (tests/math.test.js) bring the suite to 508 total. Perf: 40 synthetic rotation frames at Earth's surfaced-disc LOD tier averaged ~8ms/frame render — well under the 33ms rotate-drag throttle, no observed regression from the added clip/terminator work.

## 7n. Launch realism bundle — site lon, site marker, mock ascent, launch-time→RAAN R6.3 (220/360/570/574, 2026-07-10)

Closes critique 49's "real missions pin RAAN at launch time" gap: an orbit's Ω is now something the app can DERIVE from where/when you launch, not just something you type in directly (§7j still owns the direct-authoring path — this phase feeds that same `lan_deg` field from a physically-motivated computation instead of a blank number box).

- **Sites gain longitude** (220-launch-sites.js): every `LAUNCH_SITES` entry already carried `lat`/`azMin`/`azMax`; this phase adds real `lon` (Cape Canaveral −80.6°, Vandenberg −120.6°, Baikonur 63.3°, Kourou −52.6°, Wallops −75.5°, etc.) and threads it through the Vehicles-page site object (`site:{lat,azMin,azMax,lon,name}`, 140-physics.js) so a saved vehicle's launch site is a full lat/lon point, not just a latitude for the Townsend rotational-velocity term. **Backward compatibility**: a vehicle/preset saved before this phase has no `lon` key; every consumer treats `lon == null` as "can't derive RAAN geometry, everything else (ΔV math, azMin/azMax) works exactly as before" — no throw, no forced re-save. `_fleetVehicleSpecFromLib`/`fleetSwapVehicle` (560) now copy `site` onto fleet entries so a mission's LAUNCH event can inherit it without re-picking.
- **Site marker + mock ascent path** (574): presentation layer only, same class as §7m's surface rendering — zero ΔV/physics consequence. `_trajLaunchSiteAndAscentSVG` finds the mission's first LAUNCH event with a resolvable site (`_missionLaunchSiteFor`, 570 — authored on the event, else inherited from its fleet vehicle) and draws (a) a small ring+label marker at the site's lat/lon rotated to its CURRENT position via the exact same `_trajLatLonUnit`/`_trajSpinRotate`/`_trajBodySpinAngle`/`_trajSurfacePoint` pipeline §7m's coastlines use — self-consistent with the rotating globe by construction, not a re-derivation — hemisphere-culled the same way (`depth < 0` skip); (b) a dashed quadratic-bezier "mock ascent" curve from the site's position AT LAUNCH TIME (`_trajBodySpinAngle('Earth', e.launchTime_s)`) to a nominal insertion point on the launch orbit's ring, `progOrbitPointAtE` (360) evaluated at eccentric anomaly `E ≈ mean_motion × 510s` (~8.5 min nominal ascent, **mean anomaly used as a stand-in for eccentric anomaly** — a real Kepler solve is unwarranted for a schematic curve explicitly labeled non-propagated in its `<title>`). Both endpoints and the marker share the disc's current `rPx`/`cx,cy`/spin so the whole picture stays coherent as `viewT` scrubs; LOD-gated with the same `missionOrbitRing` window as mission rings, only drawn once the disc has reached the surfaced-disc LOD tier (`trueRpx >= _TRAJ_SURFACE_PX`, same threshold as coastlines).
- **Launch-time → RAAN authoring** (360's pure helpers + 570's LAUNCH card): three framework-free functions, gate-tested directly —
  - `progLaunchAzimuthDeg(latDeg, incDeg)`: spherical-trig required heading, `sin(az) = cos(i)/cos(lat)`. Returns both roots (`azNE` — ascending-node/NE departure, `azSE = 180 − azNE` — descending-node/SE departure); `{unreachable:true}` when `inc < |lat|` (no azimuth reaches an inclination below the launch latitude without a plane-change dogleg).
  - `progLaunchRaanFor(siteLatDeg, siteLonDeg, incDeg, tLaunchSec, spinFn)`: `Ω = λ_inertial − asin(min(1, tan(lat)/tan(i)))` where `λ_inertial = site.lon + spinFn(tLaunchSec)` is the site's INERTIAL (non-rotating-frame) longitude at the instant of launch — `spinFn` is injected (570 passes `_trajBodySpinAngle('Earth', ·)` when 574 has loaded) so the 360 module itself has no dependency on the renderer. Retrograde (`i > 90°`) mirrors the offset (`180° − dOmega`). `{unreachable:true}` under the same `inc < |lat|` condition as the azimuth function (same physical constraint, independently derived).
  - `progLaunchNextWindowS(currentRaanDeg, targetRaanDeg, siderealPeriodS)`: `Δt = ((Ω_target − Ω_now) mod 360°) / ω_earth`, wrapped forward into `[0, period)` — never negative, never a same-value NaN.
  - **Self-consistency, not calibration**: all three reuse `_trajBodySpinAngle`'s Earth constant (`PROG_EARTH_SIDEREAL_S = 86164.1`, duplicated in 360 with a comment pinning it to 574's value) and its **uncalibrated prime-meridian epoch** (critique 55) — the RAAN this produces is internally coherent with everything else drawn (the rotating globe, the site marker, the ascent curve) but is NOT tied to a real UTC launch-window clock. A "site.lon = −80.6°" launch at `t=0` does not correspond to any specific real-world day/time; it is a self-consistent in-app convention, same caveat class as the rest of §7m.
  - **LAUNCH card wiring** (570): a Launch Site picker (built-in sites with `lon != null`, or "no site"), a Launch Time (seconds-from-epoch) field, and the LAN field are threaded together — editing site/launch-time live-recomputes LAN (`missionLaunchGeoUpdate`) and annotates it "(from launch time)"; typing directly into LAN with no launch time authored is a plain manual override (`_lanFromLaunchTime` cleared); clearing the launch-time field frees LAN back to manual. A caption line shows the required azimuth pair and, once a LAN value exists (authored or derived), a "window in ~Xh Ym" readout (`progLaunchNextWindowS` against the RAAN launching right now (`t=0`) would produce) — same idea as a real launch-window countdown, computed against the app's internal clock rather than UTC.
  - **What this does NOT do**: it does not enforce the derived RAAN through a wait/COAST event — authoring a launch time computes what plane you'd reach, but nothing currently blocks launching at a time whose derived RAAN conflicts with a separately-authored one (the LAN field is simply overwritten last-write-wins, same as any other authored field). Reaching a plane at a SPECIFIC calendar time via an automatic launch-window solve remains the critique 49(c) gap, now narrower (the geometry is derivable; the scheduling isn't).
- **Gate** (`tests/math.test.js`, 12 new assertions, 2026-07-10): azimuth at `i = lat` is exactly 90° (due east); KSC (28.5°N) → 51.6° (ISS inclination) pins to ≈44.98° NE / ≈135.02° SE; `i < lat` flags `unreachable` for both azimuth and RAAN rather than emitting NaN/garbage; RAAN at KSC/28.5°/t=0 and KSC/51.6°/t=0 pin to their computed values; a 2h launch-time offset shifts RAAN by exactly the corresponding Earth-spin delta (self-consistency check, not an independent physical reference); next-window wraps forward through 360° and is ~0 when already under the target plane.

## 7o. World-view handedness fix — screen-y mirror bug R6.3' (574, 2026-07-11)

**Convention, stated once so this never regresses silently**: every world-frame vector in this program (heliocentric/planetocentric position from `progBodyEphemState`/`physElementsToState`, and the surface lat/lon → unit-sphere mapping `_trajLatLonUnit`) uses the standard right-handed ecliptic frame — `+z` = ecliptic north, reference direction `+x`, and angles increase **counterclockwise** as seen from `+z` looking down (i.e. `+x → +y` is the positive-angle sense: prograde orbital motion and increasing east longitude both go this way). This is a math convention (x-right, y-UP).

**The bug**: `_trajProjectVec` (130) — the ONE seam every world-space consumer routes through (surface points via `_trajSurfacePoint`, orbit rings, the maneuver gizmo, hover rails, ring-direction tangents, the site marker/mock-ascent pipeline) — mapped world `(x,y)` straight to screen `(u,v)` with `v = y` at the default top-down camera (`el = π/2, az = 0`). SVG screen space is **y-DOWN**. Plotting world-`y` directly onto a y-down screen is a reflection across the x-axis, not a rotation: it flips chirality. Effect was system-wide because it's one shared seam:
  - **Geography mirrored**: Australia rendered LEFT of Asia (correct CCW-from-north order is Europe → Asia → Australia → Pacific → Americas; the bug reversed it).
  - **Orbital motion mirrored**: real prograde motion (CCW from ecliptic north) rendered as clockwise on screen.
  - Everything downstream (site marker/Florida placement, gizmo prograde-handle direction, ring direction-of-motion fade, hover-ball tracking) inherited the same mirror because none of them duplicate the y-mapping — they all call `_trajProj3`/`_trajSurfacePoint`, which call `_trajProjectVec`.

**The fix**: negate the projected screen-y in `_trajProjectVec`'s return (one line): `y: -(ya·ct − z·st)` instead of `ya·ct − z·st`. `depth` (the camera-forward component used for hemisphere culling/z-order) is untouched — only the 2D screen mapping flips. Because every consumer is downstream of this one function, the fix is global and self-consistent by construction; no other file needed a compensating change.

**Gate impact**: the R2 projection-identity goldens (§ R2 block, `tests/math.test.js`) are pinned to `_trajProjectVec`'s literal output and DO change with this flip (re-derived in the same commit: `el=90` identity is now `v=-y` not `v=y`; `el=0` edge-on is now `v=+z` not `v=-z`; the `az=90` case is now `v=-1` not `v=+1`). Pure-angle goldens that don't touch the projection function (`progLaunchRaanFor` §7n, `progOrbitSamplePoints`'s radius/closure checks, hemisphere-clip depth tests) are untouched — they operate on world-frame vectors or camera-depth, never on the flipped screen axis.

**Not touched**: `_trajWorldToScreen` (622) — the render-space → overlay-px affine map used by labels/markers — has no independent y-mapping of its own; it linearly scales/translates whatever `_trajProj3` already produced, so it inherits the fix automatically and needed no edit.

## 8. Trade studies (165)

All sweeps evaluate the SAME pure pipeline per point: assemble vehicle (worksheet DOM or `_tsVehicleToBase` adapter) → S1.5 expand → `destOnOrbitDV` for the destination (or the swept parking/inclination values via the minimal circular-orbit plane-change replica `_tsOnOrbitDVCircular`) → `lvMaxPayload` / `lvPerformance`. Metrics: max payload; ΔV margin (at the vehicle's payload input); **payload fraction** = maxPay / tMas(at maxPay) × 100 (tMas from lvPerformance = exact liftoff mass incl. fairing/boosters — not an approximation); total ΔV; total burn time; liftoff T:W = A0/g0. Capability curves are clipped at their interpolated zero-crossing; margin curves keep their negative region. **Tornado**: one-at-a-time ±p% on each authored parameter (per-stage Isp/dry/prop, booster prop/thrust/Isp), re-deriving the S1.5 split per evaluation, bars sorted by |swing|; no parameter interactions.

## 9. Regression protection

`tests/math.test.js` (run by every `python build.py`): Tsiolkovsky hand values, lvPerformance invariants + golden snapshot, booster single-vs-array equivalence, lvMaxPayload convergence, `destOnOrbitDV` pinned to browser-captured `calculate()` outputs (GTO / plane change / escape), S1.5 split conservation + expanded-golden + divergence proof, preset name-resolution integrity. **Changing physics intentionally = re-capture goldens in the same commit, with a dated comment.**

---

## Critiques (known weaknesses, roughly by severity)

**Ascent model**
1. K3/K4/T3s/Tmix are fitted empirical constants of unstated provenance and envelope. They validate well against 1960s–present orbital LVs (Saturn V, F9 land within a few % of published capability), but nothing warns the user when a design leaves the fit's envelope (TWR ≪ 1.2 at liftoff, very lofted or depressed trajectories, air-launch).
2. Single static Isp per stage/engine — no altitude dependence. First-stage numbers are whatever the data author blended by hand; two vehicles with identical physics but different Isp-averaging conventions aren't comparable at the few-percent level.
3. The booster integrator blends Isp correctly per interval, but the Townsend penalty is computed from aggregate `A0`/`Isp_avg`/`Ta` — parallel-staged vehicles get a serial-equivalent loss estimate.
4. `Ta`'s fractional-stage interpolation uses an exponential prop-fraction heuristic, not the actual time-to-Vcirc within the stage.

**Destination ΔV**
5. Plane-change logic assumes initial inclination = |site latitude| and charges nothing for inclinations above latitude (real azimuth constraints and range-safety corridors can make those expensive). Retrograde/SSO targets are only "charged" via lost rotation credit.
6. The combined-plane-change-at-apo optimization triggers on an arbitrary `apogee > 10,000 km` threshold; elliptical targets never get the combined treatment (plane change added scalar, not vectorially).
7. Tolerance constants (5 km "circular", 50 km "same perigee", 0.5° plane-change deadband) are unlabeled magic numbers.
8. Escape declination penalty is a crude great-circle plane change at parking speed; no DLA/azimuth interaction.

**S1.5**
9. BECO located on stage-only mass (payload ignored) — the "< 5 %" error claim is asserted, not tested, outside Atlas-class proportions.
10. Phase-1 blend (when used) is static; booster-pack jettison is instantaneous with no thrust decay, and `s15_beco_twr` defaults to 1.2 regardless of vehicle.
10a. The blend's sustainer term uses `s15_sust_isp`, which the library stores as the VACUUM value (it primarily serves Phase 2) — so the Phase-1 blend is optimistic at low altitude, where the sustainer's real SL Isp is far worse (LR-105: ~215 s SL vs 309 s vac). With Atlas data the blend RAISED Ph.1 Isp (282 → 285.5 s) rather than lowering it. To make the blend represent liftoff conditions you'd need a separate sea-level sustainer Isp field, or the user must fold the pessimism into `s15_boost_isp`. Known and accepted for now.

**Mission / node map**
11. Two ascent models coexist: Townsend (when LV performance data exists) vs `Vcirc + fixed-loss table`. A mission can silently switch fidelity depending on how the vehicle was created. The loss table itself (Earth 1550 etc.) is a rough constant independent of TWR or target altitude.
12. LOI/TEI patched-conic underestimates (~10 %, documented); TEI = LOI symmetry is a simplification; no free-return or finite-burn losses anywhere.
13. Interplanetary transfers: circular coplanar planets, no plane change, no arrival-timing coupling between the node map ΔVs and the porkchop's Lambert solutions — the two subsystems can disagree about the same trip.
14. Docking tolerance windows (1 km, 0.1°, 1° LAN) are arbitrary, and `epoch`/phasing is entirely ignored — co-orbital ≠ co-located.
15. Lunar/Mars ascent = scaled constants; no TWR sensitivity, no plane targeting from launch site latitude on those bodies.
16. Boiloff: constant fractional rate, insulation as a linear multiplier in the exponent; no tank thermal state, no ullage/pressurization losses. Rate is constant over the whole coast regardless of duration — a 261-day cislunar loiter and a 3-day one lose mass on the exact same exponential curve, with no allowance for a tank's fill fraction changing its effective surface-to-volume ratio over time.
16f. Propellant-type provenance is asymmetric and easy to get wrong silently: launch-vehicle stages always get a valid cryo/storable type (Isp-inferred by `progVehicleDefToLiveStages`), so they DO boil off; spacecraft stage definitions default to `propType: 'MMH/NTO'`, which is not a registered key in `PROG_PROPELLANT_TYPES` — an unedited spacecraft stage therefore boils off NOTHING, not because MMH/NTO is storable (it correctly would be, if modeled) but because the lookup silently misses. The net effect (storable-by-default) happens to be physically reasonable for a real MMH/NTO service module, but it's an accident of naming, not a deliberate default — a future propellant type added to the registry under a different key would need to also update this default, and nothing enforces the two stay in sync.
16g. Boiloff during LAUNCH ascent is modeled (applied, not skipped) but negligible by construction — ascent burn times are minutes, and the exponential over minutes is indistinguishable from zero at any realistic cryo rate. Documented rather than special-cased to avoid a silent gap in the "every clock advance boils off" invariant.

**Mission time (T1)**
16a. Hohmann-model TOF ≠ real trajectories: the translunar half-ellipse estimate lands ~5 days vs. Apollo's actual ~3-day free-return trajectory — the model doesn't know about faster non-Hohmann transfers, and overrides are the intended escape hatch for any mission that needs an accurate date.
16b. All burns (BURN and MANEUVER) are treated as impulsive/instantaneous in the clock — only the COAST/transfer between them accumulates time. A long low-thrust burn would in reality itself take meaningful time; not modeled.
16c. Interplanetary TOF ignores launch-window phasing (planets aren't actually where a Hohmann departure needs them) unless the mission has a specific porkchop-selected Lambert window (`PROG_ACTIVE_PROGRAM.launchWindow`) — absent that, the number is a generic Hohmann estimate, not a real date.
16d. `durationOverride`/`dvOverride` intentionally bypass the physics by design — they exist so the user can substitute a known real value (e.g. actual Apollo TOF/ΔV) for the model's estimate. The UI badges overridden events "(custom)" so this is never silently confused with the computed value, but nothing prevents an override that's physically inconsistent with the vehicle's actual propellant/ΔV budget beyond the normal margin checks.
16e. Transit-corridor convention: a coast is charged once, on the leg EXITING the corridor node (TLC→LLO carries the ~5 d translunar TOF; LEO→TLC is the impulsive injection, 0 s). Found the hard way: the first implementation charged both legs and doubled every corridor transit.

**Boiloff on clock advancement (T2)**
16h. Boiloff is applied to EVERY live vehicle whenever ANY vehicle's clock-advancing event fires — a docked depot idling in LEO boils off in step with the vehicle actually coasting to the Moon, which is physically correct (time passes for everyone), but it means a mission with many idle vehicles pays the boiloff cost once per clock-advancing event rather than once per vehicle-day, which is the same total mass loss but a different (equivalent) computational path worth noting for anyone auditing `applyMissionBoiloff` in `missionRecompute` (570).
16i. The readiness check's cumulative-boiloff percentage (572, `boiloff-losses`) divides by "initial propellant capacity," which is captured at LAUNCH/DEPLOY time (`fv._initialPropCap`). A vehicle born mid-mission from a SEPARATE or DOCK (no LAUNCH/DEPLOY of its own) has no cached initial value; the check falls back to that vehicle's CURRENT capacity at the moment boiloff is first detected, which understates the percentage for any vehicle whose stages were combined after already losing propellant to burns — a known, documented approximation, not a hard guarantee.

**Body kinematics (C1a) — RETIRED BY R1 (2026-07-09)**
16j. ~~No ephemeris~~ — RESOLVED: R1 rails are the JPL approximate mean elements evaluated at real absolute time (`epochJD`); `t=0` is a calendar date.
16k. ~~Perfectly circular orbits~~ — RESOLVED: real eccentricities (Mercury e=0.2056, Moon e=0.0549) with elliptical speed variation (analytic Ė-form velocity).
16l. ~~Everything coplanar~~ — RESOLVED: bodies have real inclinations and 3D states (R1), the renderer is a 3D camera over true geometry (R2), and vessel states carry their authored inclination (R3, §7h). Remaining plane fictions are the Ω=0 convention (critique 49) and the schematic plane-change accounting (critique 50).

**Trade studies**
17. Inclination/altitude sweeps use the minimal circular-orbit ΔV replica (clearly labeled) — fine for trends, not for elliptical or escape comparisons.
18. Tornado is one-at-a-time and symmetric: no parameter interactions, no asymmetric sensitivities (e.g. Isp loss usually hurts more than gain helps near margin limits).

**Trajectory view LOD + mission-state rendering (C2)**
21. Vehicle-on-arc position (`_trajArcPointAt` driven by `_trajLegPathFraction`) is uniform PATH-LENGTH-ANGLE progress along the drawn half-ellipse, not true orbital motion — real vehicles move faster near periapsis (non-uniform true-anomaly rate under Kepler's second law), so the dot's on-screen speed is constant while a real vehicle's isn't. Schematic "roughly here" indicator only. — **Retired for physics-backed legs (P3, 2026-07-09)**: legs with a propagated side-table record place the dot by interpolating the propagated samples at viewTime (`_trajPolylinePointAt`, §7f), which IS Kepler-true to sample resolution; the linear-fraction schematic remains only for legs without physics (physics off, degenerate/unpropagated legs).
22. The Moon-frame patched-conic seam fallback (`_trajLocalRadius`'s `max(2×bodyR, 0.5×mo.r)` departure radius for a transit arriving at its own destination frame) is an arbitrary rendering placeholder, not a Hill-sphere/SOI calculation — it exists purely so the arc has SOME finite departure point to draw from in the destination's own frame; the radius value carries no physical meaning (it's tuned to clear the transferArc LOD window's px floor across camera anchors, not to model the real hyperbolic approach geometry).
23. Orbit-ring "history" dimming only tracks vehicle-LEVEL EXPEND events (`expendMetByOwner`, keyed by originKey) — a stage-level EXPEND (partial jettison) doesn't retire the ring's owner, so a ring whose vehicle survives via a remaining stage never dims even if the specific stage that occupied that orbit is long gone.
24. The zone-of-influence fade-in threshold (~30px neighborhood extent) and the LOD ramp's "20% of `lo`" cap are both hand-tuned constants with no derivation beyond "looked right in testing" — same category as the pre-existing magic-number critiques elsewhere in this document (see critique 7).
25. ~~Planet-phase calibration is per-mission fiction~~ — RETIRED BY R1 (2026-07-09): the calibration and its second-leg ghost rendering were deleted with the schematic rails; all missions and the porkchop plotter now share the ONE real ephemeris. The trade: a schematic Hohmann arc at an out-of-window MET now honestly fails to reach its planet (converged:false, ghost marker at the true arrival position) instead of being bent to connect.
26. **Zone-of-influence gate blocks mission content at moonless-planet camera anchors** (found while implementing planet-phase calibration, not introduced by it — pre-existing): `_trajWorldSVG`'s per-planet `zoiAlpha` computation is `Math.max(neighborhoodExtentPx, _trajBodyNeighborhoodPx(body, zoom))` against the `zoneOfInfluence` window's 30px floor, but `neighborhoodExtentPx` is a fixed 6px clamp by construction of `_trajBodyPxR` (not derived from actual mission-content extent) and `_trajBodyNeighborhoodPx` only considers a body's MOONS. A moonless planet (Mars, Venus, Mercury, etc.) therefore NEVER opens its own zone-of-influence at any zoom, so its transfer arcs and orbit rings never render — verified in-browser (a Mars-orbit mission leg's arc stayed invisible with the camera anchored directly on Mars, at every tested `cam.wKm`; content only became visible after monkeypatching `_trajBodyNeighborhoodPx` in a debug session, confirming the extraction/geometry itself is correct and only the LOD gate is at fault). This is a real user-facing gap (the planet focus-bar button flies exactly to this camera state) — flagged as a follow-up task, not fixed here (out of this change's scope: `574-trajectory-view.js`'s general LOD architecture, not the calibration feature itself). — FIXED (2026-07-09): the ZOI gate and the fly-to fit now share one neighborhood definition (_trajBodyNeighborhoodKm: moon rings, mission-content extent, 4R floor), so any camera that frames content opens the gate that draws it.

27. **Physics core rails — R1 update**: ~~circular and coplanar~~ — rails are now real mean-element ephemerides (§7a); the remaining rail-fidelity gaps are the mean-element approximations themselves (critiques 41–43). `physKeplerPropagate` still returns `null` on Newton non-convergence (near-parabolic + extreme dt corners) — every consumer MUST handle the null rather than assume a state.
28. **Integrator (P1, 386) accuracy is schematic-grade, not ephemeris-grade**: 2nd-order leapfrog at 360 steps/orbit accumulates ~0.3 km/orbit of phase error, and Jacobi fidelity is ~1e-3 on high-e lunar encounters — fine for illustrating free returns and flybys, NOT for real trajectory design. Close-approach accuracy also depends on the step ladder catching the encounter: a vessel screaming through the Moon's SOI at a coarse cruise step takes its first fine step only AFTER the SOI event is detected (transition location error up to one coarse step). The rails' small dynamical inconsistency is still absorbed as a permanent perturbation — R1 flavor: the Moon's mean-element rate L̇ (solar-perturbed, 27.32 d) differs ~0.5% from the two-body rate √(μE/a³) the integrator's gravity field implies, so a body's rail motion and the field it generates don't exactly agree (same class as the old ω²d³ vs μ mismatch, now smaller and better justified).
29. **SOI transitions are detected, not bisected, in P1**: the handoff happens at the first step-end INSIDE the new sphere rather than at the exact boundary crossing (physFindEventTime exists but isn't wired into the transition path yet) — a deliberate simplification; revisit if P4 shooter convergence turns out to be sensitive to the patch point.
30. **P2 analytic phasing is aim, not targeting**: `physSolveNodeBurn` places the burn point assuming a pure Hohmann half-ellipse in a two-body field, then propagates in the FULL perturbed field — lunar gravity bends the real trajectory, so the propagated leg generally arrives near, not at, the Moon (recorded as SOI-entry `converged` metadata; a clean miss is legal and expected until P4's differential corrector). Interplanetary phasing is cruder still: the LEO burn point is placed at a schematic heliocentric angle with no ejection-asymptote geometry (that arrives in P3's `physEscapeGeometry`).
31. **MNODE honesty gap (P2)**: an MNODE burns real propellant through the rocket equation but advances NO orbit state — the node-map orbit and the physics-side trajectory diverge silently after any MNODE. The band/orbit views keep showing the pre-burn orbit. P3 (render from side-table) and P4 (authoring UI + shooter) close this; until then MNODE is a console-only accounting event.
32. **P2 physics TOF is stale-by-one**: the duration a replay uses comes from the PREVIOUS rebuild's legs, reconciled by at most one extra recompute pass per edit. If an edit changes an upstream duration, downstream legs fall back to schematic TOF for that single pass (their MET signature no longer matches) — momentarily inconsistent durations inside one recompute cycle are possible by design, never persisted.
33. **P2 delivered-ΔV coupling through boiloff**: because physics TOF changes coast durations and boiloff is time-dependent, enabling physics shifts *delivered* ΔV/propellant on cryo missions slightly (Apollo seed: +1 m/s / +8 kg on LOI) even though *required* ΔV is byte-identical. Anyone diffing mission totals across the P2 boundary must compare required, not delivered, figures.
34. **P3 spur orientation is schematic, not solved (574/385)**: the departure spur's asymptote is aligned with the departure body's heliocentric velocity direction (prograde/retrograde by destination), NOT with the ejection asymptote a Lambert/porkchop solution would demand — and the capture spur is an explicit mirror (incoming asymptote anti-parallel to a schematic arrival v∞ ≈ ∓v_planet) with no hyperbolic timing at all. The spur's burn point on the parking ring therefore generally DISAGREES with the physics leg's analytically-phased burn point (§7e) and with the heliocentric arc's departure end — they are separate schematic layers that agree in spirit, not in angle. P4's shooter is the fix.
35. **P3 polyline is decimated and clip-approximate**: drawn physics legs use ≤256 integrator samples (frame changes preserved), linearly interpolated for the vehicle dot — fine visually, but tight flyby curvature between samples is chorded, and the clip at the arrival leg's `metArrive` trusts the mission's replayed duration rather than re-locating the periapsis event, so up to one sample interval of post-arrival trajectory can render (or be cut) at the seam. (The second half of this critique — polylines rendered against calibrated positions while propagation used uncalibrated rails — is RESOLVED by R1: there is one position source and no calibration.)

36. **P4 shooter targets closest approach, not arrival state**: the corrector drives the trajectory's closest approach to the destination to the destination-orbit radius — it does NOT control the arrival v∞, approach direction, or capture geometry, so the arrival burn's ΔV (patched-conic, §6) and the propagated approach can be mutually inconsistent (a trajectory arriving retrograde is charged the same LOI as a prograde one). The 2-DOF timing component (0.5 km/s scale factor) is a hand-tuned conditioning constant, same category as critique 7/24.
37. **P4 MNODE honesty gap, narrowed but not closed**: the MNODE trajectory is propagated and drawn (closing critique 31's "advances NO orbit state" render half), but the NODE-MAP orbit still doesn't change — the band view and downstream maneuvers keep planning from the pre-burn orbit while the trajectory view shows the post-burn ballistic path. Also the burn is placed on the MEAN-altitude circular reconstruction at θ = n·MET — an elliptic parking orbit's true position at that MET is not honored (same circularization simplification as §7e's solved burns).
38. ~~P4 free-return solve leans on rotational symmetry~~ — **REWORKED (R3, 2026-07-10)**: the rotated-golden seed is replaced by the deterministic seed scan of §7h (the rotation shortcut could not seed an inclined departure at all — measured: the R1 golden never re-enters the return band from a 28.5° ring). The [30, 500] km band check on the FINAL propagation still guards against a bad seed shipping a non-returning "free return"; window-limited coverage and the scan's tuned constants are critique 51.
39. **P4 shooter cache is keyed on authored identity only**: `fromNode|toNode|met|dv` — a change to the gravitating-body set, integrator constants, rails, or EPOCH invalidates nothing (module-lifetime cache, cleared on reload). Deliberate for code-level constants; note the new epoch wrinkle: an epochJD edit mid-session (no UI yet, console only) would serve stale aims until reload.

**Real ephemeris rails (R1, 2026-07-09)**
40. **Rings are still schematic circles; glyphs ride real orbits**: the top-down view draws heliocentric and moon rings at the old mean radii (`PROG_HELIO_R`/`PROG_MOON_ORBITS.r`) while body glyphs sit at real element-evaluated positions — an eccentric planet visibly sits off its ring (Mercury: up to ~9.1e6 km ≈ 24 px at Mercury-fit zoom; measured 2026-07-09). Stated in the view footer; R2 replaces rings with true sampled orbits. — **RESOLVED (2026-07-09, R2)**: rings sample the real elements (§7f-R2); Mercury's glyph measured 1.9 px from its 120-point orbit polyline (vertex quantization, on-curve ≈ 0).
46. ~~R2 mission-orbit rings assume Ω = ω = 0~~ — **RESOLVED for FLOWN rings (R3.1, 2026-07-10, §7i) and AUTHORED rings (R3.2, 2026-07-10, §7j)**: a ring whose (body, peri, apo) matches a converged physics leg's departure or arrival now draws with that leg's ACTUAL plane (`rec.elements`, tooltip "plane from flight") instead of the Ω=ω=0 convention — the drawn ring and the physics that created it are one object for the departure case (literally, by construction) and agree to finite-difference accuracy for the arrival case. R3.2 adds a tier ABOVE that: an orbit spec that explicitly authors `lan_deg`/`argp_deg` wins outright over both flight-derived and default, tooltip "Ω,ω authored" — factored through the pure `_trajRingOrientationFor(rec)` resolver so the precedence is one function, gate-tested directly. The Ω=ω=0 convention survives only for rings with NEITHER physics parentage NOR an authored orientation (schematic-only legs, unauthored define-an-orbit previews, orphan snapshots) — those still draw Ω=ω=0, annotated as before. Two same-inclination orbits launched at different times still can't be distinguished if neither is physics-backed NOR authored.
47. **R2 painter sorting is per-body, not per-primitive**: a body's glyph and ALL its embedded content share one depth (the body center), and orbit rings always draw behind glyphs. Exact occlusion for ring-vs-glyph and content-vs-content across bodies is not modeled — visible only in contrived tilted alignments; a full z-sorted primitive pipeline is not worth the complexity at SVG's layering granularity.
48. **R2 rotate/tilt interaction constants are hand-tuned**: 0.008 rad/px drag rate, 33 ms rotate-render throttle, 5° minimum elevation, grid alpha ∝ sin(tilt) capped 0.35 — same magic-number category as critiques 7/24.
41. **Moon is mean-elements-only**: no evection (±1.27°), variation (±0.66°), annual equation, or parallactic inequality — real lunar position error up to ~2° in longitude (≈14,000 km). Fine for mission schematics; not an ephemeris for real ops. Same class: planet elements are the Standish 1800–2050 approximation (arcminute-level), not DE-series.
42. **Porkchop Lambert is still 2D**: real ephemeris positions are PROJECTED to the ecliptic (z dropped) because `progLambert2D` is planar — C3 error is small for low-inclination targets (Mars 1.85°, Venus 3.39°; worst-case few % near nodes) but grows for high-i targets if ever added.
43. **Titan's plane is wrong on purpose**: modeled circular in SATURN'S ORBITAL plane; the real Titan orbits near Saturn's equatorial plane (~26.7° tilt from orbital). Positions are schematic for any Titan mission; fix when a Saturn-system mission actually matters.
44. ~~Vessels are still coplanar (z = 0) against inclined targets~~ — **RESOLVED (2026-07-10, R3)**: parking orbits, solved burns, MNODE burns and the free-return solve all live in the orbit's authored plane (§7h), the shooter gained the out-of-plane yaw DOF, and moon-leg acceptance tightened to min(SOI/3, 25,000 km) with a 5,000 km solver tolerance. Residual gaps live on as critiques 49/50: the plane is authored-inclination-with-Ω=0 (a convention, not a launch-time RAAN), and the schematic ΔV engine's plane-change accounting is unchanged.
45. **Epoch has no UI and no window-coast coupling**: epochJD defaults to 2026-07-09 and is persisted, but only editable via console; and selecting a porkchop window changes a leg's TOF, not its departure MET — reaching the real window still requires the user to author a COAST to the window's dep_day (verified: Mars leg closest approach improves 61e6 → 3e6 km when a 118 d coast is authored to the real window).

**3D physics coherence (R3, 2026-07-10)**
49. **Ω = 0 is a convention on every vessel plane — EXCEPT moon-leg departures/arrivals (solved/derived, §7h/§7i), orbits that AUTHOR their own plane (R3.2, §7j), and now orbits whose plane is AUTHORED FROM A LAUNCH SITE + TIME (R6.3, §7n) — RESOLVED for the "real missions pin RAAN at launch time" gap this critique originally raised**: an orbit spec can now carry explicit `lan_deg`/`argp_deg` (§7j direct authoring, unchanged), and when it does, `physShootLegAim`'s departure-plane solve (§7h) is SKIPPED — the authored plane is fixed geometry, not a target, and a fixed-|Δv| leg that can't reach it from that exact plane honestly fails rather than being silently re-aimed onto a different, reachable plane. R6.3 adds a physically-motivated SOURCE for that same `lan_deg` field: `progLaunchRaanFor` (360) derives Ω from a launch site's lat/lon and an authored launch time via standard spherical trig, so a user no longer has to type a bare RAAN number to get a launch-time-consistent plane — the LAUNCH card computes and writes it, annotated "(from launch time)" so it's visibly derived rather than hand-picked. Arrival targets that author a plane get a dedicated plane-alignment residual (§7j) instead of the R3 yaw DOF's generic out-of-plane miss. **The drawn-ring-vs-physics-plane split is CLOSED for flown legs (R3.1, §7i) and for authored orbits (R3.2/R6.3, §7j/§7n)**: the departure/arrival plane the corrector actually flew is recorded on the leg (`departElements`/`arrivalElements`) and consumed by the ring renderer, and an authored plane (typed or launch-time-derived) wins over both. What remains: (a) UNFLOWN, UNAUTHORED rings (no converged leg, no `lan_deg`, or physics off) still draw Ω=ω=0 by convention; (b) free-return windows are STILL solved against the conventional Ω=0 departure plane even when the active vehicle's current orbit has an authored plane — R3.2 explicitly left `physFreeReturnSolve` unchanged, so a free-return template remains representative, not predictive, of an authored plane; (c) the launch-time→RAAN derivation (R6.3) computes what plane a given launch time REACHES, and the "window in ~Xh Ym" readout shows when the site next rotates under an already-authored plane, but nothing AUTOMATICALLY schedules or enforces a launch at that computed time — no COAST event is inserted, and authoring launch time twice is last-write-wins on the same `lan_deg` field. A true launch-window solve/scheduler remains future work, now narrower in scope (the geometry is derivable; only the scheduling isn't).
50. **The ΔV accounting's plane-change model is still schematic against the real 3D geometry**: |Δv| for node maneuvers comes from `progNmComputeEdgeDv` (§6 — scalar plane-change terms, patched-conic escapes), while the R3 shooter flies the REAL 3D encounter with that fixed magnitude by adjusting direction only. When the schematic magnitude is more than the real geometry needs, the shooter spends the surplus in aim; when it's less, the leg honestly fails (converged:false, schematic arc). The accounting deliberately stays schematic — required-ΔV totals must remain byte-identical with physics on/off — so the number the user budgets and the geometry they watch are consistent in outcome, not derived from each other. Same class as critique 36 (closest approach, not arrival state, is what's targeted). **R3.2 (§7j) partially addresses the same-body case, opt-in only**: `progNmComputeEdgeDv`'s coaxial same-body transfer (`_nmCoaxialTransferDv`) previously charged NOTHING for two orbits being in different planes at all — worse than "latitude-derived," it was simply absent. A new `priceOrientation` per-node flag (default OFF, no UI yet) lets a specific edge opt into a REAL plane-change charge (`progDvPlaneChangeFull` on the orbits' authored `{i, Ω}`, vectorially combined with the existing tangential burn) — but it is still schematic in the same sense as the rest of this critique (a scalar term bolted onto vis-viva, not derived from the shooter's actual flown geometry), it only covers the same-body coaxial case (transit/escape/ascent edges remain untouched), and it is off by default specifically so this phase cannot silently reprice any existing mission — the ΔV-parity discipline this whole critique is about stays intact until a user deliberately opts a node in.
51. **Free-return coverage is window-limited by construction, and the seed scan's constants are hand-tuned**: encounter estimated at a fixed 4.7 d, apogee offsets +60k/+75k km, 24-point grid with 1/8 + 1/64 refinements and ±16k km apogee sweep (~70 propagations before the corrector runs) — all empirically chosen against the 2026-07 epoch (same magic-number class as critiques 7/24). Departure METs whose lunar phase puts the Moon far from the Ω=0 ring's plane genuinely have no solution at the fixed geometry and fail with the "try a different MET" message rather than a wrong trajectory.
52. **The maneuver gizmo's px→m/s gain (2 m/s/px, 0.2 fine) is a hand-tuned constant with no calibration against anything physical** (§7k) — it was picked so a ~100 px mouse gesture produces a ~200 m/s burn, a "feels right" number for a LEO-class transfer, not derived from screen DPI, current zoom, or the vehicle's actual ΔV budget; a user zoomed in tight for a fine plane-change edit and a user zoomed out for a big interplanetary burn get the identical px-to-m/s ratio, which may feel wrong at either extreme (the plan's "shift = fine" mitigates but doesn't eliminate this). Two further gizmo scope limits from the same phase: the ring-placement MET is "current view time," not an inverted click-angle-to-MET solve (dragging the node along the ring after placement isn't supported, only precise-entry-form editing of MET); and the scratch drag-preview only draws samples in the node's OWN body frame, silently truncating a preview that would cross an SOI boundary mid-drag (the COMMITTED leg, rendered after release via the normal `_trajPhysLegRender` path, has no such limit — only the live-drag preview does).
53. **R3.4's fidelity-ladder debounce (2000 ms) and the center-drag time-gain formula's `kmPerPx` are both hand-tuned/derived-but-uncalibrated constants** (§7k): the 2 s debounce was picked by feel (long enough continuous dragging never trips it, short enough a paused drag refines quickly) with no calibration against actual `physPropagateSegment` cost on a given machine — a slow device doing a full n-body pass that takes noticeably longer than 2 s will show a preview that appears to "hang" mid-refinement with no progress indicator; a fast device gets a fixed 2 s wait even though it could afford full fidelity continuously. Separately, the node-time drag's `dMET = pxAlong·kmPerPx/|v|` is exact GEOMETRY (screen px → km via the current zoom, km → s via the node's own orbital speed) but silently assumes the node's speed stays roughly constant over the dragged MET range — for a highly eccentric orbit dragged near periapsis, `|v|` changes fast enough within a single gesture that the px-to-time ratio measured at drag-start increasingly under/over-shoots as the drag continues (not re-sampled mid-drag). Also inherited from R3.3 and now doubled in importance: the CA pair's target selection (`_trajGizmoPickTarget`) is a simple "next MANEUVER's body, else Moon-if-Earth" heuristic with no notion of whether that maneuver is actually reachable from the node's current authored state — a node authored in a different part of the mission than intended still shows a CA pair against whatever the next-in-log destination happens to be.

**Structural**
19. `calculate()` (frozen) and `destOnOrbitDV` are twin implementations held together only by regression goldens — an intentional physics change requires editing both plus re-capturing goldens, a three-way sync that must never be done casually. If you're editing 160 or 145, stop and re-read §9 first.
20. Bodies are point masses in the underlying two-body/n-body machinery: no atmosphere beyond the fitted ascent losses, no third-body effects beyond what R4's opt-in J2 term and the restricted n-body perturbers (§7d) add, no EML/NRHO/DRO dynamics (those are cataloged as pseudo-orbits with hand-set radii). **Partially resolved by R4 (§7l, 2026-07-10)**: J2 secular precession (nodal/apsidal rates) is now COMPUTED, not merely entered as data, and SSO inclinations can be verified via `physJ2SunSyncCheck` rather than assumed — see critique 54 for what's still hand-waved about it.

54. **R4's J2 constants (`PROG_BODY_J2`, 385, §7l) are standard low-precision published figures, not independently re-derived or cross-checked against a primary ephemeris/geodesy source in this codebase** — same "unstated precision envelope" issue as critique 1's ascent constants. Consequences: (a) no error bars or "valid altitude range" attached (the closed-form secular formula itself also degrades near-singularly at very low/very high altitude and near-polar/near-equatorial geometries, which the code does not flag); (b) higher-order zonal harmonics (J3, J4, ...) are absent entirely — the model captures only the DOMINANT oblateness term, adequate for the "orbital economy" order-of-magnitude use case this phase targets (sun-sync verification, apsidal-drift awareness) but not precision mission design; (c) the optional `physAccel` J2 integrator term uses the ECLIPTIC plane as an equatorial-plane stand-in for EVERY body (documented inline and in §7l) since body obliquity/axial tilt isn't modeled anywhere in this program — for Earth the ecliptic-vs-equatorial obliquity is 23.4°, a real source of error if the integrator term is ever relied on for anything beyond "does precession happen in roughly the right sense," which is all this phase claims for it.

**Presentation layer (R6.2, 2026-07-10)**
55. **Body surface rendering (§7m) has no axial tilt and no calibrated prime-meridian epoch**: every body's spin axis is assumed coincident with the ecliptic normal (real tilts: Earth 23.4°, Mars 25.2°, others unmodeled), and Earth/Mars's sidereal spin angle carries an arbitrary zero-offset at viewT=0 rather than a real GMST/Mars-Ls calibration — so a specific longitude facing a specific screen direction at a specific mission time is coincidence, not a claim this phase makes. The Moon's tidal lock IS geometrically correct (nearside always faces Earth, driven by the same real ephemeris the rest of the program uses) since it needs no tilt or epoch calibration to get right. Jupiter/Saturn bands are procedural placeholders (no real cloud-band data). **Round-4 update (2026-07-11)**: the limb-shading gradient's day-side offset is STILL a cheap 2D screen-space dot (unchanged — good enough for "which way is roughly toward the Sun"), but it is now layered under an actual terminator (an orthographically-projected great-circle night scrim, §7m round-4) that IS a real 3D sun-vector solve — the eclipse/lighting-accuracy gap this critique originally flagged is narrowed to "no axial tilt" (the terminator itself is geometrically exact given the no-tilt approximation, not a further cheapening of it). This is a purely cosmetic layer (§7m's header note) with zero coupling to any ΔV/physics function in the program — the gap is honesty-of-picture, not honesty-of-number. **R6.3 (§7n) deliberately reuses this exact uncalibrated spin convention** for the launch-site marker, the mock ascent curve, and the launch-time→RAAN derivation — internally self-consistent (a site marker always sits where the ascent curve starts, and the derived RAAN always matches what's drawn) but NONE of it is anchored to a real UTC clock; a "launch at t=0" does not correspond to any specific real calendar moment. Calibrating the prime meridian would automatically make the RAAN math (and the marker/ascent visuals) real-time-accurate with no changes to §7n's formulas — they only consume `spinFn`'s output, not its epoch.

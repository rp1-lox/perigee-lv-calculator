# MISSION_MODEL_V2 — physics-primary mission model

**Status: AGREED v1.0 (2026-07-12) — all six decisions settled (§0). Phase 1 (shadow state, §10): DONE 2026-07-14. Phase 2 (the flip, §11): DONE 2026-07-14 — §11.5/§11.6 hold the measured acceptance. Phase 3 (reference orbits, dwell/transit, direct manipulation, §13): DONE 2026-07-14 — T1-T5 all complete, measured acceptance inline per subsection. This program is the v3.0 release train** (v2.1 was abandoned; the last released version remains v2.0.0 "Integral", and old files stay readable there per D3).

This is the architecture spec for inverting the mission model's center of gravity: from **ΔV-accounting-primary** (physics derived as a side-table) to **physics-primary** (accounting derived from a real simulated vehicle state). It is an *evolutionary re-architecture of the replay core*, not a rewrite. Prime directive: **the gate stays green at every step, and V2's derived accounting reconciles with V1's goldens within a documented tolerance (D6).**

---

## 0. Decisions (settled 2026-07-12)

| # | Question | Decision |
|---|---|---|
| **D1** | Drag an intermediate orbit — which burn absorbs? | **Upstream.** The transfer that *delivers* you to the node re-solves to hit the new orbit; downstream edges keep their own targets fixed and re-solve only because their origin state moved. Changes never propagate past adjacent edges. |
| **D2** | Reference-orbit library scope | **Global catalog** (like STAGE_LIBRARY/vehicle libraries) — Gateway-style reuse across programs. Programs may still define local one-offs; the catalog is the shared tier. |
| **D3** | Legacy missions | **Retired entirely. Breaking change, by design.** V1 `.program`/autosave mission blobs are NOT migrated — on load, a version gate detects them and says so plainly (old builds remain published for old files). This deletes the whole compatibility tax: no lazy-migration shims, no legacy `MANEUVER` replay case, no `detachedFrom` fallbacks, no parallel accounting path kept alive for old saves. |
| **D4** | Master clock | **EPOCH-PRIMARY (settled 2026-07-12).** The mission timeline runs on absolute time: a required, user-visible mission start date (JD/calendar), events authored and displayed at absolute dates, launches as events *at* dates (which naturally enables multiple launches on one timeline — the Gateway/multi-vehicle prerequisite). Window-pinned events (Grand Tour flybys, porkchop departures, rendezvous) anchor to the calendar and do NOT slip if the start date moves. T+ (from a chosen reference, e.g. first launch) remains available as a derived readout, not the authority. Implementation note: the internal simulation clock is ALREADY seconds-from-`epochJD` (what V1 calls "MET"), so the propagation/ephemeris plumbing is unchanged — this decision governs authoring + display semantics and the persistence format. |
| **D5** | Vehicle model beyond r,v | **NEVER attitude. Point-mass forever.** This is a hard non-goal, not a deferral: vehicles are state vectors + staged mass, full stop. Docking/rendezvous are modeled as position/phase coincidence, never orientation. (We are not building a KSP clone.) |
| **D6** | Reconciliation strictness | **Documented tolerance, not byte parity.** V1's stretched-ΔV accounting is acknowledged as the less accurate model; where physics-primary disagrees, physics is presumed right. Shadow-phase gate goldens compare V2-derived accounting to V1 within stated margins (default: per-burn ΔV within 1% or 5 m/s, whichever is larger; totals within 1%); any excursion beyond margin must be understood and documented before the flip. After the flip, goldens are RE-PINNED to V2's numbers and V1's are historical. |

---

## 1. The inversion (the whole idea in one paragraph)

**V1 (today):** `m.log` is a sequence of *accounting operations*. `missionRecompute(m)` replays them, building runtime vehicles and applying ΔV penalties; the real physics (`_physTrajByMission`) is computed *afterward*, as a derived side-table. Physics is a report the accounting generates.

**V2 (target):** `m.log` is authored *intent*. `missionRecompute(m)` **simulates a continuous vehicle-state timeline** — real state vectors propagated through real dynamics — and the ΔV budget / prop / margins become a *readout of that simulation*. Accounting is a report the physics generates.

Same log. Same replay skeleton. Same gate. **Flipped primacy.**

The one genuinely new primitive underneath everything: **`VehicleState(t)`** — a vehicle's real physical state at a mission time, as a first-class continuous thing that every event reads and writes.

## 2. What STAYS (non-negotiable)

- **`m.log` is the source of truth; recompute is pure replay.** Undo, autosave, determinism ride on this. Unchanged.
- **The physics engine** (385 core, 386 integrator), the shooters (`physSolveNodeBurn`, `physShootLegAim`, `physShootToTarget`), the ephemeris (360), the launch planner (415), `physPropagateSegment` as the ONE propagation entry point. Unchanged.
- **The renderer** (574), the gizmo (5745), the trajectory view, the node map's rendering layer. They already consume real physics — they get *more* correct, not rewritten.
- **The LV calculator core**: `calculate()`/`evalAtPayload()`/`lvPerformance`/`destOnOrbitDV` — frozen and orthogonal to all of this. Saturn V golden (150,838 kg) untouched.
- **`progNmComputeEdgeDv`** remains the ΔV-magnitude authority for solved Keplerian same-body edges. V2 changes what happens *around* it. (Its output participates in D6 reconciliation like everything else; the function itself is not edited.)
- **Side-table discipline:** simulation results keyed by missionId, never persisted on `m` (autosave-leak guard).

## 3. What CHANGES (the concentrated core)

- **`missionRecompute`'s replay body** — from "apply accounting deltas" to "simulate state through dynamics."
- **The event-exec functions** — each redefined as an operation on `VehicleState` (§5).
- **The state representation** — today split between accounting-state (runtime vehicles) and physics legs (`_physTrajByMission`); V2 unifies into one `VehicleState` timeline per vehicle.
- **Accounting** — from *source* to *derived readout* (§6).
- **Persistence format** — V2 mission blobs get a `modelVersion: 2` stamp; V1 blobs are refused with a clear message (D3). Event groups/repetition are re-specified against V2 replay (audited in Phase 2, kept only if they earn their keep).

**Consumers audit** (all become readers of the simulation, none rewritten): band view, state panel, flight-readiness checks (572), mission report (577), node-map annotations, trajectory view. Trade studies and the LV worksheet don't touch missions — unaffected.

## 4. The state model

### 4.1 VehicleState
```
VehicleState = {
  met,                       // mission time (s) at this anchor
  frame,                     // 'Earth' | 'Moon' | 'Sun' | ... (SOI)
  r, v,                      // km, km/s in frame
  mass: { perStage: [...] }, // dry + prop per stage (composition model unchanged)
  crew, ...                  // carried non-physics attributes
}
```
Point-mass **forever** (D5). No attitude, ever.

### 4.2 The timeline
A vehicle's history = a **piecewise trajectory**: propagated segments (coasts/transfers) punctuated by discrete state edits (burns, separations, dockings). This is what `_physTrajByMission` legs already are — V2 *promotes* them from derived side-table to primary record. `stateAt(vehicle, t)` = propagate from the nearest anchor — "a vehicle has a defined position on its orbit" falls out for free (the scrubber already does this for display; V2 makes it the model).

### 4.3 Reference orbits (first-class, global catalog — D2)
```
ReferenceOrbit = {
  id, name, body,
  kind: 'keplerian' | 'propagated',
  // keplerian:  { peri/apo (or a,e), inc, raan, argp }
  // propagated: { seedState, period, frame }   // NRHO / three-body orbits
}
```
Global catalog + per-program one-offs. Nodes **bind by id** — Orion, HLS, and Gateway all point at the same NRHO object, so "hit the same orbit multiple times" is coherent by construction. The `propagated` kind is what lets an NRHO exist at all (no Keplerian elements exist for it).

### 4.4 Nodes and edges (the intent model)
- **Node = a dwell orbit** you occupy. Binds a reference orbit + invariant **intent** `{ body, role }` (park / operate / capture / rendezvous). Geometry is the editable current solution; intent is sticky under parametric manipulation. Structural edits (change destination, delete node) are deliberate re-plans and may ripple; parametric edits never do.
- **Edge = a transfer** between dwell orbits — internally possibly multi-burn (departure + midcourse + insertion). **TLI is the departure burn on the LEO→(lunar destination) edge, not a node.** Rule: *dwell = node, transit = edge.* (Terminology pass rides along: TLI/TMI/LOI/MOI etc. name burns; nodes are named as orbits.)
- **Solved vs manual** (R6.2′ unification, kept): edges default `solved` and re-solve **locally** on endpoint manipulation per D1 (upstream edge delivers the change; adjacent edges only). Detaching a burn pins it `manual` — the user's explicit "stop re-solving this." That's what keeps free manipulation non-brittle.

### 4.5 Time model (D4 — epoch-primary)
The timeline is **absolute**: a required mission start date (`epochJD`, user-visible and user-set), events authored/stored at absolute time, displayed as calendar dates. Rationale: window-pinned missions (Voyager-style Grand Tours, porkchop departures, rendezvous) are calendar phenomena — an event tied to a window must NOT slip when the start date moves. Launches are events *at* dates, so several vehicles can launch at different dates on one timeline (multi-vehicle prerequisite). T+ from a chosen reference (e.g. first launch) is a derived display, not the authority. Internally the simulation clock stays seconds-from-`epochJD` (identical to V1's "MET" plumbing) — propagation, ephemeris, and the scrubber are unchanged; the decision governs authoring, display, and persistence semantics. One time axis, one conversion seam (JD ↔ sim-seconds).

## 5. Event redefinition — operations on VehicleState

Every event is a pure operation `(stateIn, params) → stateOut` (+ its delta, for the accounting readout). Two families:

### 5.1 Physics-state events (read/write r,v — these get *simpler*)
| Event | V2 definition |
|---|---|
| **LAUNCH** | Instantiate a vehicle at a physical state: bound parking reference orbit at launch date/time, plane from launch geometry (R7 planner). |
| **DEPLOY** | Instantiate a spacecraft at a state on a bound reference orbit. |
| **COAST** | `stateOut = physPropagateSegment(stateIn, dt)`. That is the entire definition. |
| **BURN / VECTOR BURN (MNODE)** | `v += Δv(pro,rad,nrm)` at met; propellant via rocket-eq on delivered Δv (one accounting path). |
| **MANEUVER (solved edge)** | Solve the transfer between two dwell states → its burn(s); Keplerian same-body magnitudes via `progNmComputeEdgeDv`, targeted/n-body via the shooters. Re-solves locally per D1. |
| **REENTER / RECOVER** | Terminal state operations (atmosphere interface / surface). |

### 5.2 Vehicle-composition events (mass/structure — nearly unchanged)
**SEPARATE, DOCK, EXPEND, PROPELLANT TRANSFER, CREW TRANSFER** happen *at* a state but are trajectory-neutral: they edit composition/mass and pass r,v through. DOCK's precondition under V2 is state coincidence (same orbit, same phase within tolerance) — a *check*, not new physics (D5: never attitude).

### 5.3 RENDEZVOUS (the one that deepens)
Rendezvous = arrive at a shared reference orbit **at the target vehicle's phase**. Orbit-reaching is a §5.1 op; **phase-matching is a new solver, explicitly deferred** to the post-NRHO phases (§7). Until then, rendezvous remains a coincidence check the user satisfies by authoring.

## 6. Accounting becomes a readout

`missionBudget` (ΔV expended, prop consumed, margins), the state panel, readiness checks, and the report are **summed/derived from the simulation's burns and masses**. Shadow-phase goldens assert reconciliation with V1 within D6 margins (per-burn 1% or 5 m/s; totals 1%); after the flip, goldens re-pin to V2 as the reference and V1's numbers become history. Where V1 and V2 disagree beyond margin, the discrepancy is investigated and documented in MATH.md before flipping — physics presumed right, but *understood* first.

## 7. Migration strategy — gate-green every step

- **Phase 0 — this doc agreed.** (5/6 decided; D4 pending confirmation.)
- **Phase 1 — shadow state.** Recompute builds the `VehicleState` timeline *alongside* the V1 model; nothing user-visible changes. Gate goldens compare shadow-derived accounting to V1 within D6 margins. De-risks the inversion before it bears load.
- **Phase 2 — the flip.** Physics-state events read/write `VehicleState` as primary; accounting derived; V1 accounting path and legacy compatibility **deleted** (D3: version-gate old blobs; also delete the legacy `MANEUVER` shim and `detachedFrom` fallbacks). Goldens re-pinned to V2. Event groups/repetition re-validated or retired.
- **Phase 3 — reference-orbit catalog + node/edge semantics.** Global catalog, bind-by-id, dwell/transit model, TLI-as-burn terminology, upstream-absorb manipulation (D1). Substrate for the node↔trajectory direct-manipulation feature (sliders/handles on a selected node's orbit).
- **Phase 4 — NRHO.** `propagated` reference-orbit kind + the seeded canonical Gateway NRHO (9:2, ~6.5-day period), propagated by the integrator, rendered as a real three-body orbit. The power move; also proves the `propagated` kind end-to-end.
- **Phase 5+ — reach-it (n-body transfer/BLT to NRHO), then rendezvous phasing & multi-vehicle timing.** The genuinely hard tail; scoped separately when we get there.

Each phase ships alone: gate green, D6 reconciliation (through Phase 2), browser-verified.

## 8. Invariants

- **`m.log` source of truth; recompute pure replay** (edit log → recompute → render; exec fns never mutate in place). Undo/autosave/determinism preserved.
- **One state source** (`VehicleState`), **one propagation entry** (`physPropagateSegment`), **one Keplerian solved-edge ΔV authority** (`progNmComputeEdgeDv`).
- Simulation results in missionId-keyed side-tables, never on `m`.
- **Point-mass forever** (D5).
- LV calculator core frozen; Saturn V 150,838 kg immortal.
- D6 tolerance discipline through the flip; goldens re-pinned after.

## 9. Open items

None — all six decisions settled (§0). **Status: AGREED v1.0 (2026-07-12).**

---

## 10. PHASE 1 SPEC — shadow state (the first code)

**Goal:** `missionRecompute` builds a complete `VehicleState` timeline for every vehicle *alongside* the V1 model, with **zero user-visible change**, and a reconciliation harness proves the shadow's derived accounting matches V1 within D6 margins. Nothing reads the shadow yet except the reconciliation tooling. This is pure de-risking: when Phase 2 flips primacy, the thing it flips TO will already have been proven equivalent.

### 10.1 The key architectural move: PROMOTE, don't recompute

`_physTrajByMission` legs already contain real propagated segments with samples, burn states (`leg.burnState`), solved Δv vectors (`leg.dvVec`), frames, and SOI events. The V1 replay already produces per-event vehicle snapshots and mass/prop states. **Phase 1 stitches these existing artifacts into per-vehicle timelines; it does not re-propagate.** New propagation happens only where no leg exists (gap coasts between anchored events — and even those defer to `stateAt()`-on-demand rather than eager propagation). Consequence: the shadow build is cheap (target: recompute stays under ~2.5 ms on the Apollo seed vs. the 2.05 ms baseline in PERF_BASELINE.md) and correct by construction wherever a leg exists.

### 10.2 New module: `src/js/566-mission-state-v2.js`

(Loads after 565. All cross-module calls at runtime, never module-eval.)

Data (side-table, missionId-keyed, NEVER on `m` — invariant §8):
```
_v2StateByMission = {
  [missionId]: {
    vehicles: {
      [ownerKey]: {                    // same owner keys tagOwners already assigns
        anchors: [ V2Anchor... ],      // time-ordered
      }
    },
    builtJD,                           // epochJD at build (staleness guard)
  }
}

V2Anchor = {
  t,                    // sim-seconds from epochJD (V1 "MET" — same clock, per D4 note)
  frame, r, v,          // physical state (r,v may be null for surface/pre-launch anchors — explicit kind instead)
  kind,                 // 'launch' | 'deploy' | 'burn' | 'composition' | 'coast-start' | 'terminal'
  authIdx,              // the m.log entry that created this anchor (provenance)
  mass: { perStage },   // composition snapshot at this anchor
  dvApplied,            // [x,y,z] km/s for kind:'burn' (the delta this anchor applied), else null
  legRef,               // authIdx of the _physTrajByMission leg covering [this anchor, next), or null
}
```

API (all pure over the side-table + existing artifacts):
- `v2BuildShadow(m)` — walks `m.log` post-replay (called from the recompute tail, AFTER `physRebuildMissionTrajectories`, guarded `typeof`-safe), builds the timelines. Idempotent; full rebuild each recompute (same discipline as the legs).
- `v2StateAt(missionId, ownerKey, t)` — state at any time: find bracketing anchor; if `legRef`, interpolate/propagate within the leg's samples (reuse `physLegStateAt`'s machinery); else Kepler-propagate from the anchor via `physPropagateSegment` with `maxSamples` tiny. Returns `{frame, r, v, mass}` or null (pre-launch/post-terminal).
- `v2DeriveBudget(missionId)` — the accounting readout: per-burn Δv = |dvApplied| (km/s→m/s), prop per burn re-derived by the SAME rocket-eq path V1 uses (`progRocketEqPropNeeded` on the anchor's mass snapshot), totals summed. Returns `{ perBurn: [{authIdx, dv_ms, prop_kg}], dvTotal, propTotal }`.
- `v2Reconcile(missionId)` — the proof tool: pairs each V2 burn with its V1 log entry (`dvRequired`/`dv_actual`, `prop_consumed`) and `missionBudget(m)`, returns `{ rows: [{authIdx, v1_dv, v2_dv, delta, withinMargin}], totals: {...}, allWithin: bool }` using D6 margins (per-burn max(1%, 5 m/s); totals 1%).

### 10.3 Per-event shadow semantics (Phase 1 subset — V1 meanings, recorded not redefined)

| Event | Shadow action |
|---|---|
| LAUNCH / DEPLOY | Anchor `kind:'launch'/'deploy'` at the event's time: state from the parking-orbit spec via the SAME reconstruction the leg builders use (elements→state at the orbit's phase convention; reuse, don't fork). Mass = V1's post-event vehicle snapshot. |
| MNODE (solved) | Anchor `kind:'burn'`: pre-state from `leg.burnState`, `dvApplied = leg.dvVec`, `legRef` = its leg. |
| MNODE (manual) | Same, from the manual leg (burnState stamp when present, else the mean-motion reconstruction the leg builder used — read what the leg RECORDED, never re-derive differently). |
| Legacy MANEUVER (shim) | Treated identically to solved MNODE via `_evIsSolvedManeuver` (predicates already unify them). |
| COAST | No anchor needed when a leg covers it; a `coast-start` anchor only when it begins an un-legged gap. |
| SEPARATE / DOCK / EXPEND / TRANSFER (prop/crew) | Anchor `kind:'composition'`: r,v carried through from `v2StateAt` at that time; mass/composition delta from V1's replay result. New owner keys inherit their state from the parent's anchor at separation time. |
| REENTER / RECOVER | Anchor `kind:'terminal'`; timeline ends. |
| Groups/repetition | Phase 1 shadows the EXPANDED log exactly as replay sees it (clones included) — no special handling. |

**Rule of the phase:** where V1 and the legs disagree or an event can't be shadowed faithfully, record the anchor with a `note` and surface it in `v2Reconcile` — never silently skip, never "fix" V1's meaning in Phase 1. Discrepancies are FINDINGS for Phase 2, not bugs to patch here.

### 10.4 Implementation steps (each gate-green before the next)

1. **S1 — skeleton:** module, data shapes, `v2BuildShadow` no-op hook in the recompute tail (after `physRebuildMissionTrajectories`, `typeof`-guarded), `v2StateAt` for a single-anchor timeline. Gate: pure-shape tests.
2. **S2 — launch/deploy anchors + leg stitching:** timelines for launched vehicles with `legRef` coverage; `v2StateAt` inside legs. Gate: synthetic one-launch mission in the vm harness (570 loads there already) — state at t matches the orbit spec's expected radius.
3. **S3 — burns:** solved + manual MNODE anchors with `dvApplied`; per-burn Δv derivation. Gate: synthetic log with one solved + one manual burn — v2 per-burn Δv equals the authored/solved values within D6.
4. **S4 — composition events + owner forking:** separation forks timelines with inherited state; mass snapshots flow. Gate: synthetic separate → two owners, mass conservation asserted.
5. **S5 — reconciliation harness:** `v2DeriveBudget` + `v2Reconcile` + gate goldens on a synthetic 5-event mission (launch, coast, solved burn, manual burn, separate) asserting `allWithin === true`. Browser: run `v2Reconcile` on the full Apollo seed (`devSeedApolloMission({force:true})` + a manual gizmo burn) and RECORD the actual per-burn deltas in §10.6.
6. **S6 — docs:** MATH.md §8-adjacent "V2 shadow state" section (data model + reconciliation results); this doc's §10.6 filled with measured numbers; PHYSICS_PLAN cross-reference note.

### 10.5 Explicitly OUT of scope for Phase 1

No UI change of any kind. No persistence change (the side-table is transient; autosave blobs are byte-identical). No event redefinition (V1 meanings are *recorded*, not changed). No reference-orbit catalog, no epoch-display work, no node/edge rework — those are Phases 2–3. No performance optimization beyond the §10.1 promote-don't-recompute discipline.

### 10.6 Acceptance (measured 2026-07-14)

- **Gate: green.** 555 → **575 assertions** (+20 Phase-1 additions: pure-shape tests, a synthetic launch-anchor/state-reconstruction test, synthetic solved+manual MNODE dv-reconciliation tests, D6 margin-math tests, and a synthetic SEPARATE owner-forking test).
- **Apollo seed reconciliation** (`devSeedApolloMission({force:true})` + one manual gizmo burn, `_trajGizmoOpenPending(missionId,1800); _trajGizmo.dv.pro=500; _trajGizmoCommit()`):
  - Per-burn: **2 of 3 rows within margin** — TLI (authIdx 1): v1 3143 m/s vs v2 3143 m/s, Δ0, within. Manual burn (authIdx 3): v1 500 m/s vs v2 ≈500.0 m/s (Δ ≈ 5.7e-14, float noise), within. LOI/corridor-exit edge (authIdx 2): v1 638 m/s vs v2 **unaccounted** (no promotable leg in Phase 1 — 565 attributes zero propagation to a transit corridor's exiting edge; flagged `withinMargin:false` per the §10.3 "never silently skip" rule, not dropped). See MATH.md §7q critique 58.
  - Totals: v1 (`missionBudget().dvExpended`) = 13,510 m/s; v2 (`v2DeriveBudget().dvTotal`, burns only) = 3,643 m/s; **does not close** — two documented, understood gaps, not stitching bugs: (a) the LOI edge above (638 m/s unaccounted), and (b) LAUNCH's ascent Δv (≈9,229 m/s) is its own anchor `kind` in the V2 model and is never emitted as a `v2DeriveBudget` "burn" row at all, so it's absent from the v2 total while `missionBudget()`'s v1 total includes it (critique 60). `9229 + 638 = 9867 ≈ 13510 − 3643`, confirming these two gaps fully account for the shortfall — no unexplained residual.
  - `allWithin: false` overall, honestly, for the two reasons above (both cataloged in MATH.md §7q/critiques 58 & 60 as Phase-2 open items, not Phase-1 defects). A dedicated dv-double-counting bug (every owner key in a docked stack independently anchoring the same burn, inflating early measurements to 5×) was found and fixed during this same verification pass — see MATH.md §7q.
- **Recompute wall-time**, Apollo seed, 20-iteration average after 5-iteration warmup: **≈4.2–4.5 ms**, both with and without `v2BuildShadow` active (isolated A/B: v2's own contribution measured at ≈0 ms / within noise, even slightly negative). This EXCEEDS the ~2.5 ms target and the 2.05 ms `PERF_BASELINE.md` figure — but the excess is entirely pre-existing (565's physics-trajectory rebuild), unrelated to this module; Phase 1 adds no measurable propagation cost of its own, consistent with the promote-don't-recompute design (§10.1).
- **Autosave-blob neutrality: verified.** `JSON.stringify(_buildSessionObject())` byte-identical with the shadow build on vs. off, once the two pre-existing non-deterministic ISO timestamp fields (`savedAt`, top-level and nested under `program`) are excluded — those timestamps differ between ANY two calls regardless of this module and are not new. No `_v2`/`V2Anchor`/`v2BuildShadow` substring appears anywhere in the serialized blob (grepped directly), confirming the side-table is never persisted on `m` or reachable from `_buildSessionObject()`.

---

## 11. PHASE 2 SPEC — the flip (drafted 2026-07-14)

**Goal:** `VehicleState` becomes the PRIMARY record. Every number the user sees about a mission — ΔV required/delivered, prop consumed, margins, per-vehicle state — is **derived from the simulation timeline**, not from parallel bookkeeping. The V1 accounting computation and all legacy compatibility are **deleted** (D3). Goldens re-pin to V2 (D6). Phase 1's three findings are resolved as part of the flip, not bolted on after.

### 11.0 The blast-radius trick that makes this tractable

Consumers (band view, state panel, 572 checks, 577 report, node-map labels, trajectory markers) read mission numbers from two places: **stamped fields on log entries** (`e.dvRequired`, `e.dv_actual`, `e.stagingResult`, `e.prop_consumed`) and **`missionBudget(m)`**. Phase 2 keeps those exact read surfaces but changes their SOURCE: recompute stamps them **from the V2 timeline**. Consumers don't change at all in this phase — the flip happens beneath them. (Consumer-visible redesign is Phase 3+ territory.)

### 11.1 Resolve the Phase-1 findings (these are the real model work)

- **F1 — Arrival/corridor-exit burns become real (critique 58).** V1 treats a transfer's arrival burn (LOI/MOI/capture-circularize) as pure bookkeeping on the edge — zero propagated existence, which is why Phase 1 couldn't shadow it. Phase 2 makes arrival a first-class burn anchor: at the corridor's arrival state (the leg's final sample / SOI-frame state), the burn Δv VECTOR = (target dwell-orbit velocity at the insertion point) − (arrival corridor velocity), applied to the state; magnitude derived from that vector. **Expectation, stated up front:** the physics-derived arrival Δv will NOT generally equal V1's schematic 638/822-style numbers — D6 governs: investigate, document in MATH.md, re-pin. This is the first place "physics is presumed right" does real work.
- **F2 — Ascent ΔV representation (critique 60).** Launch ascent is sub-orbital abstraction (the LV calculator's domain, not the mission simulation's — the state timeline BEGINS at parking-orbit insertion). Ascent ΔV therefore stays an accounting attribute OF the launch anchor (`anchor.ascentDv`, sourced from the staging result exactly as today), and `v2DeriveBudget` gains an `ascent` line so mission totals close: `dvTotal = ascent + Σ burns`. It is never a state delta and never a fake "burn row."
- **F3 — Deterministic replay identity (blob churn).** Runtime vehicle ids become deterministic functions of (missionId, log position, repetition index) instead of fresh `progUUID()` per replay — same log ⇒ byte-identical blob. This is required anyway for "recompute is pure replay" to be literally true, and it stops every recompute dirtying autosave. Repetition-clone keys join the same scheme (groups/repetition machinery otherwise unchanged — it clones log entries pre-replay, orthogonal to the state model; re-validated by a gate case).

### 11.2 The flip itself

1. **Replay produces the timeline as its primary output.** The V1 exec functions' composition/mass machinery (stage states, prop burn via `progRocketEqPropNeeded`, tagOwners keys) is KEPT — it becomes the **mass track** of the timeline (it already computes exactly what `anchor.mass` needs). What gets deleted is the parallel ΔV/margin bookkeeping that duplicates what the timeline knows.
2. **Stamp-from-V2:** after the timeline builds, recompute writes the legacy-named fields (`dvRequired`, `dv_actual`, `stagingResult`, `prop_consumed`) from `v2DeriveBudget`/anchor data. Solved-edge required-Δv still comes from `progNmComputeEdgeDv` (unchanged authority, §2) — what flips is that *delivered/actual/prop/margins* come from the simulated state, and required-vs-delivered comparisons are simulation-vs-target rather than bookkeeping-vs-bookkeeping.
3. **`missionBudget(m)` delegates to `v2DeriveBudget`** (plus the F2 ascent line). The old summation path is deleted. (This also retires the pre-existing aggregate-undercount bug flagged 2026-07-11 — the +3-for-200 m/s manual-burn anomaly — which dies with the path that produced it.)
4. **Delete, per D3:** the legacy `MANEUVER` replay case, `_missionMigrateManeuverEntry`, all `detachedFrom` fallback reads, and every "old save" tolerance in mission load paths. `buildProgramObject`/`_buildSessionObject` stamp `modelVersion: 2`; `applyProgramObject`/`_applySessionObject` REFUSE mission blobs without it: clear message naming the v2.0.0 release for old files. (Worksheet/vehicle/stage/orbit/theme persistence is untouched — the gate applies to MISSIONS only.)
5. **Re-pin goldens (D6):** gate mission goldens move to V2-derived values. Solved-edge magnitudes (3143) should hold exactly (same authority); arrival burns (F1) and totals WILL move — each moved number gets a MATH.md entry stating old, new, and why physics disagrees with the old model. `devSeedApolloMission` continues to be the canonical case.

### 11.3 Steps (each gate-green; browser-verified where marked)

1. **S1 — F3 determinism:** deterministic ids + repetition keys; gate: two recomputes ⇒ identical blob (savedAt excluded); browser: autosave no longer dirtied by recompute alone.
2. **S2 — F1 arrival burns:** 565's transfer legs produce arrival anchors with real Δv vectors; `v2Reconcile` reports the physics-vs-V1 delta for LOI; investigate + document the delta (browser, numbers recorded in §11.5).
3. **S3 — F2 ascent line:** `v2DeriveBudget` totals close against a hand-summed expectation on the seed.
4. **S4 — stamp-from-V2 + budget delegation:** consumers now read V2-derived numbers through unchanged surfaces; browser: state panel / checks / report / node map / band all render sane values on the seed + a detached-burn + a mid-leg-burn mission; goldens re-pinned in the same commit.
5. **S5 — deletions (D3):** legacy paths removed; version gate on load with the refusal message; gate case: a synthetic V1 blob is refused, a V2 blob round-trips.
6. **S6 — docs:** MATH.md accounting sections rewritten as "derived from the state timeline" (old text moved to an appendix or deleted); critiques 58/60 closed; §11.5 filled; PHYSICS_PLAN cross-note.

### 11.4 Explicitly OUT of scope for Phase 2

Reference-orbit catalog, node/edge dwell-transit rework, epoch-primary *authoring UI* (D4's display work — the internal clock is already epoch-seconds), NRHO, phase-matching, any consumer-visible redesign. Phase 2 ends with the app LOOKING the same, computing from the simulation, and carrying zero legacy weight.

### 11.5 Acceptance — S1–S3 measured (2026-07-14; S4–S6 not yet done)

**Scope note:** this pass implements Phase 2 **S1–S3 only** (F3 determinism, F1 arrival burns, F2 ascent line). S4 (stamp-from-V2 + budget delegation), S5 (deletions/version gate), S6 (docs rewrite) are NOT done — `missionBudget()`/consumers still read the V1 accounting path; V2 remains a side-table the reconciliation tooling reads, same posture as Phase 1. Gate stayed at **575 assertions** (no new gate cases added this pass — S1–S3 were verified live in the browser per the task's instructions; a synthetic gate case is S4/S5 follow-up work).

- **S1 — determinism (F3), verified in-browser:** `_missionRekeyVehicleId` (570-mission-manager.js) derives runtime `vehicleId` from the already-stable `_originKey` (`'v_' + missionId + '_' + originKey`, sanitized) instead of a fresh `progUUID()` per replay, rekeying `PROG_ACTIVE_PROGRAM.vehicles` at all four creation points (LAUNCH, DEPLOY, SEPARATE lower/upper, DOCK merge). Two `missionRecompute(m)` calls on the seeded Apollo mission (`devSeedApolloMission({force:true})`) produced **byte-identical `m.vehicleIds` and `JSON.stringify(m.log)`**: `vehicleIds = ["v_pq5ttxh71cmu_launch:0"]` both times, `logSame: true`.
- **S2 — arrival burns (F1, critique 58 resolved):** 565's `physRebuildMissionTrajectories` 'arrival' leg branch (the exiting leg of a transit corridor) now constructs a real `leg.arrivalBurn = {t, frame, r, vPre, dvVec}` from `physArrivalStateAt` (new helper, factored out of `physArrivalOsculatingElements` so both share one bracket/finite-difference reconstruction) + a target-orbit velocity built from vis-viva at the arrival radius with direction `ĥ_target × r̂_arr` (Ω=0-convention target-plane normal). 566's `v2BuildShadow` promotes it to a `kind:'burn'` anchor exactly like a solved MNODE.
  - **Measured on the seed** (Apollo LAUNCH + TLI + LOI + one manual 500 m/s gizmo burn at t=1800): LOI edge (authIdx 2) — **V1 638 m/s vs V2 (physics) 1028.29 m/s, Δ = +390.29 m/s (+61%)**. Outside the D6 margin (not within 1%/5 m/s) — expected per §11.1's stated expectation. Not implausible (1.61× V1, well inside the "investigate if >2x or <0.3x" bound) — accepted as a genuine model disagreement, not a construction bug.
  - **Cause of the discrepancy (documented per D6):** V1's `progNmComputeEdgeDv` computes the LOI burn as a schematic same-plane Hohmann-arrival circularization at the corridor's nominal arrival radius. V2's arrival burn is built from the ACTUAL propagated corridor state (`physArrivalStateAt`'s finite-difference reconstruction of the injection leg's real trajectory, which includes the true lunar-approach geometry, plane offset from the 3-DOF shooter's yaw solve, and the corridor's actual arrival speed/angle) matched against the target LLO's vis-viva velocity in the AUTHORED target plane. The bulk of the +390 m/s gap is the plane-alignment/geometry cost that the schematic Hohmann-LOI number does not carry: the shooter's converged corridor arrives with a real out-of-plane and timing offset from the idealized coplanar transfer V1 assumes, so matching the exact authored LLO plane costs more Δv than V1's 2D approximation. This matches MATH.md's existing critique-58 framing (V1's LOI/MOI accounting is schematic, not flown) — see MATH.md's new entry for the full writeup.
- **S3 — ascent line (F2, critique 60 resolved):** `v2DeriveBudget` now sums a dedicated `ascent` field from launch anchors' `ascentDv` (sourced from `e.stagingResult.dvDelivered`, the same field V1 stamps — read, not recomputed), and `dvTotal = ascent + Σ|burns|`.
  - **Measured:** `ascent = 9229`, `burns = 3143 (TLI) + 1028.29 (LOI) + 500 (manual) = 4671.29`, `dvTotal = 13900.29`. **Totals close exactly**: `9229 + 4671.29 = 13900.29 = v2DeriveBudget().dvTotal`. Compared to V1 (`missionBudget().dvExpended = 13510`): the total delta (`13900.29 − 13510 = 390.29`) equals EXACTLY the S2 LOI delta (390.29 m/s) — the ascent and TLI/manual-burn numbers agree with V1 to float noise, so the only source of total disagreement is the one documented, understood arrival-burn gap from S2. No unexplained residual.
- **Regression (confirmed, S1–S3 must not and did not touch V1-stamped numbers):** `missionBudget(m).dvExpended` on the untouched seed is still **13510** (same as the Phase 1 measurement) after all S1–S3 changes and two recomputes. `page-program` trajectory rendering (band + orbit-map view via `missionSetBandView`/`missionRenderDetail`) still renders without console errors after the changes; `_trajApplyCam` present and callable.
- V1-blob refusal / version gate: **not yet implemented — S5, out of this pass's scope.**
- Recompute wall-time: not separately re-measured this pass (no propagation-cost-relevant change — S2 adds one `physArrivalStateAt` call + a handful of vector ops per corridor-exit edge, reusing samples/events the rebuild already computed; expected within Phase-1 noise, not re-profiled).

### 11.6 Acceptance — S4–S6 measured (2026-07-14; the flip is now complete)

- **S4 (stamp-from-V2 + budget delegation):** implemented in `missionRecompute`'s tail (570-mission-manager.js) — immediately after `v2BuildShadow`, a stamping loop overwrites `e.dv_actual`/`e.dvDelivered`/`e.dv`/`e.prop_consumed` on every BURN/MNODE expanded entry from `v2DeriveBudget`'s matching per-burn row; `e.dvRequired`/`e.dvTarget` stay sourced from `progNmComputeEdgeDv` (unchanged, §2). `missionBudget(m)` now delegates to `v2DeriveBudget(m.missionId)` (+ ascent + ascent-stage prop off the LAUNCH `stagingResult`, unchanged source) — the old per-entry summation is deleted, with a defensive fallback retained for the (rare) case `missionBudget` is called before any recompute has built the v2 side-table.
  - **Measured on the seed** (Apollo LAUNCH + TLI + LOI + one manual 500 m/s gizmo burn at t=1800): `missionBudget(m)` now returns `dvExpended: 13,900` (was 13,510 under V1's schematic LOI) and the LOI entry (`m._expanded[2]`) shows `dvRequired: 822, dv_actual: 1028, result: 'SUCCESS'` — the arrival-burn flip (§11.1 F1/S2) turned what V1 flagged `MARGINAL` (delivered < 822-ish) into an honest over-delivery once `dv_actual` reflects the real 1028.29 m/s arrival burn; the stamping loop re-derives `e.result` against the stamped value for exactly this reason (documented in MATH.md's Phase 2 S4 entry).
  - **Undercount anomaly confirmed dead:** adding a manual 200 m/s burn to the seed moved `missionBudget().dvExpended` from 13,900 to **exactly 14,100** (+200, not +203) — `v2Reconcile` on the seed (three burns, post-stamp) reports `allWithin: true` for all three rows and totals, which is now definitional (V1's `dv_actual` fields ARE the V2 numbers post-stamp) rather than a coincidence, confirming the old parallel-bookkeeping path that produced the anomaly is gone.
- **S5 (deletions, D3):** removed — the `type:'MANEUVER'` branches of `_evIsSolvedManeuver`/`_evManeuverTarget` (570-mission-event-model.js), the `_missionMigrateManeuverEntry` function and its four call sites (570-mission-manager.js ×2, 5745-maneuver-gizmo.js ×2), and every `detachedFrom` fallback read (card/report text in 570, the manual-burn info note in 572-mission-checks.js, the gizmo's re-solve-button visibility check in 5745). `_evIsManualBurn` was already MNODE-only and is unchanged. New missions are stamped `modelVersion: 2` at creation (`_missionMake`, 570-mission-lifecycle.js §S5). A new pure predicate `_missionsPassV2Gate(missions)` (450-program-module-phase-10-save-load-closur.js) gates `applyProgramObject`: any mission lacking/mismatching `modelVersion:2` refuses the WHOLE program load via `showAlert`, naming the retired v2.0.0 build, mutating nothing. `_applySessionObject`/autosave restore inherit the gate transitively (they call `applyProgramObject`); the restore banner's Discard button is untouched (`autosaveClear()` only), so a gated-out autosave blob can always still be discarded.
  - **Measured (browser):** `applyProgramObject({kind:'rocket-playground-program', formatVersion:1, missions:[{missionId:'x',log:[]}], activeProgram:{}, ...})` (no `modelVersion` on the mission — the V1 shape) left `_missions.length`/`PROG_ACTIVE_PROGRAM.name` byte-unchanged and raised a modal titled "OLDER MISSION FORMAT" with the expected message. `_buildSessionObject()` JSON before vs. after an extra `missionRecompute()` call is byte-identical once `savedAt` is stripped (S1's determinism holds under S4/S5). `missionUndo(m.missionId)` correctly reverted a `durationOverride` edit's effect on `e.durationUsed` in the same test session — undo unaffected by the flip.
- **S6 (docs):** MATH.md §7q gained the S4–S6 entry (stamp-from-V2 mechanics, the `e.result` re-derivation fix, the version gate, and a short historical note on what the deleted V1 `missionBudget` summation used to do); critique 58 and 60 marked fully CLOSED; critique 59 (prop-per-burn stage-selection heuristic) remains explicitly OPEN, with a newly observed minor symptom (a −67 kg prop-total drift when a third manual burn is appended) attributed to it and left for Phase 3+.
- **Gate:** 555 (Phase 0 baseline) → 575 (Phase 1) → **570 assertions** after S4–S6 (net −5: the legacy-MANEUVER/`_missionMigrateManeuverEntry` shim's ~19 predicate/migration assertions were deleted along with the code they tested and replaced with 9 simplified MNODE-only predicate assertions, while 10 new assertions were added — 5 for `missionBudget`'s v2-delegation and 5 for `_missionsPassV2Gate`'s version-gate behavior). All green, `python build.py` passing end to end.
- **Not yet done:** reference-orbit catalog, node/edge dwell-transit rework, epoch-primary authoring UI, NRHO, phase-matching, any consumer-visible redesign — all explicitly Phase 3+ per §11.4. Critique 59 (prop stage-selection heuristic) is the one open Phase 2 item carried forward.

---

## 12. v3.0 UI SHELL — agreed direction (2026-07-14)

Mockups: claude.ai/code artifact "Mission View Unification — Concepts" (v2-swappable-primacy). **Concept A accepted with the A′ amendment.** The Band | Orbit Map | Trajectory toggle is retired in favor of a fixed layout grammar:

- **One STAGE + two RAILS, any surface promotable.** The stage is the large working surface; the Plan rail (the node/edge skeleton, compressed vertical — was the Orbit Map) and the Timeline dock (Band lanes merged with the scrubber onto one time axis) flank it. Every rail carries a promote control (⤢); promoting swaps it with the stage, the demoted surface docks in its place, and NOTHING unmounts — the world keeps rendering live as a thumbnail when demoted, so camera/selection/time context survives every swap.
- **State inspector**: the "Vehicles & Mission State" panel becomes a dockable, event-aware card pinned to a corner of whatever surface is the stage (same content, compressed; "— at EVENT / — current" behavior preserved).
- **One time cursor, one selection, one camera intent** across all surfaces (extends R5's shared selection).
- Workflow presets fall out of promotion, not modes: world-stage = flying/authoring; timeline-stage = history audit & multi-vehicle choreography; plan-stage = early sketching.
- **Sequencing** (rides Phases 2–3): Timeline dock first (Band+scrubber merge, self-contained); Plan rail with Phase 3 (it is the natural home for reference-orbit nodes and the direct-manipulation handles); promotion mechanics once both rails exist; toggle retired last. Concept B (semantic zoom morph) remains a possible later enhancement on top — never a prerequisite.

*After Phase 2: Phase 3 (reference-orbit catalog + dwell/transit node-edge model + direct manipulation) builds on a mission model that is finally, actually, the physics — and lands inside the §12 shell.*

**Timeline dock: DONE (2026-07-14).** Built in `src/js/5747-timeline-dock.js`, wired in at `_missionTrajViewHTML` (`src/js/574-trajectory-view.js`, the `${(typeof _ttdDockHTML === 'function') ? _ttdDockHTML(m, id) : ...}` call site replacing the old bare `_trajScrubberHTML` call). Reused rather than reinvented: `_missionBandModel(m)` (570) for owner/lane extraction and per-event zone classification, `_missionBodyGroupOf`/`_MISSION_BODY_ZONE_COLOR` (570) for segment fill, `lane.color` (570's `_missionLaneColor`, user-overridable via `m.laneColors`) for the lane border/identity accent, and the UNCHANGED scrub track (`_trajScrubberHTML`/`_trajScrubDown/Move/Key`, `_trajViewTime`/`_trajSetViewTime`) nested unmodified inside the new `.traj-timeline-dock` wrapper — the `.traj-scrubber` outerHTML-sync in `_trajApplyCam` still finds and refreshes it by selector, so drag/click/arrow-key/focus-preservation all still work. `_ttdLaneModel` reprojects each owner's Band points (column-index x) onto literal MET segments `[0, maxMet]` — the SAME axis `_trajScrubberHTML` uses for tick/thumb %-left — so a segment boundary and a scrub tick for the same event always land at identical `%`.

Evidence: seeded `devSeedGatewayMission({force:true})` (2 vehicles → 3 owners after DEPLOY: LV + Service Module + Command Module). `.traj-timeline-dock` renders with 3 lane rows + a "+1 more" collapsed row; `.ttd-seg` count (15) matches the owner/point extraction. Px-alignment check: `.traj-scrub-tick` lefts `["0%","0.26%","0.26%","0.26%"]` vs `.ttd-seg` lefts for the same events `["0%","0.26%",...]` — identical. Scrub-click via synthetic mousedown on `.traj-scrub-track` moved view time (`406944s → 203164s`) and `_trajViewTime` picked it up. Clicking a `.ttd-seg` selected event index 0 (`m.log[0]._expanded === true`) via `_trajSelectEventFromView`, same path every other trajectory-view hotspot uses. Expand toggle (`.ttd-expand-btn`) flips `.ttd-lanes.expanded` and row height (7px→14px inner / 11px→18px outer); `.traj-svg` client rect resized (423×372, aspect 1.137) confirming the canvas flexes with the dock (flex column, canvas `flex:1 min-height:0` unchanged). Band page (`.traj-timeline-dock` absent there) rendered unaffected after the toggle switch. No console errors throughout. Gate: `python build.py` → 627/627 assertions passed (no pure-math changed, so no new goldens needed).

Not done in this pass (left for a later dock polish, not blocking): burn-marker (`▲`) verification — the seeded Gateway mission's log uses `MNODE`/maneuver-node entries rather than literal `type==='BURN'` events, so `.ttd-burn` count was 0 in this run; the marker code path mirrors Band's own `e.type==='BURN'` check verbatim and will light up on any mission with real BURN log entries. Segment "dim past-viewT" opacity is wired (via a `.ttd-lanes` outerHTML refresh added to `_trajApplyCam`'s existing sync block) but not independently screenshotted.

**Plan rail: DONE (2026-07-15).** Built in `src/js/5749-plan-rail.js`, wired into `_missionMainViewHTML`'s center-column assembly (`src/js/570-mission-lifecycle.js`, `missionRenderDetail`) — when `_missionViewMode !== 'nodemap'`, `.mcc-view-area` is wrapped in a new `.mcc-view-row` flex row alongside `_planRailHTML(m)`; nodemap mode renders `.mcc-view-area` bare, unchanged, so the stage IS the full map there with no duplication. Data reuse, not re-derivation: the leg→pair/chip extraction that used to live inline in `_missionNodeMapHTML`'s edgesHTML block (`src/js/570-mission-nodemap.js`) was factored OUT into `_missionNmEdgePairs(m, byId)` — a pure data function returning `{pairs, chipsByKey}` — and the full node map now calls it too, so this was a behavior-preserving extraction (verified: gate stayed at 772/772, zero new/changed assertions). The rail also reuses `_missionNmNodes()`/`_missionNodePath()` (path highlight), `_nmEdgePhysicsAnnotation` (430) and `_missionLtEdgeEstimate` (570-mission-events.js) for the identical tooltip pricing text the full map shows (including the E3 low-thrust estimate line), and the SAME `missionNodeClick`/`missionEdgeClick` entry points — selection is shared per R5, not reimplemented.

Layout: nodes grouped into the three zones PROG_NM_NODES already carries (`earth`/`lunar`/`interp` — 430's own header comment names these "Earth | Lunar | Interplanetary"); custom nodes (no authored zone) classify by their orbit's body. Edges render as a separate "Transfers" section of compact connector chips (label · Δv, click → `missionEdgeClick`) rather than being interleaved geometrically — the honest compression for a ~200px-wide vertical strip, since the full map's edge geometry is 2D and doesn't fold losslessly into a single vertical list. Reference-orbit-bound nodes (`orbitRefId`) get a ◆ marker (`.pr-ref-marker`) per the spec's "natural home for reference-orbit nodes." Collapse state (`_planRailCollapsed`) and nothing else is per-mission transient view state, same non-persisted pattern as 574's `_trajFrameByMission`; scroll offset joins `_missionSaveScroll`/`_missionRestoreScroll` (570-mission-panel.js) following the existing pattern (`.mcc-plan-rail .pr-body` alongside the node-map/view-area/document entries already there).

Evidence (browser, `devSeedGatewayMission({force:true})`): rail present (`.mcc-plan-rail`, 200px wide) in `traj` and `band` modes, absent in `nodemap` mode. 19 node chips rendered across the three zone groups, 1 transfer edge chip, NRHO node found with `.pr-ref-marker` present, 2 active-path node chips highlighted (`.pr-active`). Clicking a node chip set `m.log[1]._expanded = true` via `missionNodeClick`; clicking the edge chip set `m.log[3]._expanded = true` via `missionEdgeClick` — same selection state the trajectory/gizmo surfaces read. Collapse toggle (`.pr-toggle`) flips `.mcc-plan-rail.collapsed` and the state survived an intervening `missionRenderDetail()` call (transient in-memory state, not lost on re-render). Scroll position (`.pr-body.scrollTop`) survived `_missionRenderPreserveNm` (the standard scroll-preserving re-render path used throughout 570); a bare `missionRenderDetail()` call resets it, matching the existing node-map/view-area scroll behavior (only preserve-scroll call sites carry it forward — not a regression, the same contract as the pre-existing scroll keys). Zero console errors throughout. Gate: `python build.py` → 772/772 assertions passed unchanged (UI-only change, no math touched).

Not done in that pass, explicitly deferred: promotion mechanics (⤢ swap, U2 — next per the sequencing line); the Band|Orbit Map|Trajectory toggle retirement (last, per the sequencing line — still fully functional, unchanged in this pass); geometric (as opposed to list-section) edge placement in the rail; a dedicated screenshot diff harness (screenshot evidence above is a single manual capture, not an automated regression check).

**U2 — promotion mechanics + state-inspector docking + toggle retirement: DONE (2026-07-15). Shell COMPLETE.**

Implemented in `src/js/570-mission-lifecycle.js` (`_missionPromote`, `_missionStageOf`, `_missionStateCardHTML`/`_missionStateCardToggle`, the rewritten `missionRenderDetail` stage/rail/dock composition), `src/js/570-mission-core-state.js` (new `_missionStageSurface` per-mission transient promotion state + `_missionStateCardCollapsed`; `_missionViewMode` demoted to a legacy mirror), `src/js/574-trajectory-view.js` (`_missionWorldThumbHTML` — the compact live World thumbnail), `src/js/5749-plan-rail.js` and `src/js/5747-timeline-dock.js` (⤢ promote buttons added to the rail/dock headers), `src/js/570-mission-panel.js` (`missionSetView` rewritten as a thin alias onto `_missionPromote`), `src/js/570-mission-band.js` (the draw-maneuver flow's direct `_missionViewMode = 'nodemap'` assignment replaced with `_missionPromote(id,'plan')` so the two states can't desync), and `src/css/styles.css` (`.mcc-stage-col`/`.mcc-stage-fill`/`.mcc-dock-slot`, `.mcc-world-thumb{-rail,-corner}`, `.mcc-state-card`, `.pr-promote-btn`/`.ttd-promote-btn`).

Surface model: `_missionStageSurface[missionId]` is `'world'|'timeline'|'plan'` (default `'world'`), transient/non-persisted like 5749's `_planRailCollapsed`. `_missionViewMode` is kept as a compatibility mirror (`'traj'|'band'|'nodemap'`) rather than ripped out, because several pre-U2 call sites branch on it directly and reworking all of them was out of scope for this pass: 570-mission-band.js's low-thrust/maneuver-drawing entry (now routed through `_missionPromote`), 570-mission-nodemap.js's own selective re-render guard, and 5748-lowthrust-gizmo.js's mode-conditional guard. All three continue to work unmodified because `_missionPromote` updates the mirror on every promotion.

Layout: `missionRenderDetail` now always renders `.mcc-view-row` (rail slot + `.mcc-view-area`), and `.mcc-view-area` always contains the state card (absolutely positioned, top-left, `.mcc-view-area` was already `position:relative`) plus a `.mcc-stage-col` (flex column: `.mcc-stage-fill` + an optional `.mcc-dock-slot`). Slot assignment per staged surface: World staged → rail=Plan rail, no external dock slot (World's own markup already embeds the Timeline dock internally, unchanged from U1 — duplicating it externally was judged not worth the complexity, see "not done" below); Plan staged → rail=World thumbnail (`slot:'rail'`, 200px, matches the Plan rail's own width), dock=Timeline dock (external, standalone); Timeline staged → rail=Plan rail, dock=World thumbnail (`slot:'corner'`, absolutely pinned bottom-right over the stage rather than flexed into a literal bottom row — a deliberate CSS-simplicity deviation from a literal "dock slot", documented as the "or collapses" alternative the brief allowed).

Promotion (⤢): plan rail header (`.pr-headerbar`) and the timeline dock (`.traj-timeline-dock`, both its embedded-in-World and standalone renderings) each carry a `.pr-promote-btn`/`.ttd-promote-btn`; the World thumbnail is click-anywhere-to-promote (`onclick` on the whole `.mcc-world-thumb`) plus its own `.mcc-thumb-promote` button. All three call `_missionPromote(id, surface)`.

Thumbnail cost strategy: `_missionWorldThumbHTML` reuses the SAME single source of scene geometry as the full stage (`_trajCamByMission`'s live camera + `_trajWorldSVG`) but renders only the `<svg><g>` scene — no starfield canvas, no globe-layer image reconciliation, no overlay-label resolution pass, no toolbar/footer. Liveness during interaction (camera drag/zoom, MET scrub) needed NO new sync plumbing: `_trajApplyCam`/`_trajSetViewTime` (5740/574) already fall back to a full `missionRenderDetail()` whenever their `.mcc-view-area .traj-wrap[data-mid]` selector target isn't found — which is exactly the case whenever World is demoted — so the thumbnail is regenerated fresh on every such interaction as a side effect of the existing fallback, at the interacting-tier render cost described above.

Evidence (browser, `devSeedGatewayMission({force:true})`, via preview_eval — not screenshots, per the DOM-check preference noted for this preview harness): default render → `.mcc-stage-fill`'s first child is `.traj-wrap` (World staged), rail is `.mcc-plan-rail`, `.mcc-state-card` present and showing "Vehicles & Mission State — current". Promote Plan (`.pr-promote-btn` click) → stage first child becomes `.nm-root` (full node map), rail becomes `.mcc-world-thumb.mcc-world-thumb-rail`, camera (`_trajCamByMission[id]`) byte-identical before/after (`JSON.stringify` equality). Scrubbed view time to 70% of max MET while Plan staged (`_trajSetViewTime`) — thumbnail element survived the re-render (`.traj-svg-thumb` present, non-empty) and `_trajViewTime` reflects the new time, confirming the fallback re-render keeps the thumbnail current. Promote Timeline (`.ttd-promote-btn`) → stage first child becomes `.band-root`, `.mcc-world-thumb-corner` present. Promote World back (click on the corner thumbnail) → stage first child `.traj-wrap` again; camera AND view-time both byte/value-identical to their pre-cycle values across the full world→plan→timeline→world swap cycle. `missionSetView(id,'band'|'nodemap'|'traj')` aliases verified to set `_missionStageSurface[id]` to `'timeline'|'plan'|'world'` respectively (dev-seed/eval compatibility preserved). Event select (`missionSelectEvent(id,0)`) → state card text contains "at LAUNCH" (event-aware behavior preserved); state-card collapse toggle (`.mcc-state-card-toggle`) → `.mcc-state-card.collapsed` present after click. Zero console errors throughout (`read_console_messages` empty at every checkpoint). Gate: `python build.py` → 772/772 assertions passed unchanged (UI-only change, no math touched).

Not done in this pass: a literal flex-row "bottom dock slot" for the World thumbnail when Timeline is staged (implemented as an absolutely-pinned corner overlay instead — see Layout above, an explicitly allowed brief alternative); an external, non-embedded Timeline dock instance alongside World when World is staged (the existing U1 embedding is reused as-is rather than decomposed, to avoid duplicating scrub-state); draggable-between-corners for the state card (skipped per the brief's own "skip if it bloats scope" allowance — only collapse/expand is implemented); a dedicated automated screenshot-diff regression harness (this pass's evidence is interactive DOM/JS-eval verification, matching this session's preview-tab reliability constraints, not a pixel-diff suite). The Band|Orbit Map|Trajectory toggle IS fully retired (removed from `.mcc-view-toggle-float`) — this was done in the same pass as the promotion mechanics rather than as a strictly separate final step, since the toolbar buttons and `_missionPromote` were implemented together and verified together before removal; no regression was found in that ordering.

### U3 — tri-view restoration (SUPERSEDES the one-stage promotion grammar; user 2026-07-15)

**Why the reversal.** After living with U1/U2, the user judged the one-stage-two-rails promotion grammar a net negative: the HUD strip "atrocious", the left Plan rail "does nothing good for most of the views", and the ⤢ promotion "really just selectors, but in a more complicated and inefficient way". BUT the user explicitly keeps the ONE good insight U1/U2 struck: **"not only the central map should be switching, but almost all the panels should, too."** U3 = keep that insight, delete the machinery. This is a distillation, not a pure revert to the pre-§12 fixed toggle.

**Target layout grammar (mockup: claude.ai/code "mission_triview_layout_grammar", option A confirmed):**
- **Persistent frame (identical across all three views):**
  - *Top universal strip* — thin: timeline position (MET/date + a minimal scrubber = "where you are") + the active-vehicle name ("which vehicle you're modifying"). Minimal by decree — nothing else universal except the readiness counts chip (right end; badges stay on the cards).
  - *Left column* — the **Vehicles & Mission State** panel RETURNS to its pre-§12 home (user 2-2). Full per-vehicle state, event-aware ("— at EVENT / — current" preserved). The vehicle color swatch (was on the HUD chip) moves here; the band-legend picker stays the one write path.
  - *Right column* — event cards + Add Event, **untouchable** (keep exactly as-is).
- **Center switchable region (map + its own panel swap together as a unit):**
  - Visible tri-toggle **World | Timeline | Node map** at the top of the center region — the primary, exclusive view switch. `_missionViewMode` becomes real state again (not a mirror of `_missionStageSurface`); `missionSetView(id,'traj'|'band'|'nodemap')` is the driver, not an alias onto `_missionPromote`.
  - The active view's map fills the center (the three renderers `_missionTrajViewHTML`/`_missionBandViewHTML`/`_missionNodeMapHTML` are UNCHANGED — U3 is chrome, not map rendering).
  - **Per-view assistive panel docked UNDER the map (option A — uniform horizontal slot, all three views).** Scaffolded containers with per-view ids so they can be filled later. Two have obvious homeless content to re-home NOW rather than orphan: **World panel** = the camera/frame/anchor selects (currently floating on the World stage — move them into the docked panel, killing the last floating chrome); **Node map panel** = the ORBITS catalog (`_missionOrbitPaletteHTML`, currently in the dying Plan-rail slot — its natural home). **Timeline panel** = blank scaffold for now. (User said "leave them blank for now" — the two re-homes are exceptions that solve real orphaning; flagged for user veto.)

**REMOVE (all U1/U2/workflow-pass machinery):** `_missionHudStripHTML` (HUD strip) · the Plan rail (5749-plan-rail.js — delete the module + all call sites, node panel gone from every view) · promotion (`_missionStageSurface`, `_missionPromote`, the ⤢ controls `.pr-promote-btn`/`.ttd-promote-btn`/`.mcc-thumb-promote`, the World thumbnail `_missionWorldThumbHTML`, `.mcc-stage-fill`/`.mcc-stage-col` staging chrome, the floating `.mcc-view-toggle-float`) · the U1/U2 timeline-dock-as-bottom-rail-of-World (the top universal strip's minimal scrubber replaces it; the FULL band lanes live in the Timeline view). `missionSetView`/`_missionViewMode` survive as the real toggle (dev seeds + eval tooling keep working).

**KEEP:** the three map renderers; selection stickiness (event-click keeps the owning vehicle selected, 570-mission-cards); orbit-ring casing + selected/unselected contrast; `_missionVehicleColor` unification; the event cards + Add Event flow; readiness badges on cards (572).

**Sequencing (user 2026-07-15): base UI (U3) FIRST, then injections (the event-model split — its own thread, spec'd separately).** U3 is a big teardown + rebuild — run as a dedicated Sonnet pass, gate-green + browser-verified, not the Haiku UI lane. Success = the mockup grammar renders, view-switch swaps map+panel while the frame persists, no promotion/rail/HUD DOM remains, camera/selection/view-time survive switches, gate unchanged, zero console errors.

**U3 as-built (2026-07-15): DONE.** `_missionViewMode` is real state again (570-mission-core-state.js); `missionSetView(id,mode)` (570-mission-panel.js) sets it directly and re-renders — no `_missionPromote`/`_missionStageSurface` alias layer. `missionRenderDetail` (570-mission-lifecycle.js) now renders `.mcc-top-strip` (built by new `_missionTopStripHTML`: MET + UTC date + the UNCHANGED `_trajScrubberHTML` scrubber + active-vehicle name + the readiness chip) above `.mcc-body`, which holds `.mcc-left-col` (`_missionMultiVehicleHTML`'s unchanged event-aware output, now with a per-row color swatch — `_missionVehSwatchHTML`, same `missionSetLaneColorForVehicle`/`missionResetLaneColorForVehicle` write path as before), `.mcc-center-col` (new `_missionViewToggleHTML` tri-toggle + undo/redo, then `.mcc-view-area` with exactly one of the three UNCHANGED renderers, then `.mcc-view-panel` — World's panel is `_trajCamToolbarHTML` (574-trajectory-view.js, extracted from the old embedded `.traj-toolbar`), Node map's panel is `_missionOrbitPaletteHTML`, Timeline's panel is an empty scaffold), and the untouched `.mcc-right-col`.

**Removed:** `5749-plan-rail.js` and `5747-timeline-dock.js` (both deleted outright — neither is in the test-gate's module list; the Timeline view is `_missionBandViewHTML`, which never used the dock's lane model, so nothing was left to reuse per the brief's "keep the lane model if the Timeline view reuses it" clause); `_missionHudStripHTML`, `_missionWorldThumbHTML`, `_missionPromote`/`_missionStageOf`/`_missionStateCardHTML`/`_missionStateCardToggle`, `_missionStageSurface`/`_missionStateCardCollapsed`; CSS for `.mcc-hud-strip`, `.mcc-plan-rail`/`.pr-*`, `.mcc-stage-fill`/`.mcc-stage-col`/`.mcc-dock-slot`, `.mcc-world-thumb*`/`.mcc-thumb-promote`, `.mcc-state-card*`, `.ttd-*`/`.traj-timeline-dock`. `missionSetAddEvt`'s draw-maneuver entry (570-mission-band.js) now calls `missionSetView(id,'nodemap')` directly instead of `_missionPromote`.

Evidence (browser, `devSeedGatewayMission({force:true})`, IIFE-wrapped preview_eval): default render shows `.mcc-top-strip`/`.mcc-left-col`/`.mcc-view-toggle`/`.mcc-view-panel-world` present, active toggle button "World", `.mcc-view-panel-world .traj-toolbar` present (camera toolbar relocated); every U1/U2 DOM check (`.mcc-hud-strip`, `.mcc-plan-rail`, `.mcc-view-toggle-float`, `.pr-promote-btn`, `.mcc-world-thumb`, `.mcc-state-card`) absent. Cycle test: set camera anchor to Moon + select event 1 (view-time armed) → switch World→Timeline (`.band-root` staged, `.mcc-view-panel-timeline` present and empty, top strip/left col unchanged) → Timeline→Node map (`.nm-root` staged, `_missionOrbitPaletteHTML` present in `.mcc-view-panel-nodemap`) → Node map→World: camera (`_trajCamByMission[id]`, JSON-compared) and view-time (`_trajViewTime`) both byte/value-identical before vs. after the full cycle. Left panel event-aware: `missionSelectEvent(id,1)` → header reads "Vehicles & Mission State — at DEPLOY 2 · T+17:28". Readiness chip in the top strip read "● 1 red / 1 amber"; clicking it selected the worst finding's event (index 2). Vehicle swatch in the left panel: setting its `<input type=color>` and firing `change` wrote `m.laneColors['Saturn 1B & V S-IVB'] = '#ff00ff'` via the existing `missionSetLaneColorForVehicle` path. World panel's anchor `<select>`: setting it to "Earth" and firing `change` updated `_trajCamByMission[id].anchorBody` to `"Earth"` through the reused `trajSetFocus` handler. Right column (`.mcc-right-col`, `.mcc-events-header`, `.mcc-addevt-dock`, 4 `.mcc-evt-row` cards) rendered unchanged throughout. Zero console errors at every checkpoint (`read_console_messages` empty). Gate: `python build.py` → 877/877 assertions passed (UI-only change, no math touched, no new goldens needed).

Not done / deferred: no draggable/collapsible chrome for the left panel or the per-view panels (the brief allowed skipping collapse — "not required"); the Timeline and Node-map panels weren't pixel-polished (Timeline is intentionally blank per the brief; the Node-map orbit catalog reuses its existing rail styling with a height override rather than a bespoke horizontal layout); a dedicated automated screenshot-diff harness wasn't built (evidence above is interactive DOM/JS-eval verification, consistent with this session's preview-tab reliability constraints).

---

## 13. PHASE 3 SPEC — reference orbits, dwell/transit, direct manipulation (drafted 2026-07-14)

**Goal:** orbits become first-class shared objects (§4.3), the node map becomes the dwell/transit intent model (§4.4), and manipulating an orbit re-solves its adjacent transfers locally (D1). This is the substrate for Gateway-style architectures and the §12 Plan rail.

### T1 — Reference-orbit catalog (model + persistence)
New module `src/js/425-reference-orbits.js`: `PROG_ORBIT_CATALOG` (builtin tier: LEO 185, LEO 400/51.6 "station", SSO 800, GTO, GEO, LLO 100, lunar NRHO placeholder marked `kind:'propagated'` but UNSEEDED until Phase 4) + user tier (global catalog, persisted via a new session/autosave key + `.program` bundling; join `_buildSessionObject` per 455's rule). API: `refOrbitGet(id)`, `refOrbitAdd/Update/Delete`, `refOrbitResolve(id)` → elements. Keplerian kind only in Phase 3; `propagated` kind exists in the schema + resolver stub (returns null with note) so Phase 4 slots in without schema change. Gate: CRUD + persistence round-trip + resolve.

### T2 — Events bind by orbitId — **DONE 2026-07-14** (measured evidence below)
LAUNCH/DEPLOY orbit specs and solved-maneuver targets gain `orbitRefId` alongside the inline numbers; the inline numbers become the CACHED RESOLUTION of the ref (re-resolved on recompute; a ref edit propagates to every binder on next recompute — this is "hit the same orbit multiple times" working). Authoring boundaries: the launch card + node map bridge offer catalog picks; editing an inline value on a bound event DETACHES it to a one-off (explicit, visible), mirroring solved/manual. Node-map nodes carry the binding. Gate: two events bound to one ref; ref edit moves both on recompute; detach isolates.

**T2 as-built (2026-07-14):** binding implemented for LAUNCH + DEPLOY (`e.orbitRefId`, resolved in `missionRecompute` BEFORE the type dispatch — see MATH.md §7r); solved-maneuver targets NOT bound in T2 — maneuver targets are node-map node ids, and the node/ref unification is deliberately deferred to T4's orbit inspector (binding a maneuver's target through a node id today would be a second, conflicting indirection). Authoring UI = launch card catalog `<select>` (`missionLaunchRefPick`/`missionLaunchOrbitDetach`, "(custom)" note + `_refNote` display); the node-map bridge keeps authoring by node id, per the same T4 deferral (task allowed launch-card-only, so stated). Gate (tests/math.test.js, 4 assertions): two DEPLOYs bound to one user ref both resolve (250/45); `refOrbitUpdate`→recompute moves the still-bound one to 300/60 while a detached sibling keeps 250/45; a deleted ref keeps cached values + stamps `_refNote`, no throw. Browser: seed launch bound to `leo-185` (185/185/28.5); user-ref copy bound + `inc 28.5→30` update → recompute moved the launch inc to 30 (TLI dv stayed 3143 — the node-map edge is LEO-node→TLC, unaffected by launch-orbit inc, as expected); hand-edit detach cleared `orbitRefId` and the card shows "— custom —"/"(custom)"; session export→import (JSON round-trip) preserved the user ref AND the binding.

### T3 — Dwell/transit + terminology — **DONE 2026-07-14** (measured evidence below)
Transit corridors (TLC-class) stop being NODES: the node map renders dwell orbits only; the transfer between them is the EDGE, carrying its burns (departure TLI / arrival LOI as named burn chips on the edge). `devSeedApolloMission` re-authored: LEO 185 (dwell) → edge [TLI 3143 … LOI ~1028] → LLO 100 (dwell). Terminology audit rides along: TLC→"trans-lunar" edge, TLI/TMI/LOI/MOI name BURNS. The transit state still EXISTS in the timeline (the corridor leg) — it's just not a planning node. Band/report/checks re-verified against the new seed shape. Gate: seed produces dwell-only node map; totals unchanged (13,400-class).

**T3 as-built (2026-07-14):** the **minimal-log-change path won** — `m.log` keeps its two MANEUVER entries (LEO→TLC, TLC→LLO); only the node-map extraction merges a dwell→transit→dwell chain into one edge (re-authoring the log would have forced new replay semantics for a multi-burn event and touched boiloff/physics-leg attribution for zero model gain). `devSeedApolloMission` therefore only gained the `leo-185` binding (T2), not a new log shape — still a valid V2 log. `_nmBurnNames` (430, pure, gate-tested): Moon/Mars/Venus canon + `{injection, insertion}` fallback. Transit nodes skipped in the node loop; merged edge dashed, chips at 1/3 and 2/3. Terminology: `_missionManeuverNodeLabel` — transit as destination = "TLI (trans-lunar)", as origin = "trans-lunar coast"; labels refreshed each recompute so cached logs migrate. Measured on the seed (browser): node map has NO TLC node, LEO + LLO dwell nodes present, edge chips `[TLI, LOI]`, tooltip "trans-lunar transfer … flown ✓ · TOF 4.9d — TLI 3,143 m/s, LOI 822 m/s" (822 = LOI's `dvRequired`; the spec's "~1028" was a drafting estimate — the physics didn't move); event labels "LEO (185 – 400 km) → TLI (trans-lunar)" / "trans-lunar coast → LLO (100 km lunar)"; report shows "TLI (trans-lunar)", no corridor-as-orbit phrasing left; band + report render with zero console errors; **budget pinned: ΔV expended 13,400 m/s, unchanged** (presentation-only change, as required). Gate: 606 passed, 0 failed (594 pre-existing + 4 T2 + 8 T3).

### §12 addendum: workflow pass 1 — badges/HUD/colors (2026-07-15)

Three additive, conservative UX changes on top of the v3.0 shell above — no camera/fly-to/promotion/orbits-workflow behavior touched.

**A — Flight readiness dissolves into badges.** The standalone FLIGHT READINESS box (`_missionChecksBoxHTML`, 572-mission-checks.js) is no longer called from the layout (`570-mission-lifecycle.js`'s left column); the function itself is left defined but unused rather than deleted. 572 gained: `_missionChecksByAuthIdx(m)` (authIdx → findings), `_missionChecksWorstFinding(m)` (red beats amber, for click-to-select), `_missionChecksEventBadgeHTML(m, authIdx)` (● dot(s), red/amber only), `_missionChecksInlineHTML(m, authIdx)` (full finding text, bordered `--danger-tint`/`--warn` block). Re-homed onto: the event card header row + body (`570-mission-lifecycle.js`'s `card()` closure — badge always, inline text only while expanded), the timeline-dock ▲ burn marker (`.ttd-burn-dot`, `5747-timeline-dock.js`), and the plan-rail node chip (`.pr-check-dot`, `5749-plan-rail.js`, best-effort via the anchored event's `fromNode`/`toNode` — skips cleanly for findings with no node, e.g. dead-mass/crew-stranded). The toolbar chip (`_missionChecksToolbarChipHTML`, pre-existing) now also appears at the HUD strip's right end; its click handler (`missionChecksOpenFromToolbar`) was rewired from "scroll to the old panel" to "select the worst finding's event via `missionChecksGoTo` — the SAME shared path the event list itself uses. 572's check logic (`missionRunChecks`) is byte-untouched.

**B — HUD strip.** `.mcc-state-card` (corner-pinned) is retired; `_missionHudStripHTML` (570-mission-panel.js) renders a thin strip (`.mcc-hud-strip`, ~30px) docked across the top of `.mcc-view-area`, above `.mcc-stage-col`. It reuses the EXACT data path `_missionMultiVehicleHTML` used (event-scoped via `_missionSelectedEventSnapshotEntry` vs. live via `_missionLiveVehicles`/`missionBudget`) — presentation-only change, numbers are byte-identical for the same selection. Per-vehicle chips (name/orbit/ΔV left/prop left) + a totals chip + the readiness chip (right end). Vehicle chips click → `missionSetActiveVehicle` (the existing selection semantics already used by the old panel's live-vehicle rows) — no popover was needed since that selection concept already existed. No camera fly-to (deferred). Collapse chevron reuses the existing per-mission transient `_missionStateCardCollapsed`. CSS: `.mcc-view-area` became `display:flex;flex-direction:column` (strip + `.mcc-stage-col` as flex children instead of the strip being position:absolute over the stage); `.mcc-stage-col` changed from `height:100%` to `flex:1;min-height:0` to make room; `.mcc-stage-fill` gained `position:relative` so `.nm-root`'s `position:absolute;inset:0` still anchors to the stage (not the whole view-area including the strip) — regression-checked by driving the node map after the change.

**C — Vehicle-assigned colors.** `_missionVehicleColor(m, vehicleKey, fallback)` (570-mission-band.js) is the one color-source function: tries `m._ownerLabels[vehicleKey]` → `m.laneColors[label]` directly (the path 5741's scene extraction already used via `_missionLaneColor`, unchanged), and falls back to asking `_missionBandModel(m)` which owner lane(s) share that runtime vehicle id (a vehicle can host >1 band "owner" — e.g. a docked LV stage + payload) and returns the first one with a custom color. Consumed by: the HUD strip chip accent (`border-left-color`), and (already, pre-existing, verified not regressed) the trajectory legs/rings via `_missionLaneColor` at `5741-trajectory-scene-extract.js:96` and the timeline-dock lane border via `L.color` from `_missionBandModel`. Returns `fallback` (null) when nothing custom is assigned — missions with no `m.laneColors` see zero visual change.

Evidence (`devSeedGatewayMission({force:true})`, browser eval, IIFE-wrapped): old panel absent (`.mcc-box .mcc-box-hdr` → false), `.mcc-hud-strip` present, `.mcc-state-card` absent. Seeded mission had 1 red (`maneuver-from-mismatch`, authIdx 2) + 1 amber (`dead-mass`, no anchor) finding. Toolbar/HUD chip text `"● 1 red / 1 amber"`; clicking it selected event index 2 (`.mcc-evt-row.sel` id `mlog-<id>-2`), which showed both the header dot (`.mcc-check-dot`) and the inline block containing "Maneuver from-node mismatch". Promoting to World: `.ttd-burn-dot` and `.pr-check-dot` both present. Assigned `m.laneColors['Saturn 1B & V S-IVB'] = '#ff00ff'` + `missionRecompute` → HUD chip `style="border-left-color:#ff00ff;"`, a trajectory leg `<path stroke="#ff00ff">` found, and `.ttd-lane-label` style `border-left-color:#ff00ff` — all four surfaces (chip/leg/ring-path/dock) agree. Deleting the override + recompute → chip style reverts to `""` (default). Zero console errors throughout. Gate: `python build.py` → 772/772 assertions passed (UI-only pass, no math touched).

Not done / known limitation: the HUD strip's vehicle→color resolution is necessarily a "first owner on this vehicle with a custom color" heuristic, not a strict 1:1 — a vehicle hosting two DIFFERENTLY-colored owners (e.g. a docked LV stage and a payload with distinct lane colors) shows only one accent on its single HUD chip; this is inherent to the HUD being vehicle-granularity while `m.laneColors` is owner-granularity, and was flagged rather than silently worked around.

### §12 addendum: workflow pass 2 — sidebar removal + toolbar grammar (2026-07-15)

User-agreed, additive/conservative, no camera/fly-to/promotion/orbits-workflow changes. Toolbar grammar going forward: **selectors** (which surface is staged) are primary and live first in the mission toolbar; **camera context** (anchor + reference frame) lives ON the stage surface it controls, not in chrome; **program-level actions** (File menu) live in the GLOBAL header, not the mission toolbar.

**A — far-left `.mcc-left-col` removed.** It was already down to leftover chrome after pass 1 retired its two panels (Vehicles & Mission State → HUD strip; Flight Readiness → badges) — its only still-live occupant was the ORBITS catalog (`_missionOrbitPaletteHTML`, shown only while Plan is staged) plus a one-off "Setup" hint shown before any vehicle is launched. Both were re-homed, not dropped:
  - ORBITS catalog → stacked above the demoted World thumbnail inside the existing Plan-staged rail slot (new wrapper `.mcc-plan-orbit-rail`, same 200px/bordered treatment as `.mcc-plan-rail`), so it's still reachable in the same physical rail column adjacent to the stage.
  - Setup hint → moved above the events list in the right column (the very thing it's telling the user to use), gated on `!m.vehicleId && stageSurf !== 'plan'`.
  - `.mcc-left-col` and the already-dead `.mcc-left-budget` CSS rules are deleted (not just unused-but-present); `.mcc-center-col` claims the freed width (kept its `border-right` separating it from the events column — verify this if touching that rule again, it's easy to drop by accident when deleting a neighboring column).

**B1 — planetary-body anchor tabs → compact dropdown on the stage.** The button+flyout body-tab row in `.traj-toolbar` (574-trajectory-view.js) is now a single `<select class="traj-anchor-select">`, sibling to the existing `.traj-frame-select` (anchor + frame = camera context, both camera-surface chrome, not mission-toolbar chrome). Reuses the exact same handler (`trajSetFocus`) the tabs called — camera behavior is byte-identical, only the affordance changed. `.traj-toolbar` changed from `justify-content:space-between` (full-width bar) to `flex-start` (compact cluster pinned top-left of the World stage), Reset pushed to the far right via `margin-left:auto`. Dead code left in place per the pass-1 convention: `_trajToggleFlyout`/`_trajFlyoutOpenFor`/`_trajCloseFlyout` (5740/574) and the `.traj-focus-seg`/`.traj-flyout-*` CSS are no longer referenced by any emitter but not deleted.

**B2 — File ▾ menu → global header.** Was mission-toolbar chrome (`.mcc-export-wrap` inline in `missionRenderDetail`'s `.mcc-view-toggle-float`); moved to `src/index.html`'s header, next to the ⇣/⇡ Session buttons (program-level action, not mission-level). Implementation: `_globalFileMenuHTML()` + `_globalFileMenuRender()` (new, 570-mission-manager.js) render the SAME markup (same classes/ids — `.mcc-export-wrap`, `#mcc-export-menu`) into a static `<span id="global-file-menu-slot">` in the header, so the pre-existing `_missionToggleExportMenu`/`_missionCloseExportMenu`/outside-click handlers work completely unmodified. Rendered once at startup (`DOMContentLoaded`, guarded with `typeof document.addEventListener === 'function'` for the math-test harness's stubbed `document`) and refreshed at the tail of every `missionRenderDetail()` pass, so the program/mission name fields and the Reset item's visibility (`m.log.length`) stay live across mutations.

**B3 — World/Timeline/Plan selector buttons.** Three `.mcc-stagesel-btn` buttons, first in `.mcc-view-toggle-float`, wired to `_missionPromote(id, surf)` (the same function the rail/dock/thumb ⤢ promote controls call — those stay as the secondary path, unchanged). Active state = `var(--accent)` fill, matching the retired Band|Orbit-Map toggle's old look.

Resulting mission-toolbar order: `[World|Timeline|Plan]` · undo/redo · readiness chip (unchanged from pass 1 — still HUD-strip-adjacent via the toolbar chip, not moved). Frame selector stays camera-surface chrome per B1 (not toolbar).

Evidence (`devSeedGatewayMission({force:true})`, browser eval, IIFE-wrapped, gate 772/772 green): `.mcc-left-col` absent; `.mcc-plan-orbit-rail .mcc-orbit-cat` present while Plan staged; anchor `<select>` present next to the frame `<select>`, old focus-tab buttons absent; setting the anchor select to `Moon` updated `_trajCamByMission[id].anchorBody` to `"Moon"` (confirms the reused `trajSetFocus` handler fired); header File menu present with Save Program/Load Program/Report/PNG/Reset + program+mission name fields, `onclick="_missionCloseExportMenu();saveProgramFile()"` unchanged; `.mcc-stagesel-btn` active class correctly tracked the staged surface (World/Timeline/Plan); deleting an event then `missionUndo()` restored it (undo/redo unaffected); zero console errors.

### §12 addendum: workflow pass 3 — toolbar merged into HUD strip + ring casing (2026-07-15)

**Toolbar merge.** The floating `.mcc-view-toggle-float` (World/Timeline/Plan selectors + undo/redo) occluded the stage — its contents moved into the HUD strip (`_missionHudStripHTML`, 570-mission-panel.js): stage selectors at the strip's left end (before the "Vehicles & Mission State" label), undo/redo at the right end next to the readiness chip; shown even when the strip is collapsed. `.mcc-view-toggle-float` is removed (element + CSS); the strip is now the mission's one docked control bar — nothing floats over the stage except the camera-context selects on the World surface (unchanged per B1 above).

**Ring/leg casing.** Non-active orbit rings and trajectory legs were a thin `rgba(255,255,255,0.42)` line — invisible against a bright day-side globe at close zoom. Fix: a dark `rgba(0,0,0,0.55)` casing stroke (~2.5× the line's width) drawn immediately beneath the light stroke, one casing path per visible (occlusion-split) run, reusing that run's exact geometry — so occlusion breaks case identically with no halos. Applied to `_trajRingSVG`/`_trajPropagatedRingSVG` (5743-trajectory-rings-legs.js), the physics-leg polyline in `_trajPhysLegRender` (same file), and the schematic (unconverged) transfer arc in 574-trajectory-view.js. Measured: ~10ms/frame rebuild on the Gateway seed (4-event mission), well under any perceptible perf concern.

### §12 addendum: HUD color swatch — discoverable lane-color picker (2026-07-15)

User report (2026-07-15): "I can't figure out how to change the color of the orbit in the trajectory map." Pass 1's `_missionVehicleColor` (570-mission-band.js) made `m.laneColors` the one color source everywhere (band/trajectory/timeline/HUD accent), but the ONLY control that could set it was the band-view legend's per-lane `<input type="color">` (`missionSetLaneColor`). Fix: each HUD vehicle chip (`_missionHudStripHTML`, 570-mission-panel.js) now renders a `.mcc-hud-swatch` native color input showing the effective color (`_missionVehicleSwatchColor`); `onchange` calls new `missionSetLaneColorForVehicle(missionId, vehicleKey, hex)`, which resolves the vehicle's primary owner label via new `_missionVehiclePrimaryLabel` (same direct-label / band-lane-lookup order `_missionVehicleColor` already used) and then calls the EXISTING `missionSetLaneColor` — one write path to `m.laneColors`, no parallel storage. Reset gesture: right-click the swatch (`oncontextmenu`) calls new `missionResetLaneColorForVehicle`, which deletes the label's override and reuses the same autosave/undo/re-render tail as `missionSetLaneColor`. Chosen over an x-in-popover as the simpler affordance requiring no extra markup. Granularity note (per pass 1): the swatch edits the vehicle's PRIMARY owner key only — the same one the chip's accent already reflects — so a multi-owner vehicle's non-primary owner still needs the band-view legend for its own lane.

### T4 — Direct manipulation (the §12 payoff) — **DONE 2026-07-14** (measured evidence below)
Selecting a dwell node (node map or plan-rail-precursor) flies the trajectory camera to it and opens the ORBIT INSPECTOR: sliders + numeric fields for alt(peri/apo)/inc/LAN (the user's requested representation), editing the bound reference orbit (or one-off) live: scrub → cheap re-render of the ring; release → recompute, and the UPSTREAM edge re-solves (D1) while downstream edges re-solve only from their moved origin. Solved edges follow; manual burns pinned (existing mode semantics). Undo = one step per slider release. Gate: pure re-solve-locality test (edit node N: edge N-1 re-solved, edge N+1 re-solved from new origin, edge N+2 UNTOUCHED). Browser: full flow on the seed.

**T4 as-built (2026-07-14):** new module `src/js/5746-orbit-inspector.js`, hooked into the SAME `_missionNmSelectShared`/`_trajSelectEventFromView` entry points the gizmo already shares (570/574) — a LAUNCH/DEPLOY or solved-maneuver `toNode` dwell selection opens the card; a manual vector-burn MNODE selection does not (verified: gizmo still opens for a manual burn, inspector stays closed). Card renders inside `.traj-canvas` (own class `.traj-oi-card` — NOT `.traj-gizmo-menu`, see MATH.md §7r for why sharing that class silently deleted the card every render). Two independent binding systems handled uniformly (fork-builtin / mutate-user / edit-unbound): LAUNCH/DEPLOY via 425's `orbitRefId` (T2), maneuver `toNode` via 430's builtin/custom node split (the same split T2 deliberately left maneuver targets out of). Commit (`_oiCommit`) writes via the binding rule then calls `missionRecompute` once; D1 locality and the one-undo-step guarantee both fall out of EXISTING machinery (`progNmComputeEdgeDv`/T2's ref-resolution re-read fresh every recompute; `missionUndoCapture`'s dedupe), not new bookkeeping — see MATH.md §7r for the full mechanics writeup, the LAUNCH-origin edge-sync design (a T4-introduced fix for a real gap: the seed's first maneuver's `fromNode:'leo'` is a SEPARATE representation from the LAUNCH's `orbitRefId`-bound orbit), and a documented finding that the transit corridor's schematic arrival edge (`progNmComputeEdgeDv`'s LOI branch) is a fixed patched-conic lookup that doesn't yet respond to the bound `toNode`'s own elements (pre-existing limitation, not a T4 defect).

Measured on the seed (browser console, `devSeedApolloMission({force:true})`): selecting LAUNCH (bound `leo-185`) → inspector shows 185/185/28.5/0, camera flies to Earth; committing peri+apo→300 auto-forks `refOrbitCatalogList()` to include `"LEO 185 (copy)"` and rebinds; the LEO→TLC edge (delivering/upstream) re-solves `dvRequired` 3143→3148 m/s via the origin-sync fromNode rebind; the TLC→LLO edge's real physics `dv_actual` (§7q's arrival-burn construction) moves 1028→512 m/s as a legitimate downstream ripple from the shifted corridor state, while its schematic `dvRequired` (822) and the LLO dwell's own orbit stay untouched; one `missionUndo()` call reverts BOTH the fork and the 3148→3143 dv atomically. Selecting LLO directly (bound to the 430 builtin `'llo'` node) auto-forks a custom `"LLO (COPY)"` node and rebinds `e.toNode` correctly at the data layer. No console errors observed across the flow. Scrub-preview: chosen approach is mutate-the-live-orbit-object + throttled `_trajApplyCam` (the task's sanctioned cheap fallback) — numeric fields update live, but the RING does not visibly move during scrub because this codebase's rings are sourced from real propagated `_physTrajByMission` legs, not the schematic elements, until an actual recompute reruns physics; the ring does update correctly on commit. Full mechanics, evidence, and the two open findings are in MATH.md §7r. Gate: `tests/math.test.js` gained a pure D1 re-solve-locality test (DOM-free, synthetic 4-dwell custom-node chain A-B-C-D): editing node B's altitude moves both adjacent edges (A→B, B→C) and leaves the two-hops-away edge (C→D) exactly unchanged. 606 → **609 assertions**, all green.

### T5 — Acceptance
Gate green: 570 (Phase 2 end) → 606 (T1-T3) → **609 (T4)**, `python build.py` passing end to end (JS syntax check + `node tests/math.test.js`). Seed: dwell-only map, bound orbits (LAUNCH→`leo-185`), slider edit on the LAUNCH dwell → upstream edge re-solve (3143→3148 m/s) + a legitimate downstream real-physics ripple (LOI `dv_actual` 1028→512 m/s) → totals move sensibly (documented, not asserted as "correct" — a real model consequence of the moved departure orbit) → undo clean (one step, verified). Ref shared by two launches moving together was already proven in T2 (unchanged by T4). All §11-era numbers stable where unaffected (LAUNCH ascent Δv, undisturbed edges' schematic `dvRequired`). Docs: MATH.md §7r (edge-burn naming carried from T3; T4's binding taxonomy, commit/D1 mechanics, LAUNCH-origin sync design, measured evidence, and two honestly-reported findings — the schematic corridor-arrival edge not reading its bound toNode, and the scrub-preview ring not repainting from schematic-only mutation), this section's measured results above. Not done / explicitly deferred to Phase 4+: a bespoke ring-only scrub-preview renderer (schematic-Keplerian, bypassing the physics-leg-sourced extraction) and generalizing the transit-corridor arrival edge to read its bound `toNode`'s actual elements.

*Out of scope: NRHO seeding (Phase 4), phase-matching/rendezvous (Phase 5+), the full §12 shell (Timeline dock/promotion — separate track), semantic zoom.*

---

## 14. PHASE 4 SPEC — NRHO (drafted 2026-07-14) — **DONE 2026-07-14** (see MATH.md §7s for full measured evidence)

**Goal:** the `propagated` reference-orbit kind becomes real, seeded with the canonical Gateway-class 9:2 southern L2 NRHO, propagated by the existing integrator and rendered as a genuine three-body orbit in the trajectory view. This is the catalog's power move and the proof of the `propagated` kind end-to-end.

**As-built summary (annotated per U1-U4, MATH.md §7s has the full honest writeup):**
- **U1 — DONE, with an honest deviation.** The literal 9:2 seed (rp≈3,400/ra≈70,000, P≈6.56d) does not converge in this SOI-patched restricted-3-body integrator (the real 9:2's outer arc needs rotating-frame L2 dynamics this model doesn't have). A shorter-period NRHO-CLASS family member converged cleanly: rp=3,000 km, ra=60,000 km, P=4.71 d (406,944 s), one-rev closure 468.6 km (< 500 km gate). 3-rev boundedness is WEAK — perilune decays across revs (documented, not hidden; critique 61). Corrector: 3-DOF FD-Jacobian Newton (`[vx,vy,P]`), run offline via node+vm, hard-coded — no runtime correction ships.
- **U2 — DONE.** `nrho-nominal` (425) carries `{kind:'propagated', seedState, period_s, provenance comment}`. `refOrbitResolve` returns the propagated descriptor; `refOrbitSamplePropagated(id, nSamples)` and `refOrbitPropagatedStateAt(id, metOffset)` (new) are the two read paths every consumer shares.
- **U3 — DONE.** Ring: `_trajPropagatedRingSVG` (574) renders the real sampled loop (measured bbox ~38.8×18.8 render units, ~2:1 — asymmetric, not circular). DEPLOY: new "Ref Orbit" picker on the DEPLOY edit form (a LAUNCH's picker excludes `kind:'propagated'` entries, per spec — a LAUNCH can never target one); `_missionApplyDeploy` samples the ref at the event's actual MET. Every peri×apo×inc display (budget card, state panel rows, DEPLOY log card, orbit inspector) now honestly shows "NRHO (propagated)" instead of computing nonsense Kepler numbers from null fields. Orbit inspector (5746): read-only card (name/body/period/measured perilune-apolune), no sliders.
- **U4 — DONE.** Gate: 7 new assertions (611 → 618 total in `tests/math.test.js`) — pinned-seed closure verification (per the spec's own "gate only verifies, doesn't re-run Newton" escape hatch), period-band sanity, sample-loop non-degeneracy/asymmetry, catalog exposure, DEPLOY-anchor state validity. Browser: DEPLOY-on-`nrho-nominal` on the Apollo seed produces a real Moon-frame anchor state, a rendered asymmetric ring, honest state-panel labeling, sane budget numbers, a working read-only inspector card, and zero console errors.

- **U1 — periodicity corrector (565 or a small 416 module, pure):** `physPeriodicOrbitCorrect(seedState, periodGuess, ctx, opts)` — differential correction (FD Jacobian, same discipline as `physShootToTarget`) minimizing `|state(t0+P) − state(t0)|` over 2–3 free parameters (perilune velocity components + P). In the REAL ephemeris model (not CR3BP) exact periodicity doesn't exist — target a bounded quasi-periodic solution: acceptance = position closure < ~500 km over one rev, bounded over ≥3 revs (NRHOs are weakly unstable; document the achieved drift honestly, MATH.md §7s + critique). Seed guess: perilune ~3,400 km radius over the lunar south pole, apolune ~70,000 km, period ~6.56 d, plane near-polar oriented toward the Earth-Moon line; let the corrector refine.
- **U2 — catalog seeding:** `nrho-nominal` gets `{kind:'propagated', frame:'Moon', seedState:{r,v}, period_s, epochRef}` from U1 (computed OFFLINE during implementation, hard-coded as data with provenance comment — no runtime correction). `refOrbitResolve` returns the propagated descriptor; a small `refOrbitSamplePropagated(id, nSamples)` propagates one period for consumers.
- **U3 — render + bind:** a node/event bound to a propagated ref renders its ring as the propagated closed-ish loop (reuse the physics-leg polyline machinery — it's samples in a frame, exactly what `_trajPolylineSVG` eats). DEPLOY bound to `nrho-nominal` instantiates the vehicle on it (V2 anchor from seedState at the event time + leg propagation). Orbit inspector on a propagated orbit: read-only card (name, body, period, perilune/apolune from samples) with 'propagated orbit — parameters fixed' note, no sliders.
- **U4 — acceptance:** gate (609+): corrector convergence on the pinned seed (closure < 500 km, period in 6.2–6.9 d band); sample loop closes; DEPLOY-on-NRHO produces a valid timeline. Browser: catalog shows Gateway NRHO resolvable; a deployed station renders the halo loop around the Moon (screenshot/DOM evidence); budget/report sane.

## 15. PHASE 5 SPEC — reach it (drafted 2026-07-14)

**Goal:** a vehicle can actually GET to the NRHO: a solved LEO → NRHO transfer (Artemis-class direct: TLI-like departure, ballistic 3-body coast, insertion burn at perilune), authored as normal V2 events. **Explicitly 5a; rendezvous PHASE-MATCHING (arriving where Gateway IS) and multi-vehicle timing are 5b, deferred** — 5a delivers co-orbital arrival (same orbit, unsynchronized phase), which is the honest bounded slice.

- **V1 — transfer solver (565):** `physSolveNrhoTransfer(fromOrbit, nrhoRefId, tDepart, ctx)` — seed a TLI-class departure (existing `physShootLegAim` machinery toward the Moon), target = the NRHO's perilune state at arrival (from U2's propagated samples at the arrival epoch); differential-correct the departure Δv (2–3 DOF) to hit a perilune-proximity + velocity-match window; insertion burn = velocity difference at closest approach to the target sample (reuses the Phase-2 arrival-burn construction). Converged result: departure MNODE + insertion arrival burn on the leg, TOF ~4.5–6 d. Fail cleanly (converged:false + note) — never loop.
- **V2 — authoring:** the node-map bridge / launch planner offers 'NRHO (Gateway)' as a destination when the catalog entry is seeded: authors LEO dwell → trans-lunar edge (burns named TLI / 'NRHO insertion') → NRHO dwell bound to the ref. `devSeedGatewayMission({force})` dev-seed for verification.
- **V3 — acceptance:** gate: solver convergence from the canonical LEO (converged, miss < 2,000 km at perilune window, dv totals in a sane 3.0–3.5 km/s band pinned loosely); browser: the seeded Gateway mission renders end-to-end (launch, transfer, halo), budget/report/checks sane, node map shows LEO → NRHO with named chips. Documented honestly in MATH.md §7t: this is a DIRECT transfer, not a low-energy BLT (that remains future work), and arrival is co-orbital, not phase-matched (5b).

**5a status (2026-07-14): DONE — CONVERGED via Stage 2 (perilune-quantized arrival + target-direction RAAN + mid-course correction). See MATH.md §7t (critiques 62-66) for full evidence, including the honest history of the first failed attempt.**
- V1: `physSolveNrhoTransfer` implemented in 565. Converged construction: arrival epochs quantized to the NRHO's perilune crossings, RAAN solved against the target sample's own Earth-frame direction (Stage 1), 2/3-DOF Newton on the fixed-epoch endpoint, plus a Stage-2 MCC burn (free small Δv at TLI+0.5·TOF, 3-DOF Newton — the architecture real missions fly) when the fixed-|Δv| TLI alone misses the window. Iteration-capped, never loops, fails cleanly. (First attempt — free-floating TOF, Moon-center RAAN — stalled at ~14,000-25,000 km; kept as history in MATH.md §7t.65.)
- **Measured converged numbers** (canonical LEO 185x185 @28.5, t0=0, TLI 3,143 m/s): miss **378 km** (< 2,000 km window), TOF **4.71 d**, MCC **183 m/s**, insertion **996 m/s**, total **≈ 4.32 km/s** (4.3-5.1 across departure epochs, all converged).
- V2: 430's `nrho` node (bound via `orbitRefId:'nrho-nominal'`) + 565's `nrhoRefAfter` lookahead route a LEO→TLC→NRHO authored chain through the dedicated solver; edge chips read TLI / "NRHO insertion" (570). `devSeedGatewayMission({force:true})` (573) seeds the full reference mission. Known accounting limit: the MCC is on the leg record (physics/display) but not yet an authored budget-charged event (MATH.md §7t.66).
- V3: gate (`tests/math.test.js`, "5a:" block, 9 assertions) pins converged:true, miss < 2,000 km, TLI-class TOF band, `|Δv|` accounting parity, and the approximate total-dv band; browser-verified end-to-end via the dev seed.
- 5b (rendezvous phase-matching, multi-vehicle timing) remains explicitly OPEN, as scoped.

### 5b — rendezvous phase-matching + multi-vehicle timing (spec drafted 2026-07-15, for review)

**Goal:** arrive where the station IS, not just on its orbit. Today a RENDEZVOUS event succeeds co-orbitally regardless of phase; 5b makes phase a real, priced, visible quantity — the last dishonesty in the Gateway architecture.

**R1 — phase truth (measurement + display, no new solving).** Define the phase error between two vehicles sharing a propagated ref (or any orbit): time-along-track offset Δt_phase (seconds ahead/behind along the loop; for the NRHO, from `refOrbitPropagatedStateAt`'s wrapped clock) and its distance equivalent at the current point. Surface it: HUD chips for co-orbital vehicles show "Δphase 2h 41m · 8,910 km"; the RENDEZVOUS event card shows the phase error AT its MET and goes readiness-AMBER when it exceeds a documented capture window (~minutes/tens of km, constant with rationale in MATH.md) instead of silently succeeding. Gate: phase math pinned on synthetic two-vehicle cases (same orbit, known offsets).

**R2 — phase-matched arrival (extend the 5a solver).** `physSolveNrhoTransfer` gains `targetPhase` mode: instead of quantizing arrival to ANY perilune crossing, pick the departure epoch + TOF combination (scan the same perilune-crossing lattice 5a already builds) whose arrival minimizes phase error to the TARGET VEHICLE's actual predicted position (station state = its own anchor timeline at t_arr, not the raw ref seed). Expose the trade honestly: the solver returns the 3-4 nearest lattice options {tDepart, TOF, dv_total, Δphase_at_arrival}; the launch-window/edge UI lets the user pick (default = min phase error within the dv band). This is a SELECTION layer on 5a's machinery — no new dynamics, fails cleanly to 5a co-orbital behavior with a note when no option enters the capture window.

**R3 — terminal phasing burns (the classic two-impulse phasing).** When arrival phase error is nonzero but co-orbital (the normal case, incl. after R2's best effort): author-able "PHASING" maneuver pair (drop to/raise from a phasing orbit with period offset ΔP chosen so N revs absorb Δt_phase; standard `ΔP = Δt_phase/N` construction, N user-pickable 1-5). For the NRHO, period comes from the catalog; the burn pair prices via the existing rocket-eq/impulsive lane at the ref's perilune. The RENDEZVOUS event then requires phase-in-window (R1's check) — the workflow becomes: transfer (R2, coarse) → phasing (R3, fine) → RENDEZVOUS (now honest). Gate: ΔP construction round-trips (chosen ΔP absorbs the synthetic offset within tolerance); browser: Gateway seed extended to a full honest rendezvous chain.

**R4 — multi-vehicle timing surface (bounded) — DONE 2026-07-15, MATH.md §7ag.** The timeline dock gains a per-vehicle epoch alignment affordance: RENDEZVOUS/DOCK events draw a vertical link between the two lanes at their MET, red/amber-tinted when R1's phase check fails. NO new event kinds, no scheduling solver — display + the R1 check only (full multi-vehicle choreography stays a §16 design pass).

**Sequencing — 5b COMPLETE (R1-R4 all landed 2026-07-15).** R1 (pure math + display, small) → R2 (solver selection layer) → R3 (phasing events) → R4 (dock link). Each landed separately, gate+browser-verified; gate 826 (unchanged by R4 — UI-only). Risks: R2's lattice may be coarse for tight windows (documented, R3 absorbs the residual — that's why R3 exists); phase near a propagated ref's wrap point needs the rotating-frame wrap semantics (N2) — use the wrapped clock, never inertial position diffs. Full multi-vehicle choreography (scheduling, launch-window coordination across vehicles) remains a §16 design-pass item, explicitly deferred by R4's own scope line.

---

## 16. v3.0 BACKLOG (user, 2026-07-14 — running list, capture only, NOT next-up)

- **Lunar Lagrange-point orbits (EML1/EML2 halos, Lyapunov)** as propagated catalog entries — same corrector machinery as the NRHO (§14). Caveat carried in from §14's finding: EML2 sits at the edge of the Moon's SOI in the patched model, so EML2-family orbits will be "class" approximations like the 9:2; EML1 should converge cleanly. Widely used in mission design — high value.
- **NASA Eyes visual direction**: better textures (current JPEGs read flat), starfield background, softer body lighting/atmosphere glow, possibly smoother fly-to easing. Incremental polish track, not one big rewrite.
- **Dev GitHub Pages**: publish the dev branch's build to a separate Pages URL (workflow or dev-path commit) so flight-testing never touches the released page. Small, standalone.
- **Multi-vehicle handling + multiple launch sites**: the Gateway seed already strains single-vehicle assumptions (one launch site strip, one active-vehicle framing in several panels). Needs a design pass; adjacent to 5b (rendezvous/multi-vehicle timing).
- **Plane-aware Band view**: orbits out-of-plane by more than ~5-10° get their own band lane (band zones currently only know body groups; plane identity is cheap to answer post-V2). "Make Band view a lot more intelligent."
- **Mock ascent path defect — FIXED 2026-07-14** (same day as capture): the schematic surface→orbit bezier drew a misleading ~45° climb into an orbit it visibly didn't intersect (site lat ≠ orbit plane at the drawn instant). Per user direction: the LAUNCH event marker now maps onto the ORBIT (insertion point); the site marker remains as a cosmetic surface highlight; the connecting bezier is removed rather than faked.

---

## 17. N-SERIES SPEC — true n-body, SOI demoted, switchable frames (drafted 2026-07-14)

**Thesis (user):** this project exists to outline 21st-century mission architectures — NRHO, L-point staging, ballistic lunar transfers, WSB trajectories — which live exactly in the dynamical regime that SOI-patching amputates. The dynamics are ALREADY n-body (`physAccel` sums a real body list); what must die is the SOI machinery's third job: force-model truncation and SOI-as-terminal-condition. SOIs become visual/organizational only.

### N1 — SOI demoted to bookkeeping (the honesty fix; everything else stands on it) — **DONE 2026-07-14, MATH.md §7u**
> **Evidence / deviation from the draft:** the audit MEASURED that `physPropagateSegment` never truncated `ctx.bodies` at a handoff — the R3 `frameBodies` restriction only scopes the frame-transition scan, so `ctx.truncateToFrame` was unnecessary (the gizmo's cheap tier keeps its caller-level `[node.body]` list as the explicit truncation opt-in). The real truncation lived in the CALLERS' ad-hoc body lists, now all routed through `physBodySetFor` (565). Continuity gate-proof: 6-day lunar-flyby TLC, normal handoffs vs forced single-frame (`opts.singleFrame`, new) — residual **80.7 km / 0.22 m/s**, identical before/after (dt-ladder discretization, same 413 steps both paths), pinned < 500 km / < 5 m/s in the gate. SOI-termed acceptance re-phrased as explicit km literals (`PHYS_ENCOUNTER_SCALE_KM`, gate-pinned to physSoiRadius < 1e-6 km). Goldens: free return / NRHO closure / 5a **all unchanged — zero re-pins** (contextual mode reproduces the old lists verbatim; continuity already existed). Gate 627 → 640 assertions.
- `physPropagateSegment` integrates the FULL `ctx.bodies` list at every step regardless of the current frame center. Frame handoffs (physFrameOf crossings) become pure coordinate re-centering + sample tags — never a physics change. The frame-restricted body list (the R3-era `frameBodies` perf optimization) becomes an explicit opt-in (`ctx.truncateToFrame: true`) used ONLY by the gizmo's cheap preview tier; solvers and committed legs never truncate.
- Solver/leg acceptance windows currently phrased in SOI terms (physShootLegAim's acceptance min(SOI/3, 25000km), corridor-exit detection, escape classification) are re-phrased in explicit distance/energy terms with the same numeric values where they were sensible (mechanical; audit each use of `physSoiRadius` — it survives ONLY in rendering/labels/LOD and as the frame-selection heuristic).
- **Gate proof of the whole phase**: propagate one trajectory across an SOI boundary under the old handoff path and under a forced single-frame path — the physical states must agree to float noise at matched epochs (the boundary means nothing dynamically). Plus: existing goldens (free return, NRHO closure, 5a transfer) re-verified; expected small numeric drifts re-pinned per D6 with documented cause ("dynamics gained continuity").

### N1b — Fidelity setting (user decision 2026-07-14: contextual default, user-switchable) — **DONE 2026-07-14, MATH.md §7u**
> **Evidence:** `physBodySetFor(ctx, fidelity)` + `physFidelity`/`physSetFidelity` (565, caches cleared on change); ⚙ Settings modal (`195-settings.js` + `#modal-settings`, header button next to Themes); `physicsFidelity` in the session blob (455, applied before program restore); report header stamp (577). Measured contextual-vs-full delta on the TLC: **9.5 m** over 6 days (Sun already contextual); cost ~2.6×. Gate pins both modes' body sets + the mode round-trip.
- **New Settings surface** (the app has none; add a small `openModal`-based Settings modal reachable from the header, seeded with exactly ONE setting so it doesn't become a junk drawer): **Physics fidelity** = `contextual` (default: Sun + frame-relevant bodies + mission-target bodies — today's behavior made honest) | `full system` (Sun + all 8 planets + Moon + Titan in every propagation; ~2-3× propagation cost, still ms-scale for cislunar, meaningful for multi-year interplanetary).
- Persisted via `_buildSessionObject` (455 shared-format rule). Missions RECOMPUTE on change (the setting is a physics input; the D6 note applies — switching fidelity may legitimately move numbers, which is the point). The active fidelity is stamped into the mission report header for honesty.
- Body-set resolution becomes ONE function (`physBodySetFor(ctx, fidelity)`) so "contextual" has a single auditable definition.

### N2 — cash the unlocked checks — **DONE 2026-07-14, MATH.md §7v**
> **Evidence (two rounds same day — the first delivery's half-period-corrected seed closed at 46,185 km and was superseded within hours by the method that worked):** the corrector that converged is APOLUNE-seeded (perilune shooting is hopelessly ill-conditioned) derivative-free pattern search minimizing closure in the Earth-Moon ROTATING frame (single-shooting FD-Newton stalls at iteration 0 on every case — dt-ladder discretization makes the Jacobians noise-dominated; MATH.md critique 62), with the period LOCKED to the literal 9:2 resonance. Shipped seeds (tests/corrector_harness.js, re-runnable): **true 9:2 NRHO** P=566,987.3 s (6.5624 d exact), rotating-frame closure **345.6 km / 28.33 m/s — tighter than the retired seed's 468.6 km**, perilune/apolune 5,544/71,203 km (real Gateway-class), 3-rev bounded; **EML1 Lyapunov** 47.4 km closure, 12.42 d; **EML1 halo** 672.5 km (basin floor), 11.11 d; **EML2 halo** 1,459.7 km (basin floor), 13.71 d, 3-rev ESCAPE documented (L2 halos e-fold in days). Critique 61 fully retired. The true 9:2's apolune crosses the Moon-SOI bookkeeping boundary every rev — the first real consumer of N1's continuity (catalog propagations use `singleFrame:true`). Semantic fix this forced: **phase-wrapping moved to the rotating frame** (`_refEmRotBasis/_refToRot/_refFromRot` in 425) — a rotating-frame-periodic orbit "misses itself" by 27,241 km inertially per rev (frame turns ~86.5°; correct physics), so `refOrbitPropagatedStateAt` wraps in rotating coordinates and reconstructs at the true epoch (doubles as idealized station-keeping for the unstable halos — no consumer propagates a seed past one period), and `refOrbitSamplePropagated` re-bases the drawn ring to the seed epoch (closes visually; live frame-aware drawing is N3). 5a re-solves against the new target (gate: converged, in-band; insertion legitimately costlier — dv band re-pinned 3.0–5.5 → 3.0–8.0 km/s with cause). BLT/WSB substrate verified: Sun flips an identical seed bound→unbound (−0.363 → +0.346 km²/s²), 4.68M km apogee reached, no truncation (gate-pinned). Gate 640 → 659.
- **True 9:2 NRHO** — DONE: seed replaced (id kept), "9:2-class" label retired, critique 61 retired.
- **EML1/EML2 halos + Lyapunov** — DONE: `eml1-lyapunov`, `eml1-halo-s`, `eml2-halo-s` catalog entries with per-entry measured closure pins; read-only inspector cards fall out of the existing `_oiPropagatedCardHTML` path.
- **BLT substrate** — DONE: gate-pinned measurement (tests/math.test.js "N2 BLT" block), 8-phase-angle sweep in the harness; a real BLT solver remains future work (5b-adjacent).

### N3 — switchable reference frames (Principia-style; pure rendering) — **DONE 2026-07-14, MATH.md §7x**
- Frame selector in the trajectory toolbar: **body-centered inertial** (today) · **body-fixed** (rotates with the body's spin — launch-site work) · **Earth-Moon rotating** (the Moon is pinned; NRHOs/L-points render as stationary loops) · **Sun-Earth rotating** (WSB/L1/L2 work). Agreed initial set (user 2026-07-14).
- Implementation seam: the ONE floating-origin + projection point in `_trajWorldSVG` gains a pre-projection world→frame transform. THE key detail: trajectory polylines/rings transform EACH SAMPLE BY ITS OWN EPOCH's rotation angle (samples carry `t`), which is what makes a rotating-frame trajectory render as the physically meaningful loop instead of spaghetti. Bodies/markers transform at viewT. Camera az composes with the frame rotation (document the convention).
- Occlusion/LOD/labels ride the transformed positions automatically (they consume projected output). Spin-textured globes in body-fixed frame: the globe stops rotating on screen (the frame rotates with it) — verify, it falls out of the same transform.
- Frame choice is per-mission transient view state (like camera), not persisted in the log; session-persist alongside the camera if cheap.

> **Evidence (2026-07-14):** implemented as `q(t) = B(t)ᵀ·pRel(t)` fed directly to the camera projector (574's `_trajFrameTransform`/`_trajFrameBasisAt`), a documented simplification of this spec's own `B(viewT)·B(t)ᵀ·pRel(t)` wording — the literal formula makes every `t=viewT` point (i.e. every body glyph) an algebraic no-op for ANY frame (`B(viewT)·B(viewT)ᵀ=I` always), which would make the whole feature visually inert; dropping the outer re-expansion is what actually pins the Moon and closes the NRHO ring (full derivation + the deviation rationale: MATH.md §7x). Four frame kinds shipped exactly as scoped (`inertial`/`body-fixed`/`earth-moon-rotating`/`sun-earth-rotating`), selector live in the trajectory toolbar (`<select class="traj-frame-select">`, per-mission `_trajFrameByMission`, NOT session-persisted — matches the camera, which 455-autosave.js also doesn't persist). Seams: polyline samples transform at their own `s.t` (`_trajPolylineSVG`); propagated rings gained a raw un-rebased sample source (`refOrbitSamplePropagatedRaw`, 425) so non-inertial frames close the NRHO/libration loops LIVE at any viewT (inertial keeps the existing seed-epoch-frozen `refOrbitSamplePropagated` path untouched — zero regression, critique 64 retired only for the frames that opt in); body-fixed globes stop spinning for free (no special-casing — the same spin baked into surface geometry algebraically cancels against the frame's own basis, verified via the raster cache key going constant under scrub). `_refEmRotBasis` (425, N2) generalized to `_refRotBasisPair(primary,secondary,t)` so sun-earth-rotating reuses the identical math — zero behavior change for the existing N2 wrap/harness callers. Gate: 8 new pure-math assertions (679 → 687) — inertial exact-identity, EM-frame Moon-direction fixed to <1e-6 rad across a week with magnitude in the real 350k-410k km band, magnitude preservation (the honest replacement for the spec's literal "t===viewT ⇒ identity", which only holds for inertial — see MATH.md §7x), basis orthonormality across epochs, and the NRHO's raw loop closing to <500 km live in the EM frame (vs. its own ~27,000+ km raw inertial gap, confirming the transform — not the propagation — is what closes it). Browser-verified via `devSeedGatewayMission({force:true})`: EM-rotating+anchor-Moon NRHO ring renders closed/stationary across a viewT scrub (visibly rotates in inertial); Moon pins to a fixed screen direction in EM-rotating+anchor-Earth; body-fixed Earth globe stops turning (raster cache key constant); inertial default unchanged (scene string diff empty at matched cam/viewT); frame selector present per-mission, survives view toggle; zero console errors. Two new critiques (70, 71): body-fixed only freezes the anchor's OWN surface features (not other bodies' apparent spin-driven sky motion — out of scope, launch-site work was the agreed use case); the analytic Keplerian reference rings stay a viewT snapshot (no per-point epoch to thread) rather than a live frame-relative shape.

### Sequencing & risk
N1 (+N1b settings) first — smallest, everything stands on it; gate-proof of dynamical continuity before anything consumes it. N2 second (mostly re-running existing machinery in the freed model; re-pins per D6). N3 last (rendering-only; the per-sample-epoch transform is the one genuinely new renderer concept). Perf watch: full-system fidelity on multi-year legs (profile before optimizing; the dt ladder already coarsens far from bodies).

---

## 18. V-SERIES — NASA-Eyes visual direction (user go 2026-07-14; reference https://eyes.nasa.gov/apps/solar-system/#/earth)

**Direction (from the reference):** black space with a subtle starfield; bodies read as LIT OBJECTS (day side bright, night side dark, soft atmosphere rim glow) instead of flat texture discs; orbit/trajectory lines thin and desaturated (near-white, low alpha) with the ACTIVE trajectory carrying the color; labels small, uppercase, dimmed, with leader ticks; camera moves eased. Incremental polish track — same renderer (SVG + canvas raster globes), NOT a WebGL rewrite.

### V1 — space, light, and line language (one pass)
- **Starfield**: screen-space layer (does NOT zoom/pan with the world — it is at infinity) behind the scene: a few hundred deterministic points (seeded PRNG so it never twinkles between frames), 2-3 size/alpha tiers, plus a handful of slightly-larger tinted stars. Neutral white-alpha = allowed non-theme scrim; regenerate on resize only. Cheap: one static canvas or SVG group built once.
- **Sun-driven terminator shading**: the Sun's direction at viewT is KNOWN (ephemeris). Darken each raster globe's night hemisphere (multiply-shade in the existing `_trajRasterGlobe` inverse-orthographic pass — it already iterates pixels; add a lambert term from the sun vector, floor ~0.15 so the night side stays readable, soft terminator band). This is the single highest-impact change. Verify the terminator moves with viewT scrubbing.
- **Atmosphere rim glow**: per-body radial-gradient halo just outside the limb (Earth blue, Mars dusty orange, Venus pale yellow, Titan orange haze; airless bodies NONE — Moon/Mercury get only a faint white limb). Data-layer colors (PROG_BODY_COLORS-adjacent, new PROG_BODY_ATMOSPHERE constant next to it) — exempt from chrome theming like body colors.
- **Line + label restyle**: non-active orbit rings drop to thin desaturated near-white (low alpha); the selected/active trajectory keeps its vehicle/zone color at full weight; labels move to small uppercase tracking-spaced style with a short leader tick (match the reference), dimmed until hover/selection. Respect existing LOD/occlusion.
- **Fly-to easing**: camera anchor changes ease (cubic in-out, ~600ms) instead of snapping — compose with `_trajApplyCam`; skip easing during drag.
- **Texture pre-warm (the "loading" answer)**: the trajectory view's first-open lag is canvas texture rasterization, not network. Pre-rasterize the fidelity-ladder tiers for the bodies present in the mission during an idle callback shortly after app init (requestIdleCallback, chunked one body per idle slice) so trajectory mode opens warm. NO dedicated loading screen — with pre-warm + the existing cheap-disc-then-swap ladder there is nothing left to wait for; a loading screen would advertise a delay we can remove instead (decision 2026-07-14).

### V2+ (captured, not next-up)
- Smoother fly-to between bodies (zoom-out-arc-zoom-in path like Eyes, not straight lerp), body selection glow/pulse, richer starfield (density map from a real catalog), day/night city lights on Earth's night side (needs a second texture), rings for Saturn — **rings DONE 2026-07-14** (PROG_BODY_RINGS, occlusion-correct front/back split).

---

## 19. E-SERIES — electric propulsion / low-thrust trajectories (user go 2026-07-14; spec draft for review)

**Thesis (user):** very-low-thrust spiraling over months — electric propulsion mission architectures (LEO→GEO raises, escape spirals, interplanetary SEP cruises). Feasible BECAUSE of the N-series: one integrator entry point (`physPropagateSegment`, N1-honest), frame handoffs as pure bookkeeping, fidelity setting, and N3 rotating frames (the natural way to LOOK at a spiral).

**Core interaction decision (user, 2026-07-14): expensive legs are computed on EXPLICIT user action ("Compute trajectory" button in the event card) with a real progress bar — never inside the synchronous replay.** Rationale: `missionRecompute` replays the whole log on every mutation; a multi-second integration inside it would hang every unrelated edit. The full contract:
- **Instant estimate, always**: authoring a low-thrust event immediately prices it analytically (Edelbaum) — ΔV, TOF, prop — labeled `est.` Budgets/node map never blank; the un-computed trajectory renders as an honest schematic (dashed spiral glyph), not a fake integrated path.
- **Compute button + progress**: chunked integration (a few simulated days per rAF/idle slice — progress through a spiral is near-linear in simulated time, so the bar and ETA are honest), UI stays responsive, CANCELLABLE mid-solve (partial results discarded).
- **Signature-keyed cache + STALE badge**: computed result cached in the physics side-table (NEVER on `m` — autosave-leak rule) keyed by a hash of its actual inputs (initial state, engine params, mass, duration, steering law, coast windows, fidelity mode). Replays consume the cache for free. ANY upstream edit that changes the signature flips the leg to a visible STALE badge; budget falls back to `est.` until recomputed. Nothing ever silently renders a trajectory that no longer matches the plan (D6 honesty, new lane). Not session-persisted: a restored session shows STALE; one click rebuilds.

### E1 — physics substrate
- `physPropagateSegment` gains an OPT-IN thrust term: `ctx.thrust = { thrust_N, isp_s, m0_kg, law, coastWindows? }` — acceleration `+ (T/m)·û(law)`, coupled mass ODE `ṁ = −T/(Isp·g₀)`; state extends to (r, v, m). **Ballistic paths byte-identical when ctx.thrust is absent** (gate-pinned: propagate a golden case with and without the code path present).
- Steering laws v1: `prograde` / `retrograde` (tangential — the spiral workhorses); law is a function û(r, v, t) so later laws (normal for plane change, Q-law) slot in.
- **Step control**: a thrusting arc must resolve every rev — cap dt ≤ period(local)/40 while thrusting, composed with the existing dt ladder (which handles distance). A 6-month LEO spiral ≈ 10⁵–10⁶ steps — seconds-scale, hence the compute-button contract above.
- **Gate**: pin against analytic truth — Edelbaum circular-to-circular ΔV (`Δv = √(v₀² + v₁² − 2v₀v₁cos(π/2·Δi))`, and the planar `|v₀−v₁|` case) vs the integrated spiral's actual ΔV within a documented band (~1-3%: Edelbaum assumes constant acceleration, ours depletes mass); mass depletion vs rocket equation exactly; a short spiral's energy gain vs `∫ a_T·v dt`.
- MATH.md §7y: model, steering laws, step policy, Edelbaum derivation + its assumptions as critiques.

### E2 — vehicle + event model — **DONE 2026-07-14, MATH.md §7z**
> **Evidence:** new `LOWTHRUST` log type (not an MNODE mode — §7z documents why the impulsive/node substrate doesn't fit) in a new pure module 568-lowthrust.js + 570 card/572 readiness wiring. Est. lane replays synchronously (rocket-eq dv over authored duration, prop via progBurnPropellant, orbit via a tangential-impulse vis-viva SMA update — critique 75); computed lane = "Compute trajectory" button, 2-day rAF chunks, cancellable, cached in `_ltComputedByMission` keyed by an FNV-1a signature over {r,v,m0,thrust,isp,throttle,law,duration,fidelity}; signature mismatch → STALE badge, budget falls back to est. Vehicle model: `XENON_EP` + (finally) `MMH/NTO` registered in PROG_PROPELLANT_TYPES; `ep_thrust_N`/`ep_isp_s` on SC stages, editor-surfaced for EP only. **Measured (orchestrator-reproduced independently):** LLO 100 km, 0.29 N/3,100 s NEXT-class, 30 d prograde: est 157.07 m/s / computed 157.886 m/s (Δ 0.816 m/s, inside max(1%, 5 m/s)); prop 24.726 kg identical both lanes; duration edit → STALE + fallback to the new est. Gate 703 → 736. Honest gaps (critiques 76-79): 566 v2-shadow blind to LOWTHRUST anchors (V1 accounting correct), compute pins singleFrame (SOI-crossing escape spirals unhandled), no coast windows v1, samples cap ~2000 (aliasing until E3).
- **Electric stages**: stage-library entries with realistic SEP numbers (Isp 1,500–4,000 s, thrust mN–N, power fields optional/cosmetic v1). Register a proper electric propType in PROG_PROPELLANT_TYPES (fixes the adjacent MMH/NTO backlog item's pattern while there).
- **New event kind LOWTHRUST** (or MNODE mode `'lowthrust'` — decide against the MNODE substrate's assumptions and document): authored fields = duration, throttle, steering law, coast windows; engine/mass from the vehicle's live stage state at the event, like every burn.
- **Accounting (the identity rule, new lane)**: planning/required ΔV = Edelbaum (the node-map/pricing side); delivered ΔV = `∫(T/m)dt` from the computed leg; prop = integrated mass depletion, reconciled against `progBurnPropellant` within a documented tolerance (D6). The solved-burn magnitude rule (`progNmComputeEdgeDv` only) explicitly does NOT govern low-thrust legs — document the boundary.
- Replay/undo/autosave: the log entry stores AUTHORED intent only (params + signature of last computed result); recompute stamps `est.`/computed/STALE state; undo snapshots stay tiny.

### E3 — planning + rendering — **DONE 2026-07-15, MATH.md §7aa**
> **Evidence:** full-form Edelbaum `ltEdelbaumFullDv` + TOF est. `ltEdelbaumTofEst` + rev-boundary resampler `ltResampleSpiralRevs` (568, gate-pinned: Δi=0 → |v₀−v₁| exact, Δi=90° → √(v₀²+v₁²), Δi=180° → v₀+v₁, monotonic in Δi; synthetic 40-rev spiral resample recovers rev count to 5%, endpoints exact, middle → ~1/rev). **Dual pricing wired** (PARALLEL est. only — progNmComputeEdgeDv untouched): node-map edge tooltip + Add-Event maneuver form show "low-thrust est.: X m/s · TOF Y d" when the acting vehicle's active stage is EP-capable, with an "Add as Low-Thrust instead" insert pre-filled with the Edelbaum TOF (browser-measured: LEO 28.5°→GEO 0°, Δi 28.5° → 5,302 m/s · 130.6 d on a 2 N/3,100 s/4.8 t stage; hand-check √(7.79²+3.07²−2·7.79·3.07·cos 28.5°)=5.29 km/s ✓). **Launch planner**: "spiral to escape est.: ≈v_circ(parking)" line for EP vehicles (7,797 m/s at 185 km · TOF ~185 d measured). **Spiral LOD render** (574 `_trajLowThrustSVG`): computed legs store a head/mid/tail rev-LOD split (512 samples/2-day chunk → resampled once at completion); head/tail draw as real polylines through `_trajPolylineSVG` (per-sample epochs → N3 frames apply automatically, EM-rotating verified legible), the dense middle as a translucent min/max-radius annulus (built with `_trajProj3` at viewT, NOT Saturn's ring-band path — that bypasses the N3 transform); STALE/uncomputed legs draw an honest dashed 4-rev log-spiral glyph in var(--text-dim) (browser-verified: 30 d LEO spiral, 2 N → 394 revs, computed 1,141 m/s vs est 1,100 m/s (+3.7% spiral loss); duration edit → STALE → schematic appears, recompute → LOD returns). **Timeline dock** (5747): LOWTHRUST renders as a duration-wide accent2 low-alpha span + the ▲ start marker. Gate 757 → 757 (E3 math pins landed in the prior pass's 736→757). Zero console errors.
- **Node-map pricing**: Edelbaum edge pricing for low-thrust-capable vehicles (LEO→GEO the canonical case); "Plan for destination" gains a low-thrust option when the vehicle's active stage is electric (TOF from Δv/a₀, honest `est.`).
- **Spiral rendering**: per-rev or curvature-adaptive sampling (a 2,600-rev spiral through 512 uniform samples is aliasing garbage); LOD: first/last N revs as real polylines, dense middle as a translucent annulus/envelope band; N3 rotating frames make the spiral legible (showcase: LEO→GEO raise in body-fixed? no — inertial + EM frame for lunar spirals; document which frame shows what).
- Timeline dock: a low-thrust leg is a months-long BAND, not a tick — render as a span with the thrust duty cycle.

### E4 — low-thrust planning gizmo — **DONE 2026-07-15, MATH.md §7ab**
> **Evidence:** duration-drag handle (new module 5748-lowthrust-gizmo.js, ~330 lines) emitted by 574's `_trajLowThrustSVG` only for the selected/expanded LOWTHRUST event (schematic-glyph endpoint when uncomputed/stale, LOD-tail endpoint when computed); drag axis captured off the handle's own `data-tx/data-ty` DOM attrs at mousedown (5745's pattern), 5 px/day sensitivity, week-boundary tick marks drawn into the shared `svg.traj-overlay` layer, Escape cancels. Est.-lane-ONLY during drag (`ltEstimateLeg`/`ltApplyDvToCircularAlt`/`ltEdelbaumTofEst` — never `physPropagateSegment`); the uncomputed schematic's dashed `<path d>` is rewritten live in place via a 2-point-correspondence affine recovery (no access to 574's ox/oy/zoom closure from the gizmo module); a computed/LOD leg's numeric readouts update live but its polyline shape does not re-draw during the drag (documented gap, critique 90). Release authors `duration_s` through `m.log → missionRecompute → missionRenderDetail` (the ONE sanctioned mutation — the gizmo never touches `_ltComputedByMission`/`e._ltState` itself); the existing signature machinery flips any cached computed leg STALE automatically. What-if readouts (duration/Δv/prop/prop-remaining/DEPLETED tag/arrival date via `progMissionTimeToDate`) mount on the event card's `ltg-card-readout-*` slot. **Target-orbit mode**: `ltInverseEdelbaumDuration` (568) is the EXACT algebraic inverse of `ltApplyDvToCircularAlt` (`v1 = √(2μ/r0 − μ/r1)`, not the naive `√(μ/r1)` planar triangle — chosen so the round-trip and the drag gizmo's own live redraw agree on what "resulting altitude" means), wired to a "Target altitude…" input + Solve button on the card; unreachable targets show a `var(--warn)` badge with `ltMaxAchievableAlt`'s honest max-achievable altitude (full-tank rocket-eq dv, cross-checked against `ltEstimateLeg`'s own capped-duration output). **Critique 86 closed**: `ltSignature` gained an 8th field, `metStart_s` (60s-bucket rounded — gate-pinned: sub-60s jitter does NOT flip the signature, any real upstream timing edit does), threaded from 570's `metClock`. **Gate 757 → 772** (15 new assertions: inverse-Edelbaum round-trip exact to 1e-6, max-achievable-altitude cross-check, 8-field signature sensitivity + 60s-jitter immunity). Honest gaps (critiques 88-91): the inverse solve inherits `ltApplyDvToCircularAlt`'s single-tangential-impulse-at-r0 approximation (not a real spiral); `ltMaxAchievableAlt` reads as "no gain" rather than "can reach escape" for wide-margin stages whose full-tank dv drives the est-orbit approximation hyperbolic; only the un-computed schematic redraws live during a drag (a computed/LOD leg's shape does not); the affine-recovery redraw has one documented (vanishingly rare, self-healing) degenerate-input skip.
- The impulsive-burn gizmo (5745) is useless for a months-long arc — a low-thrust leg's authorable knobs are duration/throttle/law, not a dv vector at a point. E4 gives LOWTHRUST events their own planning affordance so the user can USE them for planning, not just author-and-hope:
- **Drag-the-duration on the trajectory**: selecting a LOWTHRUST event shows the est. spiral endpoint as a handle; dragging it along the trajectory direction scales `duration_s` live (est.-lane numbers update in real time — cheap), with tick marks at week boundaries. Release = author the new duration (STALE flow applies to any computed cache, automatically).
- **Target-orbit mode**: "reach altitude X" — inverse-Edelbaum: given target SMA, solve duration/prop from the est. lane (closed form); shows reachability honestly (prop cap → max achievable altitude badge when the target exceeds it).
- **What-if readouts on the card while dragging**: dv, prop, TOF, arrival date (epoch-aware per the s19 calendar work), prop-remaining after.
- Scope guard: est.-lane math ONLY while dragging (never integrate in a drag loop); the Compute button remains the truth path. The gizmo composes with N3 frames (drag in whatever frame is displayed — the handle lives on the rendered polyline/schematic).

### Sequencing & risk
E1 first (pure physics + gate, no UI); E2 (event kind + accounting rules — the design-heavy step); E3 (pricing + rendering); E4 last (planning gizmo on top of the whole stack). Risks: perf (bounded by the compute-button contract), rendering aliasing (E3's whole job), accounting drift between Edelbaum est. and integrated truth (bounded + badged, never silent). Multi-rev step control is the one place the dt ladder gets a new rule — gate it hard.

---

## 20. OBLIQUITY — real spin axes (user-diagnosed 2026-07-15; spec for review)

**The reported problem (user, verbatim intent):** "the Moon's plane is offset relative to its orbit, not to the equatorial" — plane-matching a KSC launch to the Moon reads UNREACHABLE (5.15 deg target vs 28.5 deg site latitude) when real KSC launches reach the Moon routinely. **Root cause:** the world frame is the ecliptic and no body has an axial tilt — Earth's equator coincides with the ecliptic in the model, so launch inclinations (equator-referenced by convention and by the site-latitude constraint) are being compared against ecliptic-frame planes. Reality: Earth's equator is tilted 23.44 deg, making the Moon's inclination TO THE EQUATOR oscillate ~18-28.6 deg — reachable from 28.5 N. Decision (user options weighed): tilt the bodies (honest), NOT inflate the Moon's ephemeris inclination (a ephemeris-corrupting hack).

### O1 — spin-axis model + the launch-geometry seam
- **Per-body spin axis** in the body table: obliquity to the ecliptic + node azimuth (v1: Earth 23.44/0 as the load-bearing entry; Mars 25.19, Saturn 26.73 — Saturn's RING tilt already hardcodes this value in the renderer, unify to the one table; Moon/others as data permits). One helper `physBodyPoleAt(body)` (static v1 — no precession; document).
- **Authoring convention UNCHANGED (user-facing):** launch/orbit inclinations remain EQUATOR-referenced numbers (28.5 means 28.5-from-equator, as every user expects). NEW seam: one pair of transforms equator-frame <-> world(ecliptic)-frame elements (inc/LAN through the pole rotation), applied where authored elements become physics state (the physElementsToState call sites on the program side) and inverted where physics state becomes displayed elements.
- **Plane-match/planner fixed by construction:** the Moon-plane target is computed in the EQUATOR frame (its inc-to-equator lands in the real 18-28.6 band depending on node phase) — reachable from 28.5 N most epochs; the unreachable warning stays for the epochs where it genuinely is not.
- **FROZEN ZONES UNAFFECTED:** calculate()/evalAtPayload/destOnOrbitDV (145/160) are equator-self-consistent and never touch world frames — zero change, goldens must not move.
- **D6 applies on the program side:** existing missions authored inc numbers keep their VALUES but the physics planes they map to rotate — mission-side numbers may legitimately move (transfer solves, plane penalties); re-verify the Gateway/Apollo seeds and re-pin with cause. modelVersion stays 2 (semantics refined, not a schema break).

### O2 — rendering + frames follow the same table
- Globe raster/spin (574) spins about the tilted pole (Earth visibly tilted — also a NASA-Eyes visual win); site markers/launch geometry ride it; N3 body-fixed frame basis = the real pole (its current z-axis spin assumption replaced); Saturn rings consume the table instead of their local constant.
- Gate: transform round-trips (equator<->world elements), Moon inc-to-equator in [18, 28.6] across a saros-scale epoch sweep, plane-match-from-28.5N reachable at a majority of epochs, ring-tilt table consistency; goldens 145/160 byte-stable.

### Sequencing & risk
O1 first (the seam + Earth only — fixes the reported bug), O2 with it or immediately after (visual tilt is cosmetic once the table exists). RISK: the equator<->world seam is exactly one transform, but it must be applied at EVERY program-side authored-elements boundary and NOWHERE twice — audit the physElementsToState/physStateToElements call sites first and list them in MATH.md s7ac before editing. Launch-window/RAAN math (360/415) is the subtle consumer (LAN is node-on-equator now).

### DONE (2026-07-16) — evidence

- **Pole table + seam:** `PROG_BODY_POLES`/`physBodyPoleAt` (360), `physEqBasis`/`progEqToWorldElements`/`progWorldToEqElements` (385). Full audit list (10 call sites, which were fixed vs. deliberately scope-cut, and why) in MATH.md §7al.
- **O1 fix locus:** `physShootLegAim` (565-physics-targeting.js, the Apollo-style Earth→Moon/interplanetary leg shooter) — RAAN-solve cone-axis now centered on Earth's real pole instead of world-z; ring construction rotates equator→world exactly once. `progIdealParkingOrbit`/`progMoonPlaneAt` (415, the launch-planner's advisory numbers — the actual UI-facing symptom) now equator-referenced. `v2BuildShadow` (566, Phase-1 shadow state) updated for consistency.
- **Scope cut, honest:** `565-physics-nrho.js` (Gateway/NRHO shooter), `565-physics-blt.js` (BLT), `565-physics-mission.js`'s remaining `physAimBurnState` callers, and the 5744/5745 rendering call sites (event-node/gizmo ring positions) were NOT touched this pass — audited and found to need their own frame-consistency check before a shared choke-point fix is safe (double-application risk). Full rationale in MATH.md §7al.
- **User acceptance test, measured:** `progResolvePlaneTarget('Moon', epoch, 0, 28.5)` at the program's default epoch: inc-to-equator 19.02°, penalty 9.48° (a real, single-digit dogleg) — was a ~23° gap (28.5° vs. the pre-fix ~5.15° ecliptic figure) reading unreachable on every date. 19-year saros sweep (76 samples): inc-to-equator stays in [18.30°, 28.60°] on every sample; reachable (zero penalty) on 5/76 samples, penalty capped at 10.20° (avg 5.23°) on the rest — down from the pre-fix ~23° gap on literally every date. Honest note: "majority reachable" is NOT the right bar here — exact coplanar match is a genuine once-per-nodal-cycle launch-window constraint in real cislunar planning (why Apollo/Artemis have monthly windows), not a daily-availability feature; see MATH.md §7al.
- **Seed re-verification:** `devSeedGatewayMission` — unaffected (NRHO path untouched by this pass), legs `{nrho:converged:true, arrival:converged:true}` unchanged. `devSeedApolloMission` — the LEO→TLC leg's miss distance moved from 129,221 km to 216,917 km (WORSE, >20%, reported per the spec's own instruction rather than silently re-pinned) but stays `converged:false` in BOTH cases — verified by reverting this pass's 565-physics-targeting.js edits alone and re-measuring; this leg was already non-convergent pre-fix (a pre-existing "capture stage honestly open" gap, B3/B3.1), and the equator-correct cone now honestly reports a larger true miss for the fixed-|Δv| burn at this geometry rather than a smaller, wrong-cone-axis one. No previously-converged mission-solver number moved.
- **O2:** Saturn ring tilt now sourced from `PROG_BODY_POLES` (mechanical unification, zero behavior change — gate-pinned: ring-plane normal == `physBodyPoleAt('Saturn')` exactly). Full globe-tilt rendering (spin-about-real-pole, site markers, `body-fixed` frame basis) EXPLICITLY DESCOPED — the shared rendering seam it would touch (`_trajProjectVec`/`_trajFrameBasisAt`) had a real chirality regression as recently as 2026-07-11 (§7o) and reworking it needs its own dedicated verification pass. Rationale in MATH.md §7al.
- **Gate:** 893 → 916 assertions (+23), 0 failures: 7-pair + 1 untilted-body eq↔world round-trip identity, 19-year Moon inc-to-equator saros sweep (band + reachability + penalty-cap), Saturn ring-tilt-table consistency, re-derived `progIdealParkingOrbit`/plane-contains-vInf goldens (now equator-frame, re-pinned with cause in MATH.md §7al), 145/160 (calculate()/destOnOrbitDV) untouched and still passing byte-stable.
- **Browser:** `devSeedApolloMission`/`devSeedGatewayMission` both run clean, zero console errors. Full click-through of the launch-card plane-match picker/align buttons and a visual tilted-globe screenshot were NOT captured this pass (O2 globe rendering itself was descoped — nothing new to screenshot there); the numeric acceptance test above was run live in-browser via `progResolvePlaneTarget`, not just the Node gate.

---

## 21. B-SERIES — ballistic lunar transfer solver (paper-based; user-supplied reference 2026-07-15)

**Reference:** Griesemer, Ocampo, Cooley, "Targeting Ballistic Lunar Capture Trajectories Using Periodic Orbits in the Sun-Earth CRTBP," NASA NTRS 20090016184. Read in full; method distilled here. This turns N2's "the dynamics no longer forbid BLTs" into an actual solver. Their example: dv 3.050 km/s / TOF ~99 d / captured (KEm = -0.105 km2/s2 at perilune) from a 7,200 km parking radius - roughly 1.3 km/s cheaper than our direct 5a transfer.

### B1 - STM substrate (the derivatives lesson)
- The paper rejects finite differences for chaotic arcs (OUR N2 Newton failure mode, independently confirmed) and integrates the state transition matrix alongside the trajectory. Implement opt-in `ctx.stm` on physPropagateSegment (E1s opt-in pattern): propagate Phi via Phi-dot = F(t)Phi, F = [[0,I],[dA/dr,0]], dA/dr = sum over bodies of mu(3 rr^T/r^5 - I/r^3) (paper Eq. 15) - the Jacobian of the exact physAccel sum. Ballistic byte-identity when absent (gate-pinned, as E1). Gate: Phi vs central-difference on a SHORT smooth arc (where FD is still valid), symplectic-ish sanity (det ~ 1).
- This also retro-upgrades existing solvers (5a, corrector) if we ever want Newton back - note, do not refactor them in B-series.

### B2 - f16/f-prime-16 reference family (corrector harness)
- Generate the Markellos f16 family member in the Sun-(Earth+Moon) CRTBP whose nearest perigee = the parking radius: perpendicular-crossing differential correction (the harness ALREADY does perpendicular-crossing pattern search - a 5-crossing variant; STM Newton from B1 if pattern search grinds). One-parameter family: cache/pin the family curve (initial x vs Jacobi c) offline in the harness, seed provenance like every 425 seed.
- Family selection per the paper: Moon in Sun-far quadrants at epoch -> f-prime-16, Sun-near -> f16. Arrival tf = Moons rotating-frame y-z plane crossing nearest 100 d (p1) or ~180 d (p2) from t0.

### B3 - incremental targeting (the paper staging, on our ephemeris)
- Step 1 (RTBP, Earth+Moon combined at barycenter, our mean-element ephemeris): 1 DOF |dv| prograde from the parking orbit, constraint = y-z plane crossing at tf. Seed dv = |v_ref_perigee - v_circ| from the scaled family member.
- Step 2: parking-orbit orientation (alpha,beta,gamma - ECLIPTIC-referenced; the S20 obliquity seam converts to/from user equator-referenced elements when authoring): 4 params target the y-axis point at r = EM-L2 distance.
- Step 3 (full four-body): same 4 params, inequality constraint r_sc-Moon <= r_L2-Moon.
- Step 4: minimize Keplerian energy wrt Moon (free: dv, angles, tf; constraint: end at perilune). SQP substitute: our pattern search with the perilune constraint folded in (penalty or lattice), or STM-gradient descent - decide by measurement, document.
- Success metrics (paper, adopt verbatim): KEm < 0 at final perilune; a SECOND perilune with KEm < 0 on forward propagation; perilune radii above the lunar surface. Gate-pin all three on one canonical epoch + the dv/TOF bands (~3.0-3.2 km/s, 90-180 d).

### B4 - authoring surface
- The node map/launch planner offers "Ballistic (WSB)" alongside the direct transfer where the target is lunar (NRHO/LLO/DRO): compute-button contract (E2s lane - this is a seconds-scale solve), honest est. before compute (paper dv/TOF bands), STALE on upstream edits. Arrival is co-orbital; phase-matching composes with 5b R2/R3 (the tf lattice here is per-month, coarser - document).

### Sequencing
After 5b (user-approved order). B1 standalone-committable; B2 offline harness; B3 the solver; B4 UI. Risks: our mean-element ephemeris vs DE405 (expect band-level agreement, not their exact numbers - pin OUR numbers); step-4 minimization method choice (measure first).

---

## 22. TRANSFER CHAINS — injection separated from destination (user go 2026-07-16)

**The complaint (user, 2026-07-15/16):** departure and injection are two burns but are folded into one node, because the model over-focuses on "get to a specific orbit" — "what we are not focused on enough is being able to have a destination set up without having it all in one node."

**Decisions (user, 2026-07-16):** NO legacy support — breakage fine, replace outright (no split-button migration path; dev seeds updated in the same change). Chain ownership = LOOSE-WITH-RESOLVE: the solver writes the chain ONCE at authoring; thereafter the events are user-owned like any others; the group header carries a re-solve button that rewrites members (one undo capture); staleness surfaces through the existing readiness machinery, never auto-overwrites.

### C1 — the chain model
- Authoring a transfer to a destination (node-map edge insert, Add Event maneuver flow, launch-planner handoff) produces a GROUPED CHAIN of real log entries bound by the existing m.groups machinery (new group kind 'transfer', header shows route + total dv + the re-solve control):
  - BURN departure ("TLI"-class) at the departure MET
  - BURN "MCC" ONLY when the solver actually produced one (5a Stage-2; BLT small-DSM when B4 lands)
  - BURN injection ("<dest> injection") at the arrival MET
- **One-source rule preserved:** each chain members solved magnitude comes from the leg records existing components (departure split, leg.mccBurn, leg.arrivalBurn) — exec/replay never recomputes physics. The node maps schematic edge pricing (progNmComputeEdgeDv) stays untouched as the requires readout.
- **Budget honesty (D6):** each burn charges dv + prop AT ITS OWN MET — the prop-mass timeline becomes physically truthful (today the whole transfer charges at departure). Totals unchanged within tolerance; any golden asserting single-event charging re-pins with cause.
- **Flybys fall out free:** deleting the injection member = the vehicle honestly continues past the target on the transfer leg. Readiness notes it as an INFO-level "no insertion — flyby" line (not a warning; it is a legitimate architecture).

### C2 — re-solve + staleness semantics
- Group-header re-solve: re-runs the transfer solve with the CURRENT authored inputs (departure event MET, destination orbit, vehicle state at entry) and rewrites the member events in place (one undo capture). Never triggered implicitly.
- Staleness: editing the destination orbit, the departure time, or upstream mass WITHOUT re-solving flags the chain amber via the existing checks pattern ("chain inputs changed since solve"); members individually editable (they are just burns — manual overrides live like any dvOverride).
- 5b phasing pairs and (later) B4 BLT DSMs author INTO the chain as members — same grouping, same rules.

### C3 — replacement scope (no legacy)
- The combined-maneuver authoring paths (node-map edge insert, maneuver Add Event with from/to) author chains instead. The old single-node MNODE remains valid ONLY as a plain manual burn / detached maneuver (mode:'manual' unaffected); the SOLVED from-to maneuver becomes the chain form.
- devSeedApolloMission / devSeedGatewayMission updated to author chains; all goldens that count events or assert single-node dv re-pin in the same commit (D6, documented cause).
- Gate: chain authoring produces the expected members with component dvs summing to the previous combined total (tolerance); per-MET budget charging; flyby-on-delete; re-solve rewrite; staleness flag.

Sequencing: single Sonnet build pass (C1-C3 together — they are one coherent change), gate + browser verified; docs/MATH.md section on the accounting change in the same commit.

### DONE (2026-07-16) — evidence

- **C1 chain authoring:** `missionExecManeuver` (570-mission-panel.js) authors a `m.groups[gid]={kind:'transfer',...}` chain for the two-hop corridor pattern (both dev seeds). Entry edge (e.g. LEO→TLC) stays the unified solved MNODE, tagged `chainRole:'depart'` — reused verbatim so 565's `lastTransit` corridor machinery is untouched. Exit edge (e.g. TLC→destination) is split by `_missionChainSplitInject` into an optional MCC `BURN`/`CUSTOM` member (`chainRole:'mcc'`, dv from `leg.mccBurn`) plus the original MNODE re-tagged `chainRole:'inject'` (dv stamped from `leg.arrivalBurn`). No parallel exec path — MCC rides the existing BURN/CUSTOM lane (same as §15 5b's phasing-burn pair), injection rides the existing solved-MNODE lane.
- **Component sourcing:** verified on `devSeedGatewayMission` (true 9:2 `nrho-nominal`, canonical epoch) — depart 3,143 m/s (fixed solver input) / MCC 519 m/s / injection 2,762 m/s, all read straight off the ONE leg record `physRebuildMissionTrajectories` already produced (no re-solve). Gate: `tests/math.test.js` "§22" block (+16 assertions) proves MCC+injection MET-spacing sums exactly to the entry leg's own `tof_s`, and both components are strictly positive when present.
- **Budget honesty (D6):** each member charges its own dv+prop at its own MET (verified: prop timeline steps at T+17:28 for depart+MCC, T+8d06:22 for injection). Mission total ΔV for a corridor transfer now goes UP relative to the pre-chain model (MCC + the injection delta were previously uncharged, display-only) — documented as the intended honesty gain in docs/MATH.md §7ak, with the re-pin cause spelled out there. No old goldens asserted a combined single-node dv for these seeds, so nothing needed re-pinning beyond the gate additions above.
- **C2 loose-with-resolve:** group header renders route + `ΣΔV` + `↻ Re-solve` (570-mission-lifecycle.js). `missionChainResolve` (570-mission-panel.js) verified end-to-end: shifting the seed's DEPLOY timing 5 days moves the depart MET 1,047s→433,048s, which correctly flags `chain-stale` (amber, 572-mission-checks.js); re-solving holds departure at 3,143 m/s while MCC (519→485) and injection (2,762→1,800) both move to the new arrival geometry — ONE undo-stack entry per resolve (verified: stack length +1). Known gap: staleness does not fingerprint upstream vehicle mass (documented in MATH.md §7ak).
- **C1 flyby:** verified — deleting the injection member leaves `orbitState.transit:true, destination:'Moon'` (never arrives), fires an INFO "No insertion — flyby" finding (not amber/red).
- **C3 no legacy:** the solved from→to single-node MNODE path is fully replaced by the above; manual/detached MNODEs (`mode:'manual'`) are untouched (chain code paths only ever touch `mode:'solved'` entries). Both dev seeds updated automatically (no seed code changes needed — they call the same `missionExecManeuver`/`missionExecManeuver` entry points, which now author chains transparently).
- **Gate:** 877→893 assertions (+16), 0 failures. Full per-deliverable browser evidence and the component/budget numbers are in the session's final report (not duplicated here).
- Scope boundary (honest): the mcc/inject split only fires for the two-hop corridor pattern exercised by both seeds; a hypothetical direct one-hop NRHO edge (skipping the transit waypoint) is unimplemented, not merely untested — see MATH.md §7ak "Scope, honestly bounded."
